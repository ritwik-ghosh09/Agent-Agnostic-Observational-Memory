@startuml
!include /Users/q284340/Agentic/coding/docs/puml/_standard-style.puml

actor MCPAgent as "MCP Agent"
actor Developer as "Developer"
actor User as "User"

usecase "Manage Knowledge Graph" as (Manage Knowledge Graph)
usecase "Perform Data Synchronization" as (Perform Data Synchronization)
usecase "Visualize Knowledge Graph" as (Visualize Knowledge Graph)
usecase "Ensure Data Consistency" as (Ensure Data Consistency)
usecase "Maintain Codebase" as (Maintain Codebase)

package "Graph Database" {
  component GraphDatabaseService as "Graph Database Service"
  component LevelDB as "LevelDB"
  component Graphology as "Graphology"
}

package "Data Synchronization" {
  component GraphKnowledgeExporter as "Graph Knowledge Exporter"
  component JSONExporter as "JSON Exporter"
}

package "Knowledge Operations" {
  component PersistenceAgent as "Persistence Agent"
  component VkbApiClient as "VkbApiClient"
}

MCPAgent -- (Manage Knowledge Graph)
Developer -- (Maintain Codebase)
User -- (Visualize Knowledge Graph)

(Manage Knowledge Graph) ..> (Perform Data Synchronization) : includes
(Perform Data Synchronization) ..> (Ensure Data Consistency) : extends
(Visualize Knowledge Graph) ..> (Manage Knowledge Graph) : includes

GraphDatabaseService ..> Graphology : uses
GraphDatabaseService ..> LevelDB : uses
GraphKnowledgeExporter ..> GraphDatabaseService : uses
PersistenceAgent ..> GraphDatabaseService : uses
VkbApiClient ..> GraphDatabaseService : uses

@enduml