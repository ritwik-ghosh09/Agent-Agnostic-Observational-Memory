@startuml
!include /Users/q284340/Agentic/coding/docs/puml/_standard-style.puml

actor MCPAgent as "MCP Agent"
actor VkbApiClient as "VKB API Client"
actor GraphDatabaseService as "Graph Database Service"

usecase "Knowledge Storage" as (KnowledgeStorage)
usecase "Visualization" as (Visualization)
usecase "Knowledge Operations" as (KnowledgeOperations)
usecase "Graph Synchronization" as (GraphSynchronization)

package "Knowledge Management" {
  (KnowledgeStorage)
  (KnowledgeOperations)
}

package "Visualization Tools" {
  (Visualization)
}

MCPAgent -- (KnowledgeOperations)
VkbApiClient -- (Visualization)
GraphDatabaseService -- (KnowledgeStorage)

(KnowledgeOperations) ..> (KnowledgeStorage) : include
(Visualization) ..> (KnowledgeStorage) : extend
(GraphSynchronization) ..> (KnowledgeStorage) : include

package "Semantic Analysis" {
  (GraphSynchronization)
}

MCPAgent -- (GraphSynchronization)

@enduml