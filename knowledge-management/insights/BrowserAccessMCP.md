# BrowserAccessMCP

**Type:** SubComponent

BrowserAccessMCP's implementation details can be found in the integrations/browser-access directory, serving as a primary point of reference for browser-related functionality.

## What It Is  

BrowserAccessMCP is the **browser‑based access layer** of the **LiveLoggingSystem**.  Its source lives under the **`integrations/browser-access/`** folder, with the primary entry point being the **`README.md`** file that documents the sub‑component’s purpose and usage.  The component’s responsibility is to expose a web UI that lets users view live logs generated by the system and to receive real‑time updates through a Server‑Sent Events (SSE) stream.  Two configuration keys drive its behaviour:  

* **`BROWSER_ACCESS_PORT`** – the TCP port on which the browser UI server listens.  
* **`BROWSER_ACCESS_SSE_URL`** – the endpoint URL that the UI connects to for SSE‑based push of log events.  

Together these settings enable a lightweight HTTP server that serves static assets (HTML/JS) and maintains a persistent SSE connection to push new log entries to connected browsers.

---

## Architecture and Design  

The observations point to a **modular integration architecture**.  LiveLoggingSystem is organized into clearly separated directories (`integrations/`, `scripts/`, `lib/`), each housing a self‑contained feature.  BrowserAccessMCP follows this pattern by residing in its own **`integrations/browser-access/`** directory, isolated from other concerns such as storage (`GraphDatabaseAdapter`) or semantic analysis (`OntologyClassificationAgent`).  

The design leans on **configuration‑driven deployment**: the two configuration keys (`BROWSER_ACCESS_PORT` and `BROWSER_ACCESS_SSE_URL`) are the only explicit knobs mentioned, indicating that the component does not embed hard‑coded network details.  This makes the UI server portable across environments (local dev, staging, production) without code changes.  

The use of **Server‑Sent Events** (exposed via `BROWSER_ACCESS_SSE_URL`) reveals a **push‑based real‑time communication pattern**.  Rather than polling for new log entries, the browser client opens a single HTTP connection that the server keeps open, streaming log lines as they arrive.  This pattern reduces latency and network overhead, which is especially beneficial for a live‑logging view where timeliness is critical.

Because the component is documented only in a README, the actual implementation is likely a small HTTP server (e.g., Express, Fastify, or a minimal Node.js static‑file server) that serves the UI bundle and forwards SSE traffic from the LiveLoggingSystem’s internal event bus to the browser.

---

## Implementation Details  

The only concrete artefacts we have are the **README.md** file and the two configuration keys.  From the README we can infer the following logical pieces:

1. **Static UI assets** – HTML, CSS, and JavaScript that render the log view.  These are probably bundled in the same `integrations/browser-access/` folder or a sub‑folder such as `public/` or `dist/`.  
2. **HTTP server entry point** – a small server that listens on `BROWSER_ACCESS_PORT`.  Its responsibilities are to serve the static UI files and to expose the SSE endpoint defined by `BROWSER_ACCESS_SSE_URL`.  
3. **SSE bridge** – code that subscribes to the LiveLoggingSystem’s internal log event emitter (or message queue) and forwards each event to every open SSE client.  The bridge must handle client connection lifecycle (open, heartbeat, close) and back‑pressure gracefully.  

No concrete class or function names are present in the observations, so the implementation likely follows conventional naming (e.g., `startServer()`, `setupSseEndpoint()`).  The component’s simplicity is intentional: by keeping the UI thin and delegating all heavy lifting (log collection, filtering, persistence) to the parent LiveLoggingSystem, BrowserAccessMCP remains focused on presentation and real‑time delivery.

---

## Integration Points  

BrowserAccessMCP sits **directly beneath LiveLoggingSystem**, acting as a consumer of the system’s live‑log events.  The integration occurs at two levels:

* **Configuration layer** – The parent system supplies `BROWSER_ACCESS_PORT` and `BROWSER_ACCESS_SSE_URL` (likely via a central configuration file or environment variables).  Changing these values changes where the UI is reachable and which internal endpoint it listens to for events.  
* **Event bus / logging pipeline** – The LiveLoggingSystem emits log entries to an internal channel (e.g., an EventEmitter, a message broker, or a custom observable).  BrowserAccessMCP subscribes to this channel and re‑emits the data over SSE.  This coupling is loose: the UI component does not need to understand how logs are produced, only that they are emitted on the agreed‑upon channel.  

Because sibling integrations such as **OntologyClassificationAgent** and **CodeGraphRAG** also consume the same core services (graph database, semantic analysis), they share the same modular integration pattern—each lives in its own folder, declares its own configuration, and plugs into the central system via well‑defined interfaces.  BrowserAccessMCP therefore benefits from the same maintainability guarantees as its siblings.

---

## Usage Guidelines  

1. **Configure before start‑up** – Ensure that `BROWSER_ACCESS_PORT` is set to an unused port on the host and that `BROWSER_ACCESS_SSE_URL` points to the correct internal SSE endpoint (e.g., `/logs/stream`).  These values are typically supplied through environment variables or a central `config.yaml`.  
2. **Run the UI server after the LiveLoggingSystem is alive** – The SSE stream will only emit data when the logging subsystem is running.  Starting BrowserAccessMCP first will result in an empty UI until the parent component begins publishing logs.  
3. **Do not modify the static assets directly in production** – If UI changes are required, rebuild the assets in a development branch and redeploy the entire `integrations/browser-access/` folder.  This avoids mismatches between the server’s expectations (e.g., the SSE endpoint path) and the client code.  
4. **Monitor connection health** – The SSE client in the browser should implement a reconnection strategy (standard SSE behavior).  Developers adding custom UI logic should respect the `EventSource` API’s `onerror` handling to re‑establish the stream when the server restarts.  
5. **Keep the component stateless** – All state (e.g., current log cursor) should be maintained on the server side or in the LiveLoggingSystem.  The UI should treat each incoming event as an immutable append‑only record to preserve simplicity and avoid synchronization bugs.

---

### Architectural patterns identified  

| Pattern | Evidence |
|---------|----------|
| **Modular integration** | Separate `integrations/` directory, BrowserAccessMCP lives in its own folder. |
| **Configuration‑driven deployment** | `BROWSER_ACCESS_PORT` and `BROWSER_ACCESS_SSE_URL` are externalized settings. |
| **Server‑Sent Events (push‑based real‑time)** | `BROWSER_ACCESS_SSE_URL` used for real‑time updates. |
| **Thin client / thin server** | UI only renders logs; all log generation stays in LiveLoggingSystem. |

### Design decisions and trade‑offs  

* **SSE vs. WebSockets** – SSE is simpler to implement (unidirectional, built‑in to browsers) and suffices for log streaming, but it cannot handle bidirectional messages.  The trade‑off favors lower complexity at the cost of flexibility.  
* **Isolated integration folder** – Improves maintainability and allows independent versioning, but introduces a small overhead for cross‑component discovery (e.g., shared types must be duplicated or centrally exported).  
* **Port‑based server** – Exposing a dedicated port isolates the UI from other services, simplifying network policies, yet requires careful port management in environments with many integrations.  

### System structure insights  

The overall system follows a **layered modular structure**: core services (graph DB, logging, semantic agents) reside in `storage/` and `integrations/` sub‑folders, while each integration (including BrowserAccessMCP) provides a thin façade that consumes core APIs.  This hierarchy enables developers to add new UI or processing modules without touching the central business logic, mirroring the pattern used by siblings like **OntologyClassificationAgent** and **CodeGraphRAG**.

### Scalability considerations  

* **Horizontal scaling of the UI server** – Because the UI is stateless and only forwards SSE events, multiple instances can be placed behind a load balancer, each listening on the same `BROWSER_ACCESS_PORT` (or different ports with a proxy).  The underlying event source must support multiple subscribers.  
* **SSE connection limits** – Each browser holds an open HTTP connection; the server must be configured to handle the expected concurrent client count (e.g., increase `maxListeners` or file descriptor limits).  
* **Separation of concerns** – By delegating log storage and processing to LiveLoggingSystem, BrowserAccessMCP can scale independently; bottlenecks will more likely appear in the log emitter rather than the UI.  

### Maintainability assessment  

The component’s **minimal footprint** (a README, static assets, a small HTTP server) makes it easy to understand and modify.  Its reliance on external configuration reduces hard‑coded values, simplifying environment‑specific changes.  However, the lack of explicit code symbols in the repository (as per the observations) suggests that the implementation may be intentionally lightweight or possibly generated at build time.  As long as the README stays up‑to‑date and the configuration contract (`BROWSER_ACCESS_PORT`, `BROWSER_ACCESS_SSE_URL`) remains stable, future developers should encounter low cognitive overhead when extending or debugging the UI integration.

## Diagrams

### Relationship

## Architecture Diagrams

## Hierarchy Context

### Parent
- [LiveLoggingSystem](./LiveLoggingSystem.md) -- [LLM] The LiveLoggingSystem component utilizes a modular design, with separate modules for different agents and functionalities. This is evident in the directory structure, where integrations, scripts, and lib are separate directories. For example, the GraphDatabaseAdapter is located in storage/graph-database-adapter.ts, and the OntologyClassificationAgent is located in integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts. This modular design allows for easy maintenance and scalability of the system.

### Siblings
- [GraphDatabaseAdapter](./GraphDatabaseAdapter.md) -- GraphDatabaseAdapter is implemented in storage/graph-database-adapter.ts, showcasing a modular design for database interactions.
- [OntologyClassificationAgent](./OntologyClassificationAgent.md) -- OntologyClassificationAgent is implemented in integrations/mcp-server-semantic-analysis/src/agents/ontology-classification-agent.ts, demonstrating a modular approach to data classification.
- [CodeGraphRAG](./CodeGraphRAG.md) -- CodeGraphRAG is implemented in integrations/code-graph-rag/README.md, showcasing a graph-based representation of code.

---

*Generated from 5 observations*
