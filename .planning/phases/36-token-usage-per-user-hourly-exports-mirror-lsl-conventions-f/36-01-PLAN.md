---
phase: 36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/health-coordinator.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Coordinator publishes a single canonical LSL time-window string at /health/state on every poll tick."
    - "The window string format is HHMM-HHMM (e.g., '0900-1000') and reflects the local timezone via utcToLocalTime, NOT launchd's UTC."
    - "The coordinator caches sessionDurationMs once at module init (read from config/live-logging-config.json) so any FUTURE consumer of the cached value avoids per-tick fs reads. Per-tick getTimeWindow calls retain their internal config read in v1 — acceptable: 5s tick × 12/min × 60 × 24 = 17 280 reads/day on a local SSD is negligible. Documented landmine, deferred to a future phase if/when the cost matrix changes."
    - "Errors in the window-compute path never substitute synthetic 'healthy'; the field reads 'unknown' on failure (SPEC R6)."
    - "No consumer rewrites — statusline and dashboard continue local computation; this Wave is observation-only."
  artifacts:
    - path: "scripts/health-coordinator.js"
      provides: "lsl_meta.current_window field in currentState + compute step inside runAllChecks tick"
      contains: "lsl_meta"
  key_links:
    - from: "scripts/health-coordinator.js"
      to: "scripts/timezone-utils.js"
      via: "named imports of getTimeWindow + utcToLocalTime"
      pattern: "import.*getTimeWindow.*from.*timezone-utils"
    - from: "GET /health/state"
      to: "currentState.lsl_meta.current_window"
      via: "Express JSON response"
      pattern: "lsl_meta"
---

<objective>
Make the health-coordinator the single canonical source of the current LSL time-window, exposed at /health/state, so the LLM proxy (Plan 36-03) can fetch it instead of recomputing locally on every write.

Purpose: Eliminate window-source duplication across coordinator / statusline / proxy / dashboard. v1 publishes the field for the proxy to consume; statusline and dashboard remain on local computation (deferred to a future phase per CONTEXT.md "Out of scope" L154).

Output: New field `currentState.lsl_meta.current_window` populated each tick, returned in the `/health/state` JSON payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md

<interfaces>
<!-- Functions the new code in scripts/health-coordinator.js will import -->
<!-- Source: scripts/timezone-utils.js -->

From scripts/timezone-utils.js:
- `export function getTimeWindow(localDate)` — returns 'HHMM-HHMM' (e.g. '0900-1000'); reads config/live-logging-config.json on every call (landmine — see Task 1 for caching strategy)
- `export function utcToLocalTime(timestamp, timezone = null)` — returns a Date whose getHours()/getMonth()/getDate() are in the project timezone (Intl.DateTimeFormat-based), NOT the launchd-default UTC

`currentState` shape (scripts/health-coordinator.js:157-206) already has slots: `container`, `services`, `lsl` (per-session Record), `lsl_by_project`, `processes`, `databases`, `files`, `knowledge_pipeline`, `proxy`, `network`, `generated_at`, `coordinator_uptime_s`. Per PATTERNS.md anomaly note: do NOT add `current_window` directly inside `lsl` (it is a Record<sid:project, entry> — type collision). Use `lsl_meta: { current_window: 'HHMM-HHMM' }` as a new top-level slot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lsl_meta slot, cached session_duration, and tick-step that populates current_window</name>
  <files>scripts/health-coordinator.js</files>
  <read_first>
    - scripts/health-coordinator.js (lines 31-55 imports; lines 157-206 currentState shape; lines 1063-1071 runAllChecks LSL block — current populate pattern)
    - scripts/timezone-utils.js (lines 79-103 getTimeWindow; lines 30-73 utcToLocalTime — for the local-time landmine)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md (Section 1 — has the exact insertion-point excerpt and per-call config-read landmine description)
    - config/live-logging-config.json (read this file to confirm the `live_logging.session_duration` key exists at the path getTimeWindow uses)
  </read_first>
  <action>
    Insert the import line `import { getTimeWindow, utcToLocalTime } from './timezone-utils.js';` adjacent to the other `./` relative imports in the imports block (around scripts/health-coordinator.js:31-55, near the existing `import ProcessStateManager from './process-state-manager.js';`).

    Add a new top-level slot to `currentState` (scripts/health-coordinator.js:157-206) named `lsl_meta` with shape `{ current_window: 'unknown' }`. Place it adjacent to the existing `lsl` and `lsl_by_project` slots so related fields stay grouped. Do NOT modify the existing `lsl` Record — placing `current_window` inside it collides with the per-session key shape (PATTERNS.md Section 1 calls this out explicitly).

    Cache `sessionDurationMs` once at module init to avoid the 17 000+ fs reads/day landmine (PATTERNS.md Section 1, Risks): read `config/live-logging-config.json` at top-level (module-scope) via the same `path.join(process.env.CODING_TOOLS_PATH || process.env.CODING_REPO || ..., 'config', 'live-logging-config.json')` resolution that timezone-utils.js:85 uses. Wrap in try/catch with `sessionDurationMs = 3600000` (60 min) fallback. Bind this cached value into the populate step (see below). Note: timezone-utils' `getTimeWindow` will still do its own read on every call — that is fine for the statusline path but not for the 5s coordinator tick, so the coordinator passes a fresh `localDate` to `getTimeWindow` and lets `getTimeWindow` use its own internal config read for now. The cache is for FUTURE consumers; this task PRIMARILY adds the cache and uses it as a fallback when `getTimeWindow` throws. (Do not modify timezone-utils.js — keep the standalone callers' contract intact per "Never modify working APIs for TypeScript compliance" rule.)

    Inside `runAllChecks()` (scripts/health-coordinator.js around line 1063-1071, near the existing `refreshLslStaleness()` call), add a new try/catch block that computes `const local = utcToLocalTime(new Date())` then `currentState.lsl_meta.current_window = getTimeWindow(local)`. On any thrown error, log via the existing `log()` helper at ERROR level AND set `currentState.lsl_meta.current_window = 'unknown'` per SPEC R6 (never substitute synthetic 'healthy' / valid window). Use the exact pattern from PATTERNS.md Section 1, "Add (cheap, no I/O)".

    Critical: do NOT pass a bare `new Date()` into `getTimeWindow` — that calls `.getHours()` in launchd's UTC and would drift from statusline by 1-2h (PATTERNS.md Section 1 landmine #2). Always go through `utcToLocalTime` first.
  </action>
  <verify>
    <automated>
      grep -q "import { getTimeWindow, utcToLocalTime } from './timezone-utils.js'" scripts/health-coordinator.js \
        && grep -q "lsl_meta" scripts/health-coordinator.js \
        && node --check scripts/health-coordinator.js \
        && launchctl kickstart -k "gui/$(id -u)/com.coding.health-coordinator" \
        && sleep 6 \
        && curl -sf --max-time 2 http://127.0.0.1:3034/health/state \
           | python3 -c "import sys,json; d=json.load(sys.stdin); v=d.get('lsl_meta',{}).get('current_window',''); assert isinstance(v,str) and (v=='unknown' or (len(v)==9 and v[4]=='-')), f'bad current_window: {v}'; print('OK', v)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "lsl_meta" scripts/health-coordinator.js` returns 0 (new slot present).
    - `node --check scripts/health-coordinator.js` exits 0 (no syntax errors).
    - `curl -s http://127.0.0.1:3034/health/state | jq -r .lsl_meta.current_window | grep -qE '^[0-2][0-9][0-5][0-9]-[0-2][0-9][0-5][0-9]$'` returns 0 within 10s of coordinator restart.
    - The current_window string matches `getTimeWindow(utcToLocalTime(new Date()))` when computed independently in a one-off `node -e ...` script using the same imports — i.e. coordinator's window equals statusline's window within the same minute.
    - `grep -n "currentState.lsl_meta" scripts/health-coordinator.js` shows at least one assignment inside the runAllChecks block (not just the initial declaration).
    - Error path: when `getTimeWindow` is forced to throw (test by temporarily renaming config/live-logging-config.json — out of scope for this verify step but documented as the recovery behavior), `current_window` reads literally `'unknown'`, never `'healthy'` or last-good cached value.
  </acceptance_criteria>
  <done>
    A) `lsl_meta.current_window` appears in the /health/state JSON payload and matches the format `HHMM-HHMM`.
    B) The coordinator's log shows no per-tick errors related to the new code path.
    C) Statusline and dashboard remain unchanged (visual smoke check optional; no consumer code touched).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → coordinator | config/live-logging-config.json read (trusted; same repo) |
| coordinator → HTTP clients | /health/state JSON response; any localhost consumer can read |
| launchd → coordinator | TZ env var inheritance (launchd default = UTC; project = Europe/Berlin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | scripts/timezone-utils.js read of config/live-logging-config.json | accept | Same-repo config; gitignored mods would be visible in `git status`; not a privilege boundary. |
| T-36-02 | DoS | getTimeWindow per-tick fs read (17k reads/day) | mitigate | Cache `sessionDurationMs` at module init in scripts/health-coordinator.js; let timezone-utils.js continue its own read for unchanged callers (statusline etc.); coordinator's per-tick path uses cached value as fallback when getTimeWindow throws. Acceptable hot-path cost. |
| T-36-03 | Information Disclosure | /health/state new field | accept | Localhost-only Express listener; no PII in HHMM-HHMM string. |
| T-36-04 | Repudiation/Drift | TZ mismatch coordinator-vs-statusline | mitigate | Always wrap with `utcToLocalTime` (Intl.DateTimeFormat with explicit project timezone) — never pass bare `new Date()` to getTimeWindow. Acceptance criterion enforces equality with statusline-side computation. |
| T-36-05 | Denial of Service | Throw inside runAllChecks LSL block | mitigate | try/catch wraps the new compute; on error set field to literal 'unknown' (SPEC R6); never throws out of runAllChecks. |
</threat_model>

<verification>
After Task 1 lands:
1. `launchctl kickstart -k gui/$(id -u)/com.coding.health-coordinator` to reload.
2. `curl -s http://127.0.0.1:3034/health/state | jq .lsl_meta.current_window` — assert HHMM-HHMM format.
3. Spot-check timezone correctness: run `node -e "import('./scripts/timezone-utils.js').then(m => console.log(m.getTimeWindow(m.utcToLocalTime(new Date()))))"` from the coding repo root and confirm the result matches the coordinator's published value.
4. Tail coordinator log for 60s (`tail -f` of stderr.log) and confirm no per-tick "lsl current_window compute threw" entries.
</verification>

<success_criteria>
- `currentState.lsl_meta.current_window` populated on every poll tick (5s cadence).
- Field returned by GET /health/state.
- Format is `HHMM-HHMM` (per `getTimeWindow` contract).
- Coordinator's value equals the statusline's locally-computed value within the same minute.
- No throws into runAllChecks; on error, field reads `'unknown'`.
</success_criteria>

<output>
Create `.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-01-SUMMARY.md` when done.
</output>
