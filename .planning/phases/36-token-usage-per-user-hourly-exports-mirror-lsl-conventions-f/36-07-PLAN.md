---
phase: 36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f
plan: 07
type: execute
wave: 5
depends_on: []
files_modified:
  - integrations/system-health-dashboard/src/pages/token-usage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every box in the Token Consumption by Process treemap shows a hover tooltip — even boxes too small for inline text (< 40×30 px, which the existing `TreemapContent` skips on line 133)."
    - "Tooltip content shows process name + total tokens + input/output split + call count + avg latency — NOT just the value formatter that's used elsewhere on the page (the existing `<Tooltip formatter={(val) => formatTokens(val)} />` at line 389 is for the BarChart, not the treemap)."
    - "The 'Hover for details' subtitle in the treemap card header (line ~605) becomes truthful — currently it is aspirational."
    - "Small boxes that don't render an inline label still render an SVG `<title>` element so non-mouse navigation (touch, screen readers) gets the same label that hover provides — accessibility + browser-native fallback if the recharts tooltip fails to attach."
    - "The Tooltip component is wired through recharts' Treemap children — NOT as a sibling — and uses a custom React component for the content (matching the page's existing `Tooltip` usage pattern at lines 389, 498, plus the other custom-tooltip elsewhere)."
    - "After this plan lands, the dashboard at `http://localhost:3032/token-usage` shows the same process treemap with hover working on every box, no other visual regressions."
  artifacts:
    - path: "integrations/system-health-dashboard/src/pages/token-usage.tsx"
      provides: "TreemapTooltip custom component + <Tooltip content={<TreemapTooltip/>}/> child + <title> SVG fallback inside TreemapContent"
      contains: "TreemapTooltip"
  key_links:
    - from: "integrations/system-health-dashboard/src/pages/token-usage.tsx Treemap (line ~354)"
      to: "TreemapTooltip (new component in same file)"
      via: "content prop on the Tooltip child of Treemap"
      pattern: "<Tooltip content={<TreemapTooltip"
---

<objective>
Make the token-usage treemap match the promise of its "Hover for details" subtitle. Today the only inline labels are the ones rendered by the custom `TreemapContent` for boxes ≥ 40×30 px; smaller boxes are silent. Recharts' Treemap accepts a `<Tooltip>` child the same way the page's BarChart and PieChart already use elsewhere, but it isn't wired up.

Scope:
  (a) Custom tooltip component `TreemapTooltip` that receives `{ active, payload }` from recharts (same signature as other custom tooltips in the page) and renders process name + token total + in/out split + call count + avg latency in the dashboard's existing card-tooltip style.
  (b) Wire it as `<Tooltip content={<TreemapTooltip/>}/>` child of `<Treemap>` at the existing treemap render site (line ~354).
  (c) Add a `<title>` element inside `TreemapContent` for an accessibility/native-browser-tooltip fallback, regardless of whether the box renders text inline.
  (d) Rebuild the dashboard UI (FUSE-cache-aware deploy per CLAUDE.md) and verify in a real browser via /playwright-cli skill.

Out of scope:
- Tooltips for the BarChart / PieChart (already wired, line 389 + 498).
- Color scheme / palette changes.
- Showing different content based on which tab is active.
- Backend changes (Plan 36-06 covers the proxy-side model canonicalization that improves the data the treemap consumes; this plan only renders).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The existing treemap site -->

`integrations/system-health-dashboard/src/pages/token-usage.tsx`:

- Line 11: imports `Tooltip` from 'recharts'. The component is already available; only its use on the Treemap is missing.
- Lines 127-149: existing `TreemapContent` function — renders rect + optional inline label. We KEEP this for the visual box-rendering; we ADD a `<title>` child to its `<g>` for native-browser hover-text fallback.
- Lines 206-216: `treemapData` shape — each item has `{ name, value, fill, calls, avgLatency }`. We need to extend it to also carry `input_tokens` and `output_tokens` so the tooltip can show the in/out split. `summary.by_process` (per the live API capture) already has these fields — they're just dropped on the floor today.
- Lines 354-359: existing `<Treemap data={treemapData} dataKey="value" content={<TreemapContent .../>} />` — this is where we add the `<Tooltip content={<TreemapTooltip/>}/>` child.
- Line 389: existing `<Tooltip formatter={...} />` on the BarChart — for placement-style reference.
- Line 498: existing `<Tooltip ... />` (likely a custom-content tooltip elsewhere on the page) — for the local convention of custom tooltips.

<!-- The exact data shape the tooltip will receive -->

When recharts calls a custom Tooltip content component, it passes `{ active: boolean, payload: Array<{ payload: <leaf data> }> }`. For Treemap, `payload[0].payload` is the leaf object — i.e. the original treemapData[i] including all its fields.

<!-- The page's existing tooltip / card visual style -->

The page uses Tailwind utility classes for cards (look for `bg-card text-card-foreground border rounded` patterns inside the file). The new tooltip should match. If a `bg-popover` token exists in the page's other tooltips, use it. Otherwise: `bg-background border rounded-md shadow-md px-3 py-2 text-sm`.

<!-- The build + deploy convention (per CLAUDE.md) -->

The dashboard frontend is bind-mounted into the `coding-services` container:
  - Frontend bundle rebuild: `cd integrations/system-health-dashboard && npm run build`
  - Restart frontend supervisor: `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`
  - NO `docker-compose build` is needed for the frontend.
  - But: VirtioFS cache means a stale snapshot can serve. If the dashboard UI doesn't pick up the change after the frontend restart, fall back to `docker-compose restart coding-services` (the full container restart per CLAUDE.md guidance).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TreemapTooltip + wire it into the Treemap + SVG title fallback</name>
  <files>integrations/system-health-dashboard/src/pages/token-usage.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/pages/token-usage.tsx lines 1-60 (imports + types + helpers like `formatTokens`, `formatLatency`, `getProcessColor`)
    - integrations/system-health-dashboard/src/pages/token-usage.tsx lines 127-149 (current TreemapContent — for the SVG `<title>` insertion site)
    - integrations/system-health-dashboard/src/pages/token-usage.tsx lines 206-216 (treemapData mapping — needs `input_tokens` + `output_tokens` carried through)
    - integrations/system-health-dashboard/src/pages/token-usage.tsx lines 354-359 (Treemap render site)
    - integrations/system-health-dashboard/src/pages/token-usage.tsx lines 385-405 (BarChart with Tooltip — placement/style reference for the new Tooltip child)
    - integrations/system-health-dashboard/src/pages/token-usage.tsx lines 490-520 (whatever the third Tooltip site is — for the page's custom-content tooltip convention if present)
  </read_first>
  <action>
    (1) Extend `treemapData` mapping (line 210-216) to carry the input/output split:
        ```
        const treemapData = summary.by_process
          .filter(p => p.total_tokens > 0)
          .sort((a, b) => b.total_tokens - a.total_tokens)
          .map(p => ({
            name: p.process,
            value: p.total_tokens,
            fill: getProcessColor(p.process),
            calls: p.calls,
            avgLatency: p.avg_latency,
            input: p.input_tokens,    // ← new
            output: p.output_tokens,  // ← new
          }))
        ```

    (2) Add a new function `TreemapTooltip` next to the existing `TreemapContent` (after line 149). Shape:
        ```
        function TreemapTooltip({ active, payload }: { active?: boolean; payload?: Array<{ payload: any }> }) {
          if (!active || !payload?.length) return null
          const d = payload[0].payload
          return (
            <div className="bg-background border rounded-md shadow-md px-3 py-2 text-sm space-y-0.5">
              <div className="font-semibold">{d.name}</div>
              <div className="text-muted-foreground text-xs">
                {formatTokens(d.value)} tokens total
              </div>
              <div className="text-xs">
                <span className="text-blue-500">{formatTokens(d.input)} in</span>
                {' · '}
                <span className="text-green-500">{formatTokens(d.output)} out</span>
              </div>
              <div className="text-muted-foreground text-xs">
                {d.calls} {d.calls === 1 ? 'call' : 'calls'}
                {typeof d.avgLatency === 'number' && (
                  <> · avg {(d.avgLatency / 1000).toFixed(1)}s</>
                )}
              </div>
            </div>
          )
        }
        ```
        Match the file's existing JSX style (single-quote attributes / no semicolons / etc.) by reading the surrounding code first.

    (3) Add `<title>` SVG element inside `TreemapContent`'s `<g>` (line 134-148 region), as the FIRST child so it's accessible regardless of whether the inline-text branch runs:
        ```
        return (
          <g>
            <title>{`${name} — ${formatTokens(value)} tokens`}</title>
            <rect ... />
            {width > 60 && height > 40 && (
              ...existing inline text branch unchanged...
            )}
          </g>
        )
        ```
        This is the SVG-native hover tooltip that works in EVERY browser, regardless of whether recharts' Tooltip attaches correctly. Free fallback.

    (4) Wire the recharts Tooltip into the `<Treemap>` at line 354:
        ```
        <Treemap
          data={treemapData}
          dataKey="value"
          content={<TreemapContent x={0} y={0} width={0} height={0} name="" value={0} fill="" />}
        >
          <Tooltip content={<TreemapTooltip />} />
        </Treemap>
        ```
        NOTE: the Treemap self-closes today on line 359; convert it to opening/closing form.

    (5) Build the bundle: `cd integrations/system-health-dashboard && npm run build 2>&1 | tail -15` — must exit 0.

    (6) Restart the frontend supervisor: `docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend`.

    (7) Browser verify via `/playwright-cli` skill (per CLAUDE.md feedback memory on E2E verification):
        - Navigate to `http://localhost:3032/token-usage`.
        - Wait for the treemap to render (selector: `.recharts-treemap` or similar).
        - Hover over a SMALL box (one without inline text — pick the smallest fill rect by area).
        - Assert a tooltip appears with: process name, "tokens total", "in"/"out" labels, "calls", "avg".
        - Hover over a LARGE box (observation writer); same assertion.
        - Screenshot saved to `/tmp/36-07-tooltip-{small,large}.png` for the SUMMARY.

    (8) If VirtioFS cache prevents the change from taking effect (frontend serves the stale bundle even after supervisorctl restart), fall back per CLAUDE.md: `cd docker && docker-compose restart coding-services` and re-verify.
  </action>
  <verify>
    <automated>
      grep -q "function TreemapTooltip" integrations/system-health-dashboard/src/pages/token-usage.tsx \
        && grep -q "<Tooltip content={<TreemapTooltip" integrations/system-health-dashboard/src/pages/token-usage.tsx \
        && grep -q "<title>" integrations/system-health-dashboard/src/pages/token-usage.tsx \
        && grep -q "input: p.input_tokens" integrations/system-health-dashboard/src/pages/token-usage.tsx \
        && grep -q "output: p.output_tokens" integrations/system-health-dashboard/src/pages/token-usage.tsx \
        && (cd integrations/system-health-dashboard && npm run build 2>&1 | tail -3) \
        && docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend \
        && sleep 3 \
        && curl -sS -o /dev/null -w "%{http_code}" http://localhost:3032/token-usage | grep -q "200" \
        && echo "OK treemap tooltip wired and live"
    </automated>
  </verify>
  <acceptance_criteria>
    - `TreemapTooltip` defined as a React component, receives `{ active, payload }`.
    - `<Tooltip content={<TreemapTooltip/>}/>` is a child of `<Treemap>` — NOT a sibling.
    - `<title>` SVG element is the first child of `<g>` inside `TreemapContent`.
    - `treemapData` map carries `input_tokens` and `output_tokens` so the tooltip can render the in/out split.
    - `npm run build` exits 0.
    - Frontend supervisor restart succeeds.
    - `http://localhost:3032/token-usage` returns HTTP 200.
    - Playwright hover test passes on BOTH a small box and a large box; screenshots saved.
    - No new TypeScript errors (`npm run typecheck` or whatever convention the file uses).
    - `treemap` div in DOM has no extra console errors (check via `playwright page.on('console')` in the test).
  </acceptance_criteria>
  <done>
    A) Every treemap box has a working hover tooltip with in/out/calls/latency detail.
    B) SVG `<title>` fallback works for screen readers and as a defense if recharts ever breaks.
    C) Live dashboard verified end-to-end via /playwright-cli per CLAUDE.md.
    D) Visual screenshots captured for the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/token-usage/summary → React state | Untrusted upstream shape; payload[0].payload could be missing fields if API changes |
| TreemapTooltip render → DOM | Pure React render; no XSS surface (no dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-41 | Tampering | API response missing input_tokens / output_tokens on a row | mitigate | `formatTokens(d.input)` / `formatTokens(d.output)` should handle `undefined` — verify formatTokens signature; if it throws on undefined, add `d.input ?? 0` defaults. |
| T-36-42 | Tampering | A process name with HTML-special characters from the upstream | accept | React's default text rendering escapes; no dangerouslySetInnerHTML used. |
| T-36-43 | DoS | Treemap with 100+ leaves causes recharts to render tooltips on every mouseover | accept | Recharts already throttles tooltip render; we add one render of ~6 lines of JSX. Negligible. |
| T-36-44 | Information Disclosure | `<title>` element exposes the same data the visible card shows | accept | Same trust domain — local-only dashboard, no external exposure. |
| T-36-45 | Tampering | Stale VirtioFS cache means the rebuild doesn't take effect | mitigate | CLAUDE.md fallback documented: `docker-compose restart coding-services` when supervisor restart isn't enough. Task 1 step (8) explicitly invokes it on cache miss. |
</threat_model>

<verification>
1. `grep -c 'TreemapTooltip' integrations/system-health-dashboard/src/pages/token-usage.tsx` returns ≥ 2 (definition + use).
2. `grep -q '<title>' integrations/system-health-dashboard/src/pages/token-usage.tsx` — SVG title fallback present.
3. `cd integrations/system-health-dashboard && npm run build` exits 0 with no new TS errors.
4. `docker exec coding-services supervisorctl status web-services:health-dashboard-frontend` shows RUNNING.
5. `curl -sS -o /dev/null -w "%{http_code}" http://localhost:3032/token-usage` returns 200.
6. Playwright (via /playwright-cli skill): script navigates, waits for `.recharts-wrapper` or treemap selector, hovers a small + a large rect, asserts a tooltip popover appears containing the process name + "tokens" + "calls" + "in"/"out" text.
7. Visual screenshots: `/tmp/36-07-tooltip-small.png` + `/tmp/36-07-tooltip-large.png` exist.
8. No console errors in the browser when hovering — Playwright `page.on('console', msg => msg.type() === 'error' && fail)`.
9. The "Hover for details" subtitle (line ~605) — verify the user can hover any small box and see the tooltip (no longer aspirational).
</verification>

<success_criteria>
- Every box in the process treemap has a hover tooltip with process / total / in / out / calls / avg latency.
- SVG `<title>` fallback present for accessibility and as a recharts-failure safety net.
- Dashboard rebuilt + redeployed; Playwright E2E hover assertion passes on small AND large boxes.
- No visual regressions on the rest of the Token Usage page.
- Screenshots saved for the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-07-SUMMARY.md` when done.
</output>
</content>
</invoke>