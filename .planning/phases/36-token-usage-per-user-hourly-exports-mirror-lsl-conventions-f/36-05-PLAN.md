---
phase: 36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f
plan: 05
type: execute
wave: 4
depends_on:
  - 36-03
  - 36-04
files_modified:
  - .gitignore
  - scripts/migrate-token-usage-export.mjs
  - .data/llm-proxy-export/token-usage.json
autonomous: true
requirements: []

must_haves:
  truths:
    - ".gitignore covers .db-wal AND .db-shm (in addition to .db) — `git status` is clean for `.data/llm-proxy/` even when the SQLite WAL has data."
    - ".gitignore preserves the `!.data/llm-proxy-export/` allow-list (or adds it if missing) — the new per-hour JSON files MUST stay git-tracked."
    - "scripts/migrate-token-usage-export.mjs reads `.data/llm-proxy-export/token-usage.json` (the legacy monolithic file), buckets rows by (YYYY-MM-DD, getTimeWindow(utcToLocalTime(timestamp))), and writes one per-(date, window, hash) file under `.data/llm-proxy-export/YYYY/MM/`."
    - "The migration script uses scripts/user-hash-generator.js to compute the current user's hash (NOT hardcoded 'c197ef') — robust across machines if a second user runs the migration first."
    - "The migration script DELETES the legacy `.data/llm-proxy-export/token-usage.json` in the same git commit as the new per-hour files."
    - "The migration script supports --dry-run (prints bucketing plan without writing/deleting) AND is idempotent (re-running after the monolith is gone exits cleanly with `monolith already removed` message)."
    - "After the migration commit, `find .data/llm-proxy-export -name 'token-usage.json'` returns nothing AND `find .data/llm-proxy-export -name '*_*-*_*.json' | wc -l` returns >= 1 (count depends on how many distinct hour-windows the legacy 1457 rows cover)."
  artifacts:
    - path: ".gitignore"
      provides: "Coverage of .db-wal / .db-shm; preservation of !.data/llm-proxy-export/ allow-list"
      contains: ".db-wal"
    - path: "scripts/migrate-token-usage-export.mjs"
      provides: "One-shot bucketing of legacy monolithic export to per-(window, user) files; --dry-run; idempotent"
      contains: "buckets.set"
  key_links:
    - from: "scripts/migrate-token-usage-export.mjs"
      to: "scripts/timezone-utils.js getTimeWindow + utcToLocalTime"
      via: "named import"
      pattern: "from './timezone-utils.js'"
    - from: "scripts/migrate-token-usage-export.mjs"
      to: "scripts/user-hash-generator.js"
      via: "default import; generateHash()"
      pattern: "user-hash-generator"
    - from: "migration commit"
      to: "deletion of .data/llm-proxy-export/token-usage.json"
      via: "fs.unlinkSync inside the same script run"
      pattern: "unlinkSync"
---

<objective>
Close the phase with two filesystem-side changes that complete the migration story:
  (a) Widen `.gitignore` so `*.db-wal` / `*.db-shm` stop polluting `git status` (the original user pain point per CONTEXT.md L19).
  (b) Write + execute a one-shot migration script that converts the existing monolithic `.data/llm-proxy-export/token-usage.json` (637 KB, 1457 rows) into per-(date, window, user) files under the new layout, AND deletes the monolithic file in the same commit.

Purpose: After Plans 36-03 + 36-04 the proxy WRITES the new layout going forward, but the existing legacy file remains as dead weight in git history's working tree. This plan converts the legacy data losslessly into the new layout so the DB-hydrate path (Plan 36-04) has uniform input across history and future.

Output: One-line gitignore widen (or 2-3 explicit lines per repo convention) + new `scripts/migrate-token-usage-export.mjs` + a clean `.data/llm-proxy-export/` tree where the only `.json` files are per-hour ones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-03-SUMMARY.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-04-SUMMARY.md

<interfaces>
<!-- The migration script's imports (all from the coding repo, not the proxy submodule) -->

From scripts/timezone-utils.js:
- `export function getTimeWindow(localDate)` — returns 'HHMM-HHMM'
- `export function utcToLocalTime(timestamp, timezone = null)` — converts ISO UTC → local Date object

From scripts/lsl-paths.js:
- `export function dateSubdirFromFilename(filename)` — returns 'YYYY/MM' or null

From scripts/user-hash-generator.js:
- Default export with `generateHash({ debug = false } = {})` method — returns 6-char hex hash

<!-- The legacy file shape (from CONTEXT.md L19 + PATTERNS.md Section 2 SELECT columns) -->

`.data/llm-proxy-export/token-usage.json` = JSON array of objects, each with:
- id (int) | timestamp (ISO 8601 UTC) | provider | model | process | subscription |
- input_tokens | output_tokens | total_tokens | latency_ms | prompt_preview | tokens_estimated
- NO user_hash field (predates Plan 36-04 schema)

<!-- Existing .gitignore landmarks (verify before editing) -->

scripts/lsl-paths.js + .gitignore:179-183: `*.db / *.sqlite / *.sqlite3`
scripts/lsl-paths.js + .gitignore:191-193: `.data/knowledge.db / .db-shm / .db-wal` (precedent: explicit lines, NOT broadened `*.db*` glob)
scripts/lsl-paths.js + .gitignore:196-199: allow-list block — `!.data/`, `!.data/observation-export/`, etc. (verify `!.data/llm-proxy-export/` is present; if missing, ADD it before the wal/shm changes go in)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore — add WAL/SHM coverage with !.data/llm-proxy-export/ preserved</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (full file — confirm exact line numbers of the *.db block, the .data/knowledge.db-shm/-wal explicit precedent, and the !.data/* allow-list block)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md (Section 5 — has the exact existing-block excerpt + the convention guidance to use EXPLICIT per-file lines instead of broadened `*.db*` glob, matching the existing knowledge.db precedent)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md (Wave 5 paragraph; line 129)
  </read_first>
  <action>
    Open `.gitignore` and locate the database-files block (currently at lines 179-183 per PATTERNS.md Section 5):
      `# Database files`
      `*.db`
      `*.sqlite`
      `*.sqlite3`

    Replace this block with EXPLICIT per-file lines, matching the existing convention used at the `.data/knowledge.db` block (PATTERNS.md Section 5 explicitly identifies this as the convention to follow — DO NOT use `*.db*` broaden; that risks future filename-pattern surprises and the repo's style is explicit):

      `# Database files`
      `*.db`
      `*.db-wal`
      `*.db-shm`
      `*.db-journal`
      `*.sqlite`
      `*.sqlite3`

    Then verify the allow-list for `!.data/llm-proxy-export/`. Run `grep -n 'llm-proxy-export' .gitignore` — if NO output, add a new line `!.data/llm-proxy-export/` adjacent to the existing `!.data/observation-export/` line (PATTERNS.md Section 5 "Risks / landmines" #2 documents this requirement; the new per-hour JSON files MUST remain git-tracked or this whole phase is pointless).

    After saving:
    - Verify the `.data/llm-proxy/` dir disappears from `git status` (it was untracked due to `.db-wal` + `.db-shm` exposure; new lines cover them).
    - Verify the `.data/llm-proxy-export/` dir is still tracked: `git check-ignore -v .data/llm-proxy-export/somefile.json` should NOT match any ignore rule.

    CRITICAL: this gitignore change MUST land in its OWN commit, BEFORE the migration script runs, per CONTEXT.md L129 "One-line fix lands FIRST." Reasoning: if the gitignore widen lands in the SAME commit as the new per-hour files, the diff is noisy AND any `.db-wal` content currently present would NOT be retroactively ignored (it would already be tracked, if it ever was — though in this case it isn't tracked, the principle stands). The two-commit split also gives a clean revert path if the migration script has a bug.
  </action>
  <verify>
    <automated>
      grep -q "^\*\.db-wal$" .gitignore \
        && grep -q "^\*\.db-shm$" .gitignore \
        && grep -q "^\*\.db-journal$" .gitignore \
        && grep -q "^\*\.db$" .gitignore \
        && grep -q "llm-proxy-export" .gitignore \
        && ! git check-ignore -q .data/llm-proxy-export/2026/05/test.json 2>/dev/null \
        && git check-ignore -q .data/llm-proxy/token-usage.db-wal 2>/dev/null \
        && git check-ignore -q .data/llm-proxy/token-usage.db-shm 2>/dev/null \
        && echo "OK gitignore coverage"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\*\\.db" .gitignore` returns at least 4 (db, db-wal, db-shm, db-journal lines).
    - `grep -q "llm-proxy-export" .gitignore` returns 0 (allow-list line present).
    - `git check-ignore .data/llm-proxy/token-usage.db-wal` exits 0 with output (the file IS ignored).
    - `git check-ignore .data/llm-proxy/token-usage.db-shm` exits 0 with output.
    - `git check-ignore .data/llm-proxy-export/2026/05/sample.json` exits non-zero (the file is NOT ignored — the allow-list works).
    - `git status -s .data/llm-proxy/` shows NO entries (clean directory).
    - This change lands in its own commit (verifiable via `git log -1 --stat .gitignore` showing ONLY .gitignore in the diff, no co-mingled migration files).
  </acceptance_criteria>
  <done>
    A) `.db-wal`, `.db-shm`, `.db-journal` are gitignored.
    B) `.data/llm-proxy-export/` is preserved on the allow-list.
    C) `git status` is clean for `.data/llm-proxy/`.
    D) Committed in isolation, separate from the migration commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/migrate-token-usage-export.mjs (one-shot, --dry-run, idempotent)</name>
  <files>scripts/migrate-token-usage-export.mjs</files>
  <read_first>
    - scripts/migrate-lsl-to-yyyymm.js (full file, 63 lines — the YYYY/MM bucketing pattern; reuse `dateSubdirFromFilename`)
    - scripts/backfill-raw-observations.mjs (lines 25-53 — CLI arg parsing pattern; --dry-run + --limit; structured stderr log style)
    - scripts/timezone-utils.js (lines 30-73 utcToLocalTime; lines 79-103 getTimeWindow — these are what the script imports and uses)
    - scripts/lsl-paths.js (lines 17-49 dateSubdirFromFilename — utility import)
    - scripts/user-hash-generator.js (the default-export module — for `.generateHash()`)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md (Section 6 in full — has both analog excerpts + the difference-to-apply sketch + the 6 risks/landmines for this exact script)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md (Wave 5 numbered steps; line 132)
    - .data/llm-proxy-export/token-usage.json (peek the first few rows via `head -50` to confirm the shape matches PATTERNS.md Section 6 + verify it still has 1457 rows by `node -e "console.log(JSON.parse(require('fs').readFileSync('.data/llm-proxy-export/token-usage.json','utf-8')).length)"`)
  </read_first>
  <action>
    Create `scripts/migrate-token-usage-export.mjs` as a Node ESM script (matches the file extension convention in scripts/backfill-raw-observations.mjs). Shape:

    (1) Shebang line `#!/usr/bin/env node` + a top-of-file block comment explaining: "One-shot migration: bucket the legacy monolithic .data/llm-proxy-export/token-usage.json into per-(date, window, user) files under YYYY/MM/. Deletes the monolith on completion. Idempotent: re-running after the monolith is gone is a no-op. Phase 36."

    (2) Imports (top of file): `import fs from 'node:fs';`, `import path from 'node:path';`, `import { fileURLToPath } from 'node:url';`, then `import { getTimeWindow, utcToLocalTime } from './timezone-utils.js';` and `import { dateSubdirFromFilename } from './lsl-paths.js';` and `import UserHashGenerator from './user-hash-generator.js';` (default import — confirm the actual export shape; the file exports a class as default per existing usage in src/live-logging/).

    (3) CLI arg parsing (matches scripts/backfill-raw-observations.mjs pattern): `const args = process.argv.slice(2); const DRY_RUN = args.includes('--dry-run'); const VERBOSE = args.includes('--verbose');`. Print usage on `--help`.

    (4) Path resolution: derive `ROOT` from `fileURLToPath(import.meta.url)` → `path.resolve(scriptDir, '..')` (the coding repo root). Constants: `EXPORT_DIR = path.join(ROOT, '.data', 'llm-proxy-export');` and `MONO_PATH = path.join(EXPORT_DIR, 'token-usage.json');`.

    (5) Idempotency guard (PATTERNS.md Section 6 risk #5): at top of `main()`, if `!fs.existsSync(MONO_PATH)`, `process.stderr.write('[migrate-token-usage] monolith already removed — nothing to do\\n'); process.exit(0);`. This makes safe to re-run.

    (6) Compute current user's hash: `const userHash = UserHashGenerator.generateHash();` (no debug flag — clean stdout). Log via stderr: `process.stderr.write(\`[migrate-token-usage] user_hash=\${userHash}\\n\`);`.

    (7) Read + parse monolith: `let rows; try { rows = JSON.parse(fs.readFileSync(MONO_PATH, 'utf-8')); } catch (err) { process.stderr.write(\`[migrate-token-usage] failed to read \${MONO_PATH}: \${err.message}\\n\`); process.exit(1); }`. Assert `Array.isArray(rows)` — exit 1 on shape mismatch with a stderr log explaining the expected shape.

    (8) Bucket loop. Initialize `const buckets = new Map();` keyed by `${YYYY}-${MM}-${DD}_${windowStr}_${hash}` (PATTERNS.md Section 6 sketch). For each row:
        - `const localDate = utcToLocalTime(row.timestamp);` — IMPORTANT: pass the ISO timestamp string, NOT a `new Date(row.timestamp)` (PATTERNS.md Section 6 risk #3 explains the launchd-UTC-vs-project-TZ landmine).
        - Compute Y/M/D + windowStr exactly as scripts/timezone-utils.js:201-210 does (the LSL filename template — same convention).
        - Determine row's `user_hash`: if `typeof row.user_hash === 'string' && /^[a-z][a-z0-9]{5}$/.test(row.user_hash)`, use it; else use current `userHash` (PATTERNS.md Section 6 risk #1 — DON'T default to 'unknown'; we know the contributor on this machine).
        - Bucket key: `key = \`\${YYYY}-\${MM}-\${DD}_\${windowStr}_\${hash}\``.
        - Append row (with user_hash field set/preserved) to `buckets.get(key) || []`.

    (9) Log summary: `process.stderr.write(\`[migrate-token-usage] \${rows.length} rows → \${buckets.size} (date, window, user) buckets\\n\`);`

    (10) Write loop. For each `[key, bucketRows]` of `buckets`:
        - `fname = \`\${key}.json\`;`
        - `sub = dateSubdirFromFilename(fname);` — returns 'YYYY/MM' or null. If null (shouldn't happen with our key format, defensive): write to base.
        - `targetDir = path.join(EXPORT_DIR, sub);`
        - `target = path.join(targetDir, fname);`
        - DRY_RUN branch: `process.stderr.write(\`  [dry] \${target} ← \${bucketRows.length} rows\\n\`); continue;`
        - LIVE branch: `fs.mkdirSync(targetDir, { recursive: true });` then `fs.writeFileSync(target, JSON.stringify(bucketRows, null, 2) + '\\n', 'utf-8');`. Optional VERBOSE log of `[migrate-token-usage] wrote ${target} (${bucketRows.length} rows)`.

    (11) Defensive collision check: if `fs.existsSync(target)` BEFORE the write (i.e., the proxy already wrote a per-hour file for this same window — possible if the proxy was running BEFORE the migration was run), MERGE: parse existing, build `Set<string>` of `${user_hash}:${id}` for the existing rows, append only NEW bucketRows. Sort merged by id ASC. Same write. Log `[migrate-token-usage] merged with pre-existing target file (kept N existing, added M new)`. PATTERNS.md Section 6 doesn't call this out explicitly but the SUMMARY of Plan 36-03 will have produced exactly this situation if the proxy was kicked at any point between 36-03 landing and this migration.

    (12) Delete monolith (NOT in DRY_RUN): `fs.unlinkSync(MONO_PATH); process.stderr.write(\`[migrate-token-usage] deleted \${MONO_PATH}\\n\`);`

    (13) Exit 0.

    (14) chmod +x: `chmod +x scripts/migrate-token-usage-export.mjs` after writing the file. Verify by `[ -x scripts/migrate-token-usage-export.mjs ]`.

    Style: match scripts/backfill-raw-observations.mjs for spacing, comment density, error-handling verbosity. Avoid `require()` — pure ESM. Keep the file ≤ ~150 lines.

    Run sequence after writing:
      A) `node scripts/migrate-token-usage-export.mjs --dry-run` — verify the bucket summary looks sane (expect ~24-48 buckets for a single user × 1-2 days of data; verify the row count line says "1457 rows → N buckets" matching the legacy file's row count).
      B) `node scripts/migrate-token-usage-export.mjs` — live run.
      C) `ls .data/llm-proxy-export/2026/05/` — should now show per-hour files.
      D) `[ ! -f .data/llm-proxy-export/token-usage.json ]` — monolith deleted.
      E) `node scripts/migrate-token-usage-export.mjs` (second run) — should exit 0 with `monolith already removed` message.
      F) Restart proxy: `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`, then `sleep 5`, then `sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(*) FROM token_usage"` — should be UNCHANGED from pre-migration count (the hydrate step ingests the bucketed files via ON CONFLICT, no row count change). Then `sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(DISTINCT user_hash) FROM token_usage"` — should be 1 (just the current contributor). Then `sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(*) FROM token_usage WHERE user_hash='unknown'"` — should be 0 (all rows tagged with the real hash).

    The actual commit lands the new .mjs script + all the new YYYY/MM/*.json files + the deletion of the monolith in ONE commit (per CONTEXT.md L132 "Deletes the old monolithic file in the same commit"). The .gitignore commit from Task 1 is SEPARATE and lands FIRST.
  </action>
  <verify>
    <automated>
      [ -f scripts/migrate-token-usage-export.mjs ] \
        && [ -x scripts/migrate-token-usage-export.mjs ] \
        && node --check scripts/migrate-token-usage-export.mjs \
        && grep -q "DRY_RUN" scripts/migrate-token-usage-export.mjs \
        && grep -q "monolith already removed" scripts/migrate-token-usage-export.mjs \
        && grep -q "unlinkSync" scripts/migrate-token-usage-export.mjs \
        && grep -q "getTimeWindow" scripts/migrate-token-usage-export.mjs \
        && grep -q "utcToLocalTime" scripts/migrate-token-usage-export.mjs \
        && grep -q "user-hash-generator" scripts/migrate-token-usage-export.mjs \
        && ( [ -f .data/llm-proxy-export/token-usage.json ] && node scripts/migrate-token-usage-export.mjs --dry-run 2>&1 | grep -qE "[0-9]+ rows.* [0-9]+ \([a-z, ]+\) buckets" || true ) \
        && node scripts/migrate-token-usage-export.mjs 2>&1 | tail -5 \
        && [ ! -f .data/llm-proxy-export/token-usage.json ] \
        && [ "$(find .data/llm-proxy-export -name '*_*-*_*.json' | wc -l | tr -d ' ')" -ge 1 ] \
        && node scripts/migrate-token-usage-export.mjs 2>&1 | grep -q "monolith already removed" \
        && launchctl kickstart -k "gui/$(id -u)/com.coding.llm-cli-proxy" \
        && sleep 6 \
        && [ "$(sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(*) FROM token_usage WHERE user_hash='unknown'")" = "0" ] \
        && echo "OK migration complete"
    </automated>
  </verify>
  <acceptance_criteria>
    - `[ -f scripts/migrate-token-usage-export.mjs ]` — file exists.
    - `[ -x scripts/migrate-token-usage-export.mjs ]` — executable bit set.
    - `node --check scripts/migrate-token-usage-export.mjs` exits 0.
    - `--dry-run` mode prints `N rows → M (date, window, user) buckets` line and does NOT write or delete anything: `[ -f .data/llm-proxy-export/token-usage.json ]` still true AFTER dry run.
    - Live run: `[ ! -f .data/llm-proxy-export/token-usage.json ]` (monolith gone) AND `find .data/llm-proxy-export -name '*_*-*_*.json' | wc -l` returns ≥ 1.
    - Each per-hour file's name matches the regex `^[0-9]{4}-[0-9]{2}-[0-9]{2}_[0-9]{4}-[0-9]{4}_[a-z][a-z0-9]{5}\\.json$` — verify via `find .data/llm-proxy-export -name '*.json' | xargs -I{} basename {} | grep -vE '^[0-9]{4}-[0-9]{2}-[0-9]{2}_[0-9]{4}-[0-9]{4}_[a-z][a-z0-9]{5}\\.json$'` returning NOTHING (i.e., all filenames match the canonical HHMM-HHMM 4-digits-per-half form).
    - Idempotency: second run of the script exits 0 with stderr containing `monolith already removed`.
    - Post-migration + proxy kickstart: `sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(*) FROM token_usage WHERE user_hash='unknown'"` returns 0 (all rows tagged via hydrate ingesting the new files; OR the migration's bucket-tagging propagated via re-hydrate).
    - Sample file content valid: `head -3 $(find .data/llm-proxy-export -name '*.json' | head -1)` returns a `[` opening + valid JSON.
    - The commit for this task contains: scripts/migrate-token-usage-export.mjs (new) + many `.data/llm-proxy-export/YYYY/MM/*.json` (new) + .data/llm-proxy-export/token-usage.json (deleted). Verify with `git show --stat HEAD`.
  </acceptance_criteria>
  <done>
    A) Migration script created, executable, with --dry-run support.
    B) Live run successful: monolith deleted, per-hour files created.
    C) Idempotent: second run is a clean no-op.
    D) Proxy kickstart after migration: hydrate ingests the new files, DB user_hash column populated (zero 'unknown' rows).
    E) Single commit containing all three filesystem changes (new script + new files + deletion).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy JSON → migration script | Reads the trusted same-machine monolithic file; same trust domain as the proxy that wrote it |
| migration script → filesystem | Writes many new files; deletes one old file |
| user → script CLI args | --dry-run / --verbose flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-25 | Tampering | Malformed monolithic JSON corrupts migration | mitigate | JSON.parse inside try/catch; on shape mismatch (`!Array.isArray(rows)`), exit 1 with diagnostic stderr. Original file is preserved (not deleted on parse failure). |
| T-36-26 | Tampering | Filename collision with already-extant per-hour file (if proxy ran post-36-03 before this migration) | mitigate | Task 2 step (11) defensive merge: parse existing target, dedup by `(user_hash, id)`, append only new rows. |
| T-36-27 | DoS | 1457 rows × ~24 buckets × write+JSON.stringify = ~50ms total | accept | Negligible cost (PATTERNS.md Section 6 measurement). |
| T-36-28 | Repudiation | Operator forgets monolith deletion was in same commit | mitigate | Acceptance criterion requires `git show --stat HEAD` to display the deletion in the same commit as the new files (atomic w/r/t git). |
| T-36-29 | Information Disclosure | Migration script logs full prompt_preview to stderr in VERBOSE mode | mitigate | VERBOSE mode logs filenames + row counts only, NOT row contents. Document this in a comment + the --help output. |
| T-36-30 | Tampering | Re-running migration script after monolith deletion | mitigate | Idempotency guard at top of main(): `if (!fs.existsSync(MONO_PATH)) { exit 0; }`. PATTERNS.md Section 6 risk #5. |
| T-36-31 | Tampering | DST-boundary rows bucket into ambiguous windows twice/year | accept | PATTERNS.md Section 6 risk #4. ~60 rows/year worst case; documented as known limitation; not worth complexity to handle for v1. |
| T-36-32 | Tampering | scripts/user-hash-generator.js output not validated by migration script | mitigate | Validate via regex `^[a-z][a-z0-9]{5}$` after `UserHashGenerator.generateHash()`; on mismatch, exit 1 with diagnostic. Matches Plan 36-02's guard. |
| T-36-33 | Information Disclosure | .gitignore widen leaves an existing tracked .db-wal in history | mitigate | Pre-check: `git ls-files | grep '\\.db-' || echo "none tracked"` should return "none tracked" before Task 1 lands. If any are tracked, surface to user — not auto-removed by this plan. |
</threat_model>

<verification>
1. Task 1 isolation: `git log -1 --stat .gitignore` after the .gitignore commit shows ONLY .gitignore in the diff.
2. `git check-ignore -v .data/llm-proxy/token-usage.db-wal` — confirms ignore rule matches.
3. `git status .data/llm-proxy/` — clean.
4. `node scripts/migrate-token-usage-export.mjs --dry-run` — prints bucket plan; no FS changes.
5. `node scripts/migrate-token-usage-export.mjs` — live run; emits `deleted <MONO_PATH>` line.
6. `find .data/llm-proxy-export -name '*.json' -type f | head -5` — per-hour files visible.
7. `[ ! -f .data/llm-proxy-export/token-usage.json ]` — monolith gone.
8. `node scripts/migrate-token-usage-export.mjs` (re-run) — `monolith already removed` exit.
9. `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy` then `tail -20 ${LLM_PROXY_DATA_DIR}/llm-proxy/logs/stderr.log` — confirms hydrate log line shows N files (where N >= the bucket count from migration).
10. `sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(*) FROM token_usage WHERE user_hash='unknown'"` — 0.
11. `sqlite3 .data/llm-proxy/token-usage.db "SELECT COUNT(*) FROM token_usage"` — matches pre-migration count (no row gain or loss from the round-trip; new live LLM calls during the migration window may have added rows — note expected delta in SUMMARY).
12. Cross-user simulation as final smoke (optional): copy one per-hour file to a temp filename with a different `_zzzz99.json` suffix, kickstart, verify a peer row appears in DB (proves the cross-user merge path the whole phase exists to enable). Then delete the temp file.
</verification>

<success_criteria>
- `.gitignore` covers .db-wal / .db-shm / .db-journal, preserves `.data/llm-proxy-export/` allow-list, lands as own commit.
- `scripts/migrate-token-usage-export.mjs` exists, is executable, supports --dry-run, is idempotent.
- After live migration: monolithic `.data/llm-proxy-export/token-usage.json` is GONE; per-hour files under YYYY/MM/ have ALL legacy rows.
- After proxy kickstart: `SELECT COUNT(*) WHERE user_hash='unknown'` returns 0.
- Monolithic deletion AND new files are in the SAME git commit.
- Cross-user merge path verified end-to-end (manual smoke or fake-peer file).
- `git status` for `.data/llm-proxy/` is clean.
</success_criteria>

<output>
Create `.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-05-SUMMARY.md` when done.
</output>
