---
phase: 36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
  - 36-02
files_modified:
  - _work/rapid-llm-proxy/src/token-usage.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Token-usage rows are written to per-(window, user) JSON files at `<baseDir>/YYYY/MM/YYYY-MM-DD_HHMM-HHMM_<hash6>.json`, NOT to a single monolithic `<baseDir>/token-usage.json`."
    - "Each file contains ONLY rows whose `timestamp` falls in the right-exclusive window `[windowStart, windowEnd)` AND whose `user_hash` matches the file's hash suffix."
    - "Debounce is keyed per-window via a Map<windowKey, Timer>, NOT a single global timer — so an hour-boundary that produces rows for two windows in quick succession schedules TWO independent flushes (one per file)."
    - "currentWindow() reads from the coordinator at /health/state with a 30s cache + 1-2s timeout, falls back to a local recompute on coordinator-unreachable. The cache is warmed at module init (background) to keep the LLM hot path off the 2s execSync cost."
    - "The TZ source of truth for the proxy is the wrapper-exported env var `LSL_TIMEZONE` (Plan 36-02 wrapper edit), with `Europe/Berlin` as the fallback default. The proxy reads `process.env.LSL_TIMEZONE || 'Europe/Berlin'` ONCE at module init and passes it to every Intl.DateTimeFormat lookup."
    - "All new write paths (exportToHourFile, currentWindow, scheduleExport) wrap their work in try/catch with `process.stderr.write('[token-usage] X failed (non-fatal): ...')` — never throws into the LLM completion hot path."
    - "After this plan lands, `npm run build` regenerates `dist/token-usage.js` and `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy` makes the new code live."
  artifacts:
    - path: "_work/rapid-llm-proxy/src/token-usage.ts"
      provides: "resolveTokenExportDir, exportToHourFile, scheduleExport (per-window keyed), currentWindow"
      contains: "exportToHourFile"
    - path: "_work/rapid-llm-proxy/dist/token-usage.js"
      provides: "compiled output consumed by proxy-bridge/server.mjs"
      contains: "exportToHourFile"
  key_links:
    - from: "logCall"
      to: "scheduleExport"
      via: "per-window debounce timer Map"
      pattern: "scheduleExport"
    - from: "exportToHourFile"
      to: "<baseDir>/YYYY/MM/YYYY-MM-DD_HHMM-HHMM_<hash6>.json"
      via: "fs.writeFileSync with mkdirSync recursive"
      pattern: "writeFileSync"
    - from: "currentWindow"
      to: "http://127.0.0.1:3034/health/state"
      via: "execSync curl with 1-2s timeout + 30s in-memory cache"
      pattern: "COORDINATOR_URL"
---

<objective>
Rewrite the JSON export side of `_work/rapid-llm-proxy/src/token-usage.ts` to emit one file per `(time-window, user_hash)` pair under `<baseDir>/YYYY/MM/`, mirroring the LSL filesystem convention. Source the time-window from the coordinator (Plan 36-01) with local fallback. Source the user hash from `process.env.LLM_PROXY_USER_HASH` (Plan 36-02) with fallback to `'unknown'`. This is the writer half of the multi-user, git-trackable layout.

Purpose: Eliminate the merge-conflict surface of the monolithic `token-usage.json`. Each user/window combo writes to its own file → independent push/pull paths → no conflicts.

Output: New writer functions in `token-usage.ts`; legacy `exportToJson` + `resolveTokenExportPath` deprecated but retained for backward-compat callers (none expected, but cheap insurance). Schema migration + hydration is the next plan (36-04); this plan leaves `restoreFromJsonIfEmpty` UNCHANGED — Plan 36-04 replaces it with `hydrateFromExports`.

**Scope note (single-task plan)**: This plan ships as a single 7-step task because the steps are tightly coupled (constants → helpers → exporter → debounce rewrite all touch the same file and share module-level state). Splitting into 36-03a/03b would force renumbering 36-04/05 and ROADMAP for no actual encapsulation gain. The acceptance criteria are granular enough to verify each step independently; if a sub-step fails, fix-forward within this plan rather than carving up the work post-hoc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md
@.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-01-SUMMARY.md

<interfaces>
<!-- Existing exports from _work/rapid-llm-proxy/src/token-usage.ts -->
<!-- (Pre-this-plan state; verify by reading the file before editing) -->

- `resolveTokenExportPath(overrideDataDir?: string): string` — returns single-file path. KEEP (deprecate via JSDoc, do not delete — callers in dist/proxy-bridge may pull it in).
- `function scheduleExport(handle: TokenUsageDb): void` — single-timer debounce. REPLACE the body to be per-window keyed.
- `function exportToJson(handle: TokenUsageDb): void` — full-table snapshot write. KEEP for any legacy path; new code uses exportToHourFile.
- `function restoreFromJsonIfEmpty(handle, overrideDataDir?): void` — DO NOT TOUCH in this plan; Plan 36-04 replaces it.
- `function logCall(handle: TokenUsageDb, row: TokenUsageRow): void` (currently calls scheduleExport at the end) — this plan changes only what scheduleExport does internally; logCall signature unchanged.

<!-- Patterns the new code mirrors (do NOT cross-import from coding repo; rapid-llm-proxy stays standalone per CONTEXT.md L65) -->

From scripts/lsl-paths.js:17-49 — port these to inline TS:
- `dateSubdirFromFilename(filename)` — extracts `YYYY/MM` from a filename's `YYYY-MM-DD_*` prefix.
- `lslWritePath(baseDir, filename)` — `baseDir/YYYY/MM/filename` + recursive mkdir.

From scripts/timezone-utils.js:79-103 — port `getTimeWindow` formula inline (NOT import — submodule boundary):
- Read `live_logging.session_duration` from `<CODING_REPO>/config/live-logging-config.json` ONCE at module init, cache as `_sessionDurationMs` (default 3600000).
- Window = `[localHour, localHour + sessionDurationHours)` formatted `HHMM-HHMM`.

From _work/rapid-llm-proxy/proxy-bridge/server.mjs:196-231 — coordinator-curl-with-local-fallback pattern. Mirror exactly into the new `currentWindow(now: Date)` function in token-usage.ts.

`COORDINATOR_URL = process.env.HEALTH_COORDINATOR_URL || 'http://127.0.0.1:3034'`
`CACHE_TTL_MS = 30_000`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resolveTokenExportDir, currentWindow, lslWritePath port, and exportToHourFile</name>
  <files>_work/rapid-llm-proxy/src/token-usage.ts</files>
  <read_first>
    - _work/rapid-llm-proxy/src/token-usage.ts (full file — lines 76-83 resolveTokenExportPath, 134-178 restoreFromJsonIfEmpty (untouched), 190-224 exportToJson, 232-242 scheduleExport, 244+ logCall)
    - _work/rapid-llm-proxy/proxy-bridge/server.mjs (lines 196-231 detectNetworkMode — EXACT pattern to mirror for currentWindow's coordinator-curl)
    - scripts/lsl-paths.js (full file — lines 17-49 dateSubdirFromFilename + lslWritePath; this is what you port to inline TS)
    - scripts/timezone-utils.js (lines 79-103 getTimeWindow; you port the FORMULA, not import — submodule isolation)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-PATTERNS.md (Section 2 in full — has all the excerpt + difference-to-apply + landmines for this exact file)
    - .planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/CONTEXT.md (Wave 2 + Wave 4 + "Hash propagation" decision)
  </read_first>
  <action>
    Add three new exports + one new internal helper to `_work/rapid-llm-proxy/src/token-usage.ts`. Place them adjacent to the existing single-file analogs (do NOT delete those; keep file shape easy to diff). All new code goes ABOVE the existing `restoreFromJsonIfEmpty` block (lines 134-178), or wherever fits the file's existing import/constants/functions ordering. Use the existing `path` and `fs` imports already at top of file.

    (1) Module-level constants block (near existing constants — there is already an `EXPORT_DEBOUNCE_MS = 2000` at line 232; group new ones near top):
        - `const COORDINATOR_URL = process.env.HEALTH_COORDINATOR_URL || 'http://127.0.0.1:3034';`
        - `const WINDOW_CACHE_TTL_MS = 30_000;`
        - `let _cachedWindow: string | null = null;`
        - `let _cachedWindowAt = 0;`
        - `const USER_HASH = (process.env.LLM_PROXY_USER_HASH || 'unknown').trim() || 'unknown';` — read ONCE at module init per PATTERNS.md Section 3 hot-path landmine.
        - `const LSL_TIMEZONE = process.env.LSL_TIMEZONE || 'Europe/Berlin';` — read ONCE at module init; the wrapper (Plan 36-02) is the canonical export point for this var.
        - `let _sessionDurationMs = 3600000;` (default 60 min; populated by module-init try/catch reading `<repoRoot>/config/live-logging-config.json`). Resolve `repoRoot` via `process.env.CODING_REPO || '/Users/Q284340/Agentic/coding'`.

    (2) New exported `resolveTokenExportDir(overrideDataDir?: string): string` (PATTERNS.md Section 2 has the exact body). Honors `LLM_PROXY_TOKEN_EXPORT_DIR`, then `LLM_PROXY_DATA_DIR`, then cwd + '.data'. Returns `<dataDir>/llm-proxy-export`. Add JSDoc.

    (3) New non-exported `computeWindowLocally(now: Date): string` — port of `getTimeWindow(utcToLocalTime(now))`. Format: `HHMM-HHMM` using full-hour marks (PATTERNS.md Section 2 + timezone-utils.js:79-103). Use the cached `_sessionDurationMs`. Since this runs in the proxy (no Intl import needed if you can read getHours directly — but BE AWARE: on launchd-spawned processes the JS engine's TZ defaults to UTC. PATTERNS.md Section 1 landmine #2 calls this out for the COORDINATOR; for the proxy, do the same — convert via Intl.DateTimeFormat with timeZone `LSL_TIMEZONE` (module constant from step 1) to extract hour). Pseudocode (NO fenced block in <action>): use `new Intl.DateTimeFormat('en-US', { timeZone: LSL_TIMEZONE, hour: 'numeric', hour12: false }).formatToParts(now)` to get the local hour as integer, then format the window string. Tested constant: at UTC 08:30 with Europe/Berlin TZ, hour=10 → window '1000-1100'.

    (4) New exported `currentWindow(now: Date): string` — mirror PATTERNS.md "Coordinator-curl with local fallback" shared pattern. Uses `execSync` with `--max-time 1` + `timeout: 2000`, parses JSON, reads `state?.lsl_meta?.current_window || state?.lsl?.current_window` (the second is a hedge in case Plan 36-01 lands with a different field name — though it should not — keeps this code resilient to nesting choices). On any error OR empty value, fall back to `computeWindowLocally(now)`. Cache for `WINDOW_CACHE_TTL_MS`. Log NOTHING on cache hit (hot path); log via `process.stderr.write('[token-usage] currentWindow fallback ...')` on coordinator-unreachable to aid debugging.

    Hot-path warm: at module init, fire `setImmediate(() => { try { currentWindow(new Date()); } catch {} });` to seed the cache BEFORE the first `logCall` arrives. This dodges the 2s execSync tax on the very first LLM completion (PATTERNS.md Section 3 landmine).

    (5) Port `lslWritePath` inline as a non-exported helper `function tokenExportWritePath(baseDir: string, filename: string): string`:
        - Regex `/^(\d{4})-(\d{2})-\d{2}_/` against filename → extract year, month.
        - `targetDir = path.join(baseDir, year, month)`.
        - `fs.mkdirSync(targetDir, { recursive: true });`
        - return `path.join(targetDir, filename);`
        - On regex non-match, return `path.join(baseDir, filename)` (defensive — should never trigger for filenames produced by this module).

    (6) New exported `exportToHourFile(handle: TokenUsageDb, now: Date): void` — the core writer. Body (no fenced code in <action>; structural description):
        - Wrap everything in try/catch with `process.stderr.write('[token-usage] exportToHourFile failed (non-fatal): ' + (err as Error).message + '\\n');` per "Best-effort error handling" shared pattern.
        - Compute `windowStr = currentWindow(now)` (e.g. '0900-1000').
        - Compute `windowStartHour = parseInt(windowStr.slice(0,2), 10);` and parse the trailing hour; derive `windowStart` and `windowEnd` as ISO-8601 UTC strings representing the LOCAL hour boundary converted back to UTC (use the same Intl-based approach as computeWindowLocally to find today's local-midnight UTC offset, then add hours). PATTERNS.md Section 2 specifies right-exclusive: `timestamp >= start AND timestamp < end`. Note: `row.timestamp` is stored as ISO-8601 UTC strings per existing schema (line 13).
        - Derive filename: `${YYYY}-${MM}-${DD}_${windowStr}_${USER_HASH}.json` using the LOCAL date components of `now` (via the same Intl approach — do NOT use `now.toISOString().slice(0,10)` which is UTC).
        - Compute path via `tokenExportWritePath(resolveTokenExportDir(), filename)`.
        - SELECT rows: `SELECT id, timestamp, provider, model, process, subscription, input_tokens, output_tokens, total_tokens, latency_ms, prompt_preview, tokens_estimated, user_hash FROM token_usage WHERE timestamp >= ? AND timestamp < ? AND user_hash = ? ORDER BY id ASC`. NOTE: `user_hash` column does NOT exist until Plan 36-04 lands. Until then, this SELECT will throw `SQLITE_ERROR: no such column: user_hash`. ACCEPTABLE per dependency chain: Plan 36-03 ships first (this code is wired), Plan 36-04 follows immediately to add the column. As a defensive measure, wrap the SELECT in a try/catch that catches the `no such column` SqliteError and falls back to a `SELECT ...` without the `user_hash` filter / column, mapping `user_hash` to the in-memory `USER_HASH` constant for each row. Document this defensive fallback as "removable after 36-04 lands" in a `// TODO(36-04)` comment.
        - Safety merge by `(user_hash, id)`: if the target file already exists, parse it, build `Set<string>` of `"${row.user_hash}:${row.id}"` for DB rows, keep file rows not in the set (preserves a teammate's previously-pushed file rows on cross-user pull → push flow). Merge: `[...preserved, ...dbRows]`. PATTERNS.md Section 2 has the existing single-file merge analog at lines 207-216 — adapt for per-file scope.
        - No-op dedup: if `fs.readFileSync(target, 'utf-8') === newContent`, return without writing. Reduces churn on the 2s debounce burst.
        - Write: `fs.writeFileSync(target, content, 'utf-8');`. Content = `JSON.stringify(merged, null, 2) + '\\n'` (matches existing exportToJson style at line 218).

    (7) Replace the body of `scheduleExport(handle: TokenUsageDb)` to be per-window keyed (PATTERNS.md Section 2 "Difference to apply (Wave 2)" excerpt). Change the signature to `scheduleExport(handle: TokenUsageDb, now: Date = new Date()): void` (default arg preserves existing callers that pass only handle). Implementation:
        - Replace the module-level `let _exportTimer = null;` with `const _exportTimers = new Map<string, NodeJS.Timeout>();`.
        - Compute `windowKey = currentWindow(now);`.
        - If `_exportTimers.has(windowKey)`, return (debounce hit).
        - Set the timer: `setTimeout(() => { _exportTimers.delete(windowKey); exportToHourFile(handle, now); }, EXPORT_DEBOUNCE_MS);` then `t.unref();` then `_exportTimers.set(windowKey, t);`.
        - Hour-boundary correctness: at XX:59:58, a row arrives → scheduleExport keys 'XX00-(XX+1)00'. At XX:00:01, a row arrives → scheduleExport keys '(XX+1)00-(XX+2)00'. Both timers coexist in the Map; both fire ~2s later; each writes to its own file. The SELECT inside exportToHourFile filters by timestamp window (right-exclusive), so the 09:59:59 row goes into `0900-1000.json` even though its flush actually happens at 10:00:00.something. PATTERNS.md Section 2 explicitly documents this as the critical landmine — the SELECT timestamp range is the safeguard.

    (8) Keep `restoreFromJsonIfEmpty` UNTOUCHED in this plan. Plan 36-04 replaces it with `hydrateFromExports`. Add a `// TODO(36-04): replace with hydrateFromExports — see plan 36-04` comment above its declaration to signal next step.

    (9) Keep `exportToJson` and `resolveTokenExportPath` UNTOUCHED. Add `@deprecated` JSDoc to both noting they are superseded by exportToHourFile + resolveTokenExportDir, but kept for any external caller. The proxy-bridge/server.mjs currently calls `restoreFromJsonIfEmpty(handle)` on init (line ~36 — verify) and `scheduleExport(handle)` per logCall. After this plan, scheduleExport's body changes but the call site does not, so no edit to server.mjs is required.

    After code changes, build: `cd _work/rapid-llm-proxy && npm run build` to regenerate dist/. Then `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy` to load.

    CRITICAL TZ note for the proxy: launchd's child env has no `TZ` by default. PATTERNS.md Section 1 landmine #2 documents this as a coordinator concern, but it also bites the proxy. Use `Intl.DateTimeFormat` with the module-init `LSL_TIMEZONE` constant for hour/date extraction — do NOT use `localDate.getHours()` on a bare `new Date()` in the proxy. Plan 36-02's wrapper edit is the canonical export point for `LSL_TIMEZONE` (with `Europe/Berlin` as the fallback default).
  </action>
  <verify>
    <automated>
      cd _work/rapid-llm-proxy && npm run build 2>&1 | tail -20 \
        && grep -q "exportToHourFile" _work/rapid-llm-proxy/src/token-usage.ts \
        && grep -q "exportToHourFile" _work/rapid-llm-proxy/dist/token-usage.js \
        && grep -q "_exportTimers" _work/rapid-llm-proxy/src/token-usage.ts \
        && grep -q "currentWindow" _work/rapid-llm-proxy/src/token-usage.ts \
        && grep -q "USER_HASH" _work/rapid-llm-proxy/src/token-usage.ts \
        && grep -q "LSL_TIMEZONE" _work/rapid-llm-proxy/src/token-usage.ts \
        && grep -q "resolveTokenExportDir" _work/rapid-llm-proxy/src/token-usage.ts \
        && cd /Users/Q284340/Agentic/coding \
        && launchctl kickstart -k "gui/$(id -u)/com.coding.llm-cli-proxy" \
        && sleep 3 \
        && curl -sf --max-time 5 -X POST http://127.0.0.1:8089/v1/chat/completions \
             -H 'Content-Type: application/json' \
             -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"ping"}],"max_tokens":1}' \
             > /dev/null 2>&1 || true \
        && sleep 3 \
        && file_count=$(ls /Users/Q284340/Agentic/coding/.data/llm-proxy-export/2026/05/2026-05-*_*-*_*.json 2>/dev/null | wc -l | tr -d ' ') \
        && test "$file_count" -ge 1 \
        && echo "OK build + at least one per-hour file written (count=$file_count)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function exportToHourFile" _work/rapid-llm-proxy/src/token-usage.ts` returns 0.
    - `grep -q "export function resolveTokenExportDir" _work/rapid-llm-proxy/src/token-usage.ts` returns 0.
    - `grep -q "export function currentWindow" _work/rapid-llm-proxy/src/token-usage.ts` returns 0.
    - `grep -q "const _exportTimers = new Map" _work/rapid-llm-proxy/src/token-usage.ts` returns 0 (per-window keyed Map present).
    - `grep -q "WHERE timestamp >= ? AND timestamp < ?" _work/rapid-llm-proxy/src/token-usage.ts` returns 0 (right-exclusive SELECT).
    - `grep -q "process.env.LLM_PROXY_USER_HASH" _work/rapid-llm-proxy/src/token-usage.ts` returns 0 (env-read present, read at module init).
    - `grep -q "process.env.LSL_TIMEZONE" _work/rapid-llm-proxy/src/token-usage.ts` returns 0 (TZ env-read present at module init).
    - `npm run build` exits 0 from inside `_work/rapid-llm-proxy/`.
    - After `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy`, the proxy boots without errors visible in `${LLM_PROXY_DATA_DIR}/llm-proxy/logs/stderr.log` for at least 30s of uptime.
    - After ANY LLM completion routes through the proxy (the verify block triggers a minimal payload via `curl -X POST .../v1/chat/completions`; if the endpoint requires auth or a different shape on this deployment, replace with any `claude-mcp` interaction that exercises the proxy — the goal is to produce one logCall row), within 2-3 seconds a NEW file appears under `.data/llm-proxy-export/YYYY/MM/` matching the pattern `YYYY-MM-DD_HHMM-HHMM_<hash6>.json`. The automated verify asserts `file_count >= 1`. Manual cross-check: `find .data/llm-proxy-export -name '*.json' -newer .data/llm-proxy/token-usage.db | head -5`.
    - The file content is valid JSON, array of objects, each row has `timestamp`, `id`, and (post-36-04) `user_hash` matching the filename suffix. Pre-36-04: rows lack `user_hash` field (defensive fallback path active).
    - `grep -q "// TODO(36-04)" _work/rapid-llm-proxy/src/token-usage.ts` returns 0 (defensive fallback documented).
    - The legacy `.data/llm-proxy-export/token-usage.json` is NOT modified by this plan (it stays as-is; Plan 36-05 deletes it).
  </acceptance_criteria>
  <done>
    A) New writer functions present in src; dist rebuilt; launchd reloaded.
    B) New JSON files appear under YYYY/MM/ with the correct filename pattern AFTER an LLM completion is routed.
    C) Per-window debounce works — no merged-window contamination (verifiable by hand-checking that rows in `*_0900-1000_*.json` all have `timestamp` between 09:00 and 10:00 local).
    D) Coordinator unreachable does NOT block the proxy (test: temporarily stop coordinator; trigger an LLM call; proxy still writes — file is written using locally-computed window).
    E) Defensive `// TODO(36-04)` fallback compiles and runs; Plan 36-04 will remove the fallback.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → proxy | execSync curl of /health/state; coordinator's JSON shape trusted (same machine, same $USER) |
| env → proxy | LLM_PROXY_USER_HASH + LSL_TIMEZONE read once at module init |
| filesystem → proxy | Reads <baseDir>/YYYY/MM/*.json on safety-merge; writes to same |
| LLM completion → token-usage | Row data carries provider/model/prompt_preview from upstream LLM responses (untrusted in principle, but written as data not code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11 | Tampering | JSON injection from peer-pushed file during safety-merge | mitigate | Parse with JSON.parse inside try/catch; if parse fails, treat existing file as empty and overwrite with current DB rows (data loss bounded to the corrupted file's rows, recoverable from git history). Never `eval` or template-inject the parsed values. |
| T-36-12 | Information Disclosure | `prompt_preview` field leaks user prompts to git-tracked JSON | accept | Existing behavior (the legacy monolithic file already exports this column). Phase 36 is shape-change only, not retention/policy change. User accepted via CONTEXT.md "Out of scope" L154 (retention/rotation deferred). |
| T-36-13 | Denial of Service | execSync curl blocks LLM hot path on coordinator-unreachable cold boot | mitigate | 1-2s timeout + 30s in-memory cache + module-init warm via setImmediate. First LLM call after a long coordinator outage takes ≤2s extra; subsequent calls hit cache (PATTERNS.md Section 3 measurement). |
| T-36-14 | Tampering | Debounce-key race at hour boundary writes XX:59 row into wrong file | mitigate | SELECT timestamp window uses right-exclusive `>= start AND < end`, NOT "all rows since last export". Per-window Map allows two timers to coexist across the boundary. PATTERNS.md Section 2 landmine #5 documents the exact failure mode this mitigates. |
| T-36-15 | Tampering / Filename traversal | USER_HASH used in filename construction | mitigate | Plan 36-02 validates hash with regex `^[a-z][a-z0-9]{5}$` before exporting; this plan additionally guards by re-checking `USER_HASH` against the same regex at module init and falling back to 'unknown' if malformed. No path-traversal characters possible. |
| T-36-16 | DoS | hot-path TZ extraction via Intl.DateTimeFormat per call | mitigate | Cache an `Intl.DateTimeFormat` instance at module init (constructor is the slow part; formatToParts is fast). Reuse for every call. |
| T-36-17 | Repudiation | Coordinator's window mid-tick lags proxy's perception of "now" | accept | 30s cache + coordinator polls at 5s → max drift 30s. Hour-boundary rows might land in the previous window's file when coordinator reports stale window; the SELECT's right-exclusive timestamp filter is the canonical truth — the FILENAME might say `0900-1000` while the rows are `[09:00, 10:00)` which IS what we want. No correctness violation. |
</threat_model>

<verification>
1. `cd _work/rapid-llm-proxy && npm run build` — must exit 0; emits compiled `dist/token-usage.js`.
2. `launchctl kickstart -k gui/$(id -u)/com.coding.llm-cli-proxy` — picks up new dist.
3. `tail -f ${LLM_PROXY_DATA_DIR}/llm-proxy/logs/stderr.log` — confirm `[token-usage]` lines on startup, no thrown errors.
4. Trigger an LLM completion (curl POST in the automated verify, OR any `claude-mcp` interaction). Within 5s, observe new file under `.data/llm-proxy-export/YYYY/MM/` matching `YYYY-MM-DD_HHMM-HHMM_<hash6>.json`.
5. Inspect the new file's contents (`jq .` over it): array of objects, each with `id` (int) and `timestamp` ISO-8601 in the right window.
6. Coordinator-unreachable smoke (optional): `launchctl bootout gui/$(id -u)/com.coding.health-coordinator`, trigger a completion, verify file STILL written (fallback path active); `launchctl bootstrap` it back.
</verification>

<success_criteria>
- New per-hour JSON files appear under `.data/llm-proxy-export/YYYY/MM/` after LLM completions.
- Filename matches `YYYY-MM-DD_HHMM-HHMM_<hash6>.json`.
- Per-window debounce: two pending timers can coexist across XX:00 boundary; each flushes to its own file.
- Coordinator-unreachable falls back to local window compute within 2s.
- Proxy boots cleanly after kickstart; no errors in stderr.log.
- The legacy monolithic `.data/llm-proxy-export/token-usage.json` is NOT modified (Plan 36-05 deletes it).
</success_criteria>

<output>
Create `.planning/phases/36-token-usage-per-user-hourly-exports-mirror-lsl-conventions-f/36-03-SUMMARY.md` when done.
</output>
