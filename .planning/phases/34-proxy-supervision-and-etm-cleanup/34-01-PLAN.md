---
phase: 34-proxy-supervision-and-etm-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/health-verification-rules.json
autonomous: true
requirements:
  - "R4-partial: flip auto_heal=true on llm_cli_proxy + add cooldown.{max_kickstarts:3, window_seconds:300} so the FSM in Plan 34-03 has a kill-switch and a cooldown spec to read"

must_haves:
  truths:
    - "config/health-verification-rules.json has services.llm_cli_proxy.auto_heal == true"
    - "config/health-verification-rules.json has services.llm_cli_proxy.cooldown.max_kickstarts == 3"
    - "config/health-verification-rules.json has services.llm_cli_proxy.cooldown.window_seconds == 300"
    - "Operator can toggle auto_heal=false then call POST http://localhost:3034/health/refresh and the rule reload picks up the change without a coordinator restart"
  artifacts:
    - path: "config/health-verification-rules.json"
      provides: "Updated llm_cli_proxy rule with auto_heal + cooldown"
      contains: '"auto_heal": true'
  key_links:
    - from: "config/health-verification-rules.json"
      to: "scripts/health-coordinator.js loadRules() at /health/refresh"
      via: "operator-driven reload (D-07 kill-switch)"
      pattern: "loadRules\\(\\)"
---

<objective>
Update the llm_cli_proxy rule in `config/health-verification-rules.json` so the Plan 34-03 auto-heal FSM has the runtime kill-switch (auto_heal toggle) and the cooldown spec (3 kickstarts / 5 min sliding window) it needs to read.

Purpose: implements D-07 (config-driven kill-switch via existing POST /health/refresh) and seeds the cooldown numbers (D-06).

Output: one-file commit; rule schema unchanged (Phase 33 AC #10 stays passing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-SPEC.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-CONTEXT.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-PATTERNS.md
@config/health-verification-rules.json

<interfaces>
<!-- Existing rule shape this plan edits. Source: config/health-verification-rules.json:115-126 -->

```json
"llm_cli_proxy": {
  "enabled": true,
  "severity": "warning",
  "check_type": "http_health",
  "endpoint": "http://localhost:12435/health",
  "timeout_ms": 3000,
  "description": "...",
  "auto_heal": false,
  "auto_heal_action": "restart_llm_cli_proxy",
  "auto_heal_note": "...",
  "expected_status": "required"
}
```

Sibling rule with cooldown shape (analog — `leveldb_lock_check` at lines 11-19 has `auto_heal:true`, `auto_heal_action`, `max_heal_attempts`, `heal_backoff_seconds`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip auto_heal to true and add cooldown block under services.llm_cli_proxy</name>
  <files>config/health-verification-rules.json</files>
  <read_first>
    - config/health-verification-rules.json (read the whole file — it is small; locate the existing services.llm_cli_proxy block at lines ~115-126 AND any sibling rule that already uses auto_heal:true such as leveldb_lock_check at lines 11-19)
    - .planning/phases/34-proxy-supervision-and-etm-cleanup/34-PATTERNS.md (section "2. config/health-verification-rules.json" — has the exact target JSON)
    - .planning/phases/34-proxy-supervision-and-etm-cleanup/34-SPEC.md (Requirement 4 — current vs target)
  </read_first>
  <action>
Edit `config/health-verification-rules.json`. Locate the `services.llm_cli_proxy` rule (currently around lines 115-126). Replace its body with EXACTLY this content (preserving JSON indentation and trailing comma rules of the surrounding file):

```json
"llm_cli_proxy": {
  "enabled": true,
  "severity": "warning",
  "check_type": "http_health",
  "endpoint": "http://localhost:12435/health",
  "timeout_ms": 3000,
  "description": "LLM CLI Proxy - runs on host as launchd-managed daemon (com.coding.llm-cli-proxy). Coordinator probes /health every tick AND issues a tier-pinned semantic probe (POST /api/complete with provider:'copilot', tier:'haiku', single-token 'OK' prompt) every 60s. Required for observation summarization.",
  "auto_heal": true,
  "auto_heal_action": "restart_llm_cli_proxy",
  "auto_heal_note": "Phase 34: coordinator dispatches restart on sustained semantic_ok=false (>=60s) via launchctl kickstart -k gui/$UID/com.coding.llm-cli-proxy. Cooldown 3/5min then alert-only. Kill-switch: set auto_heal=false here + POST http://localhost:3034/health/refresh.",
  "cooldown": {
    "max_kickstarts": 3,
    "window_seconds": 300
  },
  "expected_status": "required"
}
```

Notes for the executor (per D-06, D-07, D-05):
- `auto_heal` flips `false` -> `true` (per D-07 this is the runtime kill-switch).
- `cooldown.max_kickstarts: 3` and `cooldown.window_seconds: 300` are the D-06 numbers — these are READ by `evaluateAutoHealFSM()` in Plan 34-03; do NOT use any other shape.
- DO NOT touch any other rule. DO NOT add a new top-level key. DO NOT modify the rule schema (`auto_healing.cooldown_config` global block lives elsewhere — leave it alone).
- The `auto_heal_action: "restart_llm_cli_proxy"` value already exists; keep it. Plan 34-03 rewrites the body of that action in `scripts/health-remediation-actions.js`.
- Run `node -e "JSON.parse(require('fs').readFileSync('config/health-verification-rules.json','utf8'))"` after the edit to confirm valid JSON.
- Run `node --test scripts/__tests__/health-coordinator/rules-schema.test.js` (Phase 33 AC #10) to confirm the rules schema still validates.
  </action>
  <verify>
    <automated>node -e "const r=JSON.parse(require('fs').readFileSync('config/health-verification-rules.json','utf8')); const p=r.rules.services.llm_cli_proxy; if(p.auto_heal!==true) {console.error('FAIL auto_heal'); process.exit(1)}; if(p.cooldown.max_kickstarts!==3) {console.error('FAIL max_kickstarts'); process.exit(1)}; if(p.cooldown.window_seconds!==300) {console.error('FAIL window_seconds'); process.exit(1)}; if(p.auto_heal_action!=='restart_llm_cli_proxy') {console.error('FAIL action'); process.exit(1)}; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "...JSON.parse..."` (above) prints `OK` and exits 0.
    - `grep -c '"auto_heal": true' config/health-verification-rules.json` is at least 1 higher than before the edit (confirms flip).
    - `grep -A2 '"llm_cli_proxy":' config/health-verification-rules.json | grep -c '"auto_heal": true'` returns 1 (precision: only the llm_cli_proxy rule flipped).
    - `node --test scripts/__tests__/health-coordinator/rules-schema.test.js` exits 0 (Phase 33 AC #10 stays green).
    - `node -e "const r=JSON.parse(require('fs').readFileSync('config/health-verification-rules.json','utf8')); console.log(JSON.stringify(r.rules.services.llm_cli_proxy.cooldown))"` prints `{"max_kickstarts":3,"window_seconds":300}`.
    - `curl -fs -X POST http://localhost:3034/health/refresh` returns 200 if the coordinator is running (NOT a hard requirement at execute time — coordinator may be off; this confirms the kill-switch path on demand).
  </acceptance_criteria>
  <done>
    The llm_cli_proxy rule flips to auto_heal=true with the cooldown numbers Plan 34-03 expects, and the rules schema test still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> config file | A developer with repo write access can flip auto_heal back to false; this is the intended kill-switch (D-07), auditable via git log. |
| coordinator -> config file | Coordinator reads the rule via loadRules() on /health/refresh; trusts the parsed JSON — but a malformed file makes loadRules throw and the FSM stays disabled (fail-closed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | config/health-verification-rules.json | accept | The kill-switch IS the deliberate config-edit path (D-07). Auditable via git log. Not a vulnerability. |
| T-34-01-02 | Denial-of-service | rules loader | mitigate | Existing loadRules() in health-coordinator.js wraps JSON.parse in try/catch; bad config -> rule registry stays at last-good state. Verification: `node --test scripts/__tests__/health-coordinator/rules-schema.test.js` exits 0 (existing test covers this).
| T-34-01-03 | Elevation of Privilege | auto_heal flip | mitigate | The action this enables (`launchctl kickstart -k gui/$UID/com.coding.llm-cli-proxy`) runs as the same UID as the coordinator (NOT root). Plan 34-03 enforces `process.getuid()` use, not hard-coded UID 0. |
</threat_model>

<verification>
- `node -e ...` JSON parse + assertions (above) exits 0
- Phase 33 AC #10 rules-schema.test.js exits 0
- git diff shows ONLY config/health-verification-rules.json changed; only the llm_cli_proxy rule region edited
</verification>

<success_criteria>
- llm_cli_proxy rule has auto_heal=true, cooldown.max_kickstarts=3, cooldown.window_seconds=300
- Phase 33 AC #10 rules-schema.test.js still passes
- Single-file commit; clean git diff
</success_criteria>

<output>
After completion, create `.planning/phases/34-proxy-supervision-and-etm-cleanup/34-01-SUMMARY.md`
</output>
