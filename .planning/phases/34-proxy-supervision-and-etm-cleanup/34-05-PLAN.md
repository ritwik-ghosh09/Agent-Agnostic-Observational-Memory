---
phase: 34-proxy-supervision-and-etm-cleanup
plan: 05
type: execute
wave: 3
depends_on:
  - 34-02
  - 34-03
  - 34-04
files_modified:
  - src/live-logging/RealTimeTrajectoryAnalyzer.js
  - src/knowledge-management/StreamingKnowledgeExtractor.js
  - src/knowledge-management/KnowledgeExtractor.js
  - src/knowledge-management/KnowledgeDecayTracker.js
  - src/knowledge-management/ConceptAbstractionAgent.js
  - src/knowledge-management/TrajectoryAnalyzer.js
  - tests/unit/knowledge-management/KnowledgeExtraction.test.ts
  - scripts/test-knowledge-extraction.js
  - tests/unit/ontology/integration.test.js
  - tests/acceptance/knowledge-system-acceptance.test.js
  - scripts/combined-status-line.js
  - integrations/system-health-dashboard/src/components/system-health-dashboard.tsx
autonomous: false
requirements:
  - "R5: ETM legacy code removal — Plan B (D-08) — delete 6 source files + dead-reader cleanup in scripts/combined-status-line.js + test-file cleanup; SPEC AC #8 grep-clean MUST pass"
  - "R6: ETM behavior preserved — both ETMs must continue posting heartbeats and observations after this plan ships (Plan 34-04 already verified the strip; this plan removes the now-dangling source files)"
  - "R2-surface: dashboard proxy-health card surfaces state.proxy.{semantic_ok, networkMode, auto_heal_status} per D-11 (depends on Plan 34-02 having added the slice + Plan 34-03 having wired the FSM)"

# WAVE/DEPENDENCY NOTE (W-3 revision, Option A):
# 34-05 now depends on 34-03 in addition to 34-02 and 34-04. Rationale: the
# [🧠] cooling-state truths and the dashboard auto_heal_status card item are
# observable only AFTER 34-03 ships the FSM (auto_heal_status='cooldown'
# transitions). Per assign_waves algorithm (planner spec): wave =
# max(waves[dep])+1; with 34-03 at wave 2, 34-05 moves from wave 2 to wave 3.
# Wave-graph delta:
#   - Wave 1: 34-01, 34-02, 34-04, 34-06 (unchanged)
#   - Wave 2: 34-03 (unchanged) — was previously {34-03, 34-05}
#   - Wave 3 (NEW): 34-05 (was wave 2)
# Tradeoff: lost nominal parallelism between 34-03 and 34-05 (they touch
# overlapping observable surface anyway — semantic_ok / auto_heal_status
# is consumed by 34-05's [🧠] badge and dashboard card, both written by
# 34-03 — so parallelism would have been a verification race). 34-06 stays
# wave 1 with its own dependencies; its parallelism with the rest of wave 1
# is preserved.

must_haves:
  truths:
    - "Six source files deleted: src/live-logging/RealTimeTrajectoryAnalyzer.js + src/knowledge-management/{StreamingKnowledgeExtractor,KnowledgeExtractor,KnowledgeDecayTracker,ConceptAbstractionAgent,TrajectoryAnalyzer}.js"
    - "SPEC AC #8 grep returns empty: grep -rn -E 'StreamingKnowledgeExtractor|RealTimeTrajectoryAnalyzer|KnowledgeDecayTracker|ConceptAbstractionAgent' --include=*.js --include=*.ts --include=*.cjs --include=*.mjs . excluding node_modules/dist/.specstory/site/.spec-workflow returns 0 lines"
    - "scripts/combined-status-line.js dead-reader sites at lines 1114, 1263, 1324, 1865-1885 cleaned (per PATTERNS.md anomaly #4 corrected list — line 593 is a comment-only ref and is LEFT alone)"
    - "scripts/combined-status-line.js has a new getProxySystemStatus() function and a [🧠] badge switch block (D-12 enum: healthy/degraded/cooling/disabled/unknown/unreachable)"
    - "[🧠] proxy badge does NOT collide with the existing UKB workflow [🧠N⏳] indicator (PATTERNS.md anomaly #1) — proxy uses single status emoji suffix; UKB uses N+counter form; both visually distinct"
    - "Dashboard renders a new LLM Proxy Health card showing semantic_ok / networkMode / auto_heal_status (D-11)"
    - "Dashboard frontend rebuilt + supervisorctl restarted per CLAUDE.md FUSE caveat"
    - "Tests for removed modules either deleted (KnowledgeExtraction.test.ts + test-knowledge-extraction.js) or skip-marked (integration.test.js + knowledge-system-acceptance.test.js)"
    - "node --check passes on all modified .js files"
    - "After both ETMs restart (already restarted in Plan 34-04 checkpoint), grep -c for the deleted modules in `pgrep -af`-listed processes returns 0 — i.e., no live process imports any deleted file (sanity check; Plan 34-04 already removed the imports)"
    - "KEEP-list (KnowledgeRetriever, UKBDatabaseWriter, QdrantSyncService, KnowledgeStorageService, GraphKnowledge*, GraphDatabaseService, KnowledgeQueryService, KnowledgeExportService) all unchanged — `git diff` shows no edits to these files"
    # Cooling/cooldown observable truths — testable now that 34-03 is a hard dep.
    - "When 34-03's FSM enters cooldown (auto_heal_status='cooldown'), the [🧠] badge renders '[🧠🚫]' AND overallColor is forced red (D-12)"
    - "When 34-03's FSM enters cooldown, the dashboard LLM Proxy Health card's Auto-heal item description reads 'cooldown — N/3 kickstarts in last 5 min' with N reflecting state.proxy.kickstart_timestamps.length"
  artifacts:
    - path: "scripts/combined-status-line.js"
      provides: "getProxySystemStatus() + [🧠] badge switch + dead-reader cleanup"
      contains: "function getProxySystemStatus"
    - path: "integrations/system-health-dashboard/src/components/system-health-dashboard.tsx"
      provides: "LLM Proxy Health card + getProxyHealthItems() builder"
      contains: "LLM Proxy Health"
  key_links:
    - from: "scripts/combined-status-line.js (getProxySystemStatus)"
      to: "GET http://localhost:3034/health/state"
      via: "execSync curl"
      pattern: "/health/state"
    - from: "integrations/system-health-dashboard (LLM Proxy Health card)"
      to: "/api/health-verifier/status"
      via: "redux slice fed by existing reverse-proxy"
      pattern: "/api/health-verifier"
---

<objective>
Plan B of D-08: delete the 6 source files now orphaned by Plan 34-04, clean dead readers in scripts/combined-status-line.js, add the [🧠] proxy badge that consumes state.proxy from Plan 34-02 + 34-03, and add the dashboard LLM Proxy Health card per D-11.

Purpose: closes R5 grep-clean acceptance (SPEC AC #8). Surfaces the proxy slice on the two existing user-facing surfaces (statusline + dashboard).

Output: one commit covering all surface changes. After merge, the [🧠] badge is live in tmux statuslines, the dashboard card is visible at http://localhost:3032, and SPEC AC #8 grep returns empty.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-SPEC.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-CONTEXT.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-PATTERNS.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-02-SUMMARY.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-03-SUMMARY.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-04-SUMMARY.md
@scripts/combined-status-line.js
@integrations/system-health-dashboard/src/components/system-health-dashboard.tsx

<interfaces>
<!-- Existing analog: getKnowledgeSystemStatus() at scripts/combined-status-line.js:591-625 — TEMPLATE for getProxySystemStatus(). -->
<!-- Existing analog: [📚] badge switch block at scripts/combined-status-line.js:1644-1668 — TEMPLATE for [🧠] switch. -->
<!-- Anomaly #1: scripts/combined-status-line.js:1670-1687 ALREADY uses [🧠 prefix for UKB workflow. Distinguish by suffix shape: proxy uses single status emoji ✅/⚠️/🚫/🔇/❓/❌; UKB uses [🧠N⏳M⚠️]. -->
<!-- Existing analog: HealthStatusCard component at integrations/system-health-dashboard/src/components/health-status-card.tsx + getPortDetailItems() builder at system-health-dashboard.tsx:322-348 — TEMPLATE for getProxyHealthItems(). -->
<!-- state.proxy slice from Plan 34-02 (semantic_ok, last_round_trip_ms, networkMode, auto_heal_status, kickstart_count, kickstart_timestamps, consecutive_failures, last_probe_end, reason). -->
<!-- 34-03 wires evaluateAutoHealFSM() — auto_heal_status transitions to 'cooldown'/'disabled' depend on this plan landing. 34-05 surface code reads those transitions. -->
<!-- D-12 [🧠] state enum: 'healthy' (semantic_ok=true) | 'degraded' (semantic_ok=false, FSM not in cooldown) | 'cooling' (auto_heal_status=cooldown) | 'disabled' (auto_heal_status=disabled) | 'unknown' (semantic_ok=null) | 'unreachable' (coordinator down). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete 6 dead source files + 2 dead test files; skip-mark 2 partially-affected tests</name>
  <files>
    src/live-logging/RealTimeTrajectoryAnalyzer.js,
    src/knowledge-management/StreamingKnowledgeExtractor.js,
    src/knowledge-management/KnowledgeExtractor.js,
    src/knowledge-management/KnowledgeDecayTracker.js,
    src/knowledge-management/ConceptAbstractionAgent.js,
    src/knowledge-management/TrajectoryAnalyzer.js,
    tests/unit/knowledge-management/KnowledgeExtraction.test.ts,
    scripts/test-knowledge-extraction.js,
    tests/unit/ontology/integration.test.js,
    tests/acceptance/knowledge-system-acceptance.test.js
  </files>
  <read_first>
    - 34-PATTERNS.md "DELETE-list audit" table (confirms each DELETE target's only consumers are also being deleted, plus where partial-skip is required)
    - 34-CONTEXT.md "KEEP-list" (audit-confirmed live consumers — DO NOT touch KnowledgeRetriever, UKBDatabaseWriter, QdrantSyncService, KnowledgeStorageService, GraphKnowledge*, GraphDatabaseService, KnowledgeQueryService, KnowledgeExportService)
    - tests/unit/ontology/integration.test.js (read fully — identify which describe/it blocks reference deleted modules)
    - tests/acceptance/knowledge-system-acceptance.test.js (read fully — same audit)
  </read_first>
  <action>
**(a) Delete 6 source files** (use `rm`, NOT `git rm` — git will pick up the deletions on commit):

```bash
rm src/live-logging/RealTimeTrajectoryAnalyzer.js
rm src/knowledge-management/StreamingKnowledgeExtractor.js
rm src/knowledge-management/KnowledgeExtractor.js
rm src/knowledge-management/KnowledgeDecayTracker.js
rm src/knowledge-management/ConceptAbstractionAgent.js
rm src/knowledge-management/TrajectoryAnalyzer.js
```

(Note: `src/knowledge-management/TrajectoryAnalyzer.js` is the smaller one used by StreamingKnowledgeExtractor — DISTINCT from `src/live-logging/RealTimeTrajectoryAnalyzer.js`. Both are deleted; do not confuse them.)

**(b) Delete 2 standalone test files** (they exercise only the now-deleted modules):

```bash
rm tests/unit/knowledge-management/KnowledgeExtraction.test.ts
rm scripts/test-knowledge-extraction.js
```

**(c) Skip-mark partially-affected tests.** For `tests/unit/ontology/integration.test.js` and `tests/acceptance/knowledge-system-acceptance.test.js`, run grep first to find the affected sections:

```bash
grep -nE "RealTimeTrajectoryAnalyzer|StreamingKnowledgeExtractor|KnowledgeDecayTracker|ConceptAbstractionAgent|KnowledgeExtractor|TrajectoryAnalyzer" tests/unit/ontology/integration.test.js
grep -nE "RealTimeTrajectoryAnalyzer|StreamingKnowledgeExtractor|KnowledgeDecayTracker|ConceptAbstractionAgent|KnowledgeExtractor|TrajectoryAnalyzer" tests/acceptance/knowledge-system-acceptance.test.js
```

For each `describe(...)` or `it(...)` block that references a deleted module, change `describe(...)` -> `describe.skip(...)` and `it(...)` -> `it.skip(...)`. ALSO remove the dangling `import` lines for the deleted modules at the top of each test file. Add a comment above each `.skip` call: `// Phase 34: skipped — exercises modules deleted by 34-05 (D-08 Plan B)`.

DO NOT delete or skip blocks that don't reference deleted modules — those must continue running.

**(d) Verify the SPEC AC #8 grep is clean.** Run the exact acceptance command:

```bash
grep -rn -E "StreamingKnowledgeExtractor|RealTimeTrajectoryAnalyzer|KnowledgeDecayTracker|ConceptAbstractionAgent" --include="*.js" --include="*.ts" --include="*.cjs" --include="*.mjs" . 2>/dev/null | grep -v node_modules | grep -v dist/ | grep -v .specstory/ | grep -v site/ | grep -v .spec-workflow/
```

Expected: 0 lines of output. If any survive, they are either KEEP-list false positives (unlikely; the audit confirmed otherwise) or new test/script files added since the audit (in which case extend Step (c) skip-marking).

**(e) Verify KEEP-list untouched.** Run:

```bash
git status --porcelain | awk '{print $2}' | sort
```

The output MUST NOT contain any of: `src/knowledge-management/KnowledgeRetriever.js`, `src/knowledge-management/UKBDatabaseWriter.js`, `src/knowledge-management/QdrantSyncService.js`, `src/knowledge-management/KnowledgeStorageService.js`, `src/knowledge-management/GraphKnowledge*.js`, `src/knowledge-management/GraphDatabaseService.js`, `src/knowledge-management/KnowledgeQueryService.js`, `src/knowledge-management/KnowledgeExportService.js`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const deleted = ['src/live-logging/RealTimeTrajectoryAnalyzer.js','src/knowledge-management/StreamingKnowledgeExtractor.js','src/knowledge-management/KnowledgeExtractor.js','src/knowledge-management/KnowledgeDecayTracker.js','src/knowledge-management/ConceptAbstractionAgent.js','src/knowledge-management/TrajectoryAnalyzer.js','tests/unit/knowledge-management/KnowledgeExtraction.test.ts','scripts/test-knowledge-extraction.js']; let fail = false; for (const f of deleted) { if (fs.existsSync(f)) { process.stderr.write('STILL EXISTS: ' + f + '\\n'); fail = true; } } const kept = ['src/knowledge-management/KnowledgeRetriever.js','src/knowledge-management/UKBDatabaseWriter.js','src/knowledge-management/QdrantSyncService.js','src/knowledge-management/KnowledgeStorageService.js','src/knowledge-management/GraphDatabaseService.js','src/knowledge-management/KnowledgeQueryService.js','src/knowledge-management/KnowledgeExportService.js']; for (const f of kept) { if (!fs.existsSync(f)) { process.stderr.write('KEEP-LIST DELETED: ' + f + '\\n'); fail = true; } } if (fail) process.exit(1); process.stdout.write('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 source files deleted (test -e returns false for each).
    - Both standalone test files deleted (test -e returns false).
    - All KEEP-list files still present (test -e returns true for each).
    - SPEC AC #8 grep returns 0 lines: `grep -rn -E "StreamingKnowledgeExtractor|RealTimeTrajectoryAnalyzer|KnowledgeDecayTracker|ConceptAbstractionAgent" --include="*.js" --include="*.ts" --include="*.cjs" --include="*.mjs" . 2>/dev/null | grep -v node_modules | grep -v dist/ | grep -v .specstory/ | grep -v site/ | grep -v .spec-workflow/ | wc -l` returns 0.
    - tests/unit/ontology/integration.test.js: grep for deleted module names matches only `.skip` blocks or removed imports — `grep -E "describe\\(|it\\(" tests/unit/ontology/integration.test.js | grep -v "\\.skip" | xargs -I{} grep -l "RealTimeTrajectoryAnalyzer\\|StreamingKnowledgeExtractor" {}` returns nothing.
    - Same for tests/acceptance/knowledge-system-acceptance.test.js.
    - Jest still discovers the non-skipped suites: `npx jest --listTests tests/unit/ontology/integration.test.js tests/acceptance/knowledge-system-acceptance.test.js` exits 0.
    - **W-2 revision — actually RUN the affected suites post-skip-mark to catch import-time crashes** (test discovery alone is not enough; a dangling import after a partial-skip would import-fail BEFORE the skip block is evaluated):
      `npx jest tests/unit/ontology/integration.test.js tests/acceptance/knowledge-system-acceptance.test.js --passWithNoTests`
      Exit code 0; reported as "X skipped" or "X passed", never "X failed". Skipped blocks do not count as failures. Import-time errors (e.g., a forgotten import line for a deleted module) WILL surface here as a failed test file, even if every block is skipped.
  </acceptance_criteria>
  <done>
    Six source files + two test files deleted; two partially-affected tests skip-marked; SPEC AC #8 grep clean; KEEP-list untouched; affected jest suites RUN cleanly (not just discovered).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getProxySystemStatus() + [🧠] badge to scripts/combined-status-line.js + clean dead readers (PATTERNS.md anomaly #4 corrected line list)</name>
  <files>scripts/combined-status-line.js</files>
  <read_first>
    - scripts/combined-status-line.js — read sections at lines 591-625 (getKnowledgeSystemStatus analog), 1644-1668 ([📚] switch analog), 1670-1687 (existing UKB [🧠 prefix collision per anomaly #1)
    - scripts/combined-status-line.js lines 1110-1116 + 1263 + 1320-1340 + 1865-1885 (the CORRECTED dead-reader sites per PATTERNS.md anomaly #4; SPEC's "line 593" is a comment-only reference and is NOT a dead reader)
    - 34-PATTERNS.md section 4 (full code excerpts: A getProxySystemStatus, B [🧠] switch, C dead-reader cleanup with corrected line list)
    - 34-CONTEXT.md D-12 ([🧠] state enum)
    - **W-1 — ~/.claude/projects/-Users-Q284340-Agentic-coding/memory/feedback_test_statusline_in_tmux.md (project mandate — never `git add` a statusline change without a live-tmux render check; offline output looking right is necessary but not sufficient — tmux's actual rendering is the only ground truth)**
  </read_first>
  <action>
**(a) Add `getProxySystemStatus()` method** to `scripts/combined-status-line.js`. Place it IMMEDIATELY AFTER `getKnowledgeSystemStatus()` (use grep `grep -n 'getKnowledgeSystemStatus' scripts/combined-status-line.js` to find the closing brace of that method, then insert after).

The full body is documented in 34-PATTERNS.md section 4.A. Key contracts:
- Reads `process.env.HEALTH_COORDINATOR_URL || 'http://localhost:3034'` (Pattern B from PATTERNS.md).
- `execSync curl -fs --max-time 2 "${url}/health/state"` (matches getKnowledgeSystemStatus).
- Reads `state.proxy` slice (added in Plan 34-02; null-safe).
- Maps to D-12 enum:
  - if `auto_heal_status === 'disabled'` -> `'disabled'`
  - else if `auto_heal_status === 'cooldown'` -> `'cooling'`
  - else if `semantic_ok === null` -> `'unknown'`
  - else if `semantic_ok === true` -> `'healthy'`
  - else -> `'degraded'`
- Returns `{status, semantic_ok, networkMode, auto_heal_status, kickstart_count, reason}`.
- Catch-all -> `{status: 'unreachable', reason: error.message}` (Pattern A fail-open).

**(b) Wire the new method into the constructor** the same way `getKnowledgeSystemStatus()` is called (around line 81). Add `const proxyStatus = await this.getProxySystemStatus();` immediately after the existing `getKnowledgeSystemStatus()` call.

**(c) Add [🧠] badge switch block** in the rendering section. Place IMMEDIATELY AFTER the existing `[📚]` switch block at lines 1644-1668. Anomaly #1 collision-resolution: the existing UKB indicator at lines 1670-1687 starts `[🧠` and appends N+counter. The proxy badge uses single emoji suffix only — no number, no extra counters — so the two are visually distinguishable. Add a comment above the new switch noting the collision is intentional.

The switch maps the D-12 enum to emoji per 34-PATTERNS.md section 4.B:
- `'healthy'` -> `'[🧠✅]'`
- `'degraded'` -> `'[🧠⚠️]'` + downgrade overallColor green->yellow
- `'cooling'` -> `'[🧠🚫]'` + force overallColor red
- `'disabled'` -> `'[🧠🔇]'`
- `'unknown'` -> `'[🧠❓]'`
- `'unreachable'` (default) -> `'[🧠❌]'` + downgrade green->yellow

**(d) Clean dead readers (CORRECTED line list per PATTERNS.md anomaly #4).** The SPEC list "lines 593, 1114, 1324, 1871, 1880" is partially wrong — line 593 is comment-only and is NOT a dead reader. The actual dead-reader sites are lines 1114, 1263, 1324, and 1865-1885.

For each dead site:
- **Line ~1110-1116 (per-project status path):** the `if (!existsSync(healthFile)) { ... }` branch reads `${codingPath}/.health/${projectName}-transcript-monitor-health.json` which the ETM no longer writes. Replace the branch with the PSM-based liveness check already present at line 1099-1101 (`psm.isProcessAlive(...)`), or simply remove the dead branch if PSM is the live signal.
- **Line ~1263:** `const match = file.match(/^(.+)-transcript-monitor-health\.json$/);` — audit and delete if dead.
- **Line ~1320-1340:** second `transcript-monitor-health.json` join inside `getRunningTranscriptMonitorsSync`. Drop the health-file age check; rely on PSM/pgrep results gathered above.
- **Line ~1865-1885:** the fallback `if (runningProjects.size === 0) { const healthDir = ...; readdirSync(...).filter(f => f.endsWith('-transcript-monitor-health.json')); ... }` — delete the entire `if (runningProjects.size === 0)` block.

LEAVE line ~593 alone — it is a comment-only reference inside `getKnowledgeSystemStatus()` explaining the legacy file's history.

**(e) Verify file still parses:** `node --check scripts/combined-status-line.js` exits 0. Run `bash scripts/combined-status-line.js | head -1` to confirm the script renders a status line without error (depending on coordinator availability the badges may show ❓/❌, but the script must not crash).

**(f) MANDATORY live tmux render check (per project memory feedback_test_statusline_in_tmux.md).** Offline syntax + grep + node-script-only output passing is INSUFFICIENT. The user has reported a recurring "looks right offline, totally broken in tmux" pattern — wcswidth and cell-width tables are not ground truth; only the actual tmux pane render is. Before declaring this task done OR `git add`-ing the file, run the live-render check below and capture proof.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('scripts/combined-status-line.js','utf8'); const required = ['function getProxySystemStatus','getProxySystemStatus()',\"'[🧠✅]'\",\"'[🧠⚠️]'\",\"'[🧠🚫]'\",\"'[🧠🔇]'\",\"'[🧠❓]'\",\"'[🧠❌]'\"]; let fail = false; for (const t of required) { if (!src.includes(t)) { process.stderr.write('MISSING: ' + t + '\\n'); fail = true; } } if (fail) process.exit(1); process.stdout.write('OK')"</automated>
    <automated>node --check scripts/combined-status-line.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function getProxySystemStatus\\|getProxySystemStatus()' scripts/combined-status-line.js` returns at least 2 (one definition, at least one call site).
    - All 6 [🧠X] tokens present in the file: ✅ ⚠️ 🚫 🔇 ❓ ❌ — `grep -cE "\\[🧠(✅|⚠️|🚫|🔇|❓|❌)\\]" scripts/combined-status-line.js` returns 6.
    - Existing UKB indicator at lines 1670-1687 still present (collision-resolved, NOT deleted): `grep -c "ukbPart" scripts/combined-status-line.js` is unchanged from pre-edit.
    - Dead-reader sites cleaned: `grep -c '\\.health.*transcript-monitor-health\\.json' scripts/combined-status-line.js` decreases significantly (target: 0 occurrences in code paths; line 593 comment may be left as historical context — verify line 593 is comment context only).
    - Specifically line 593 LEFT ALONE: `sed -n '593p' scripts/combined-status-line.js` returns a comment line (starts with `//` or is inside a comment block).
    - `node --check scripts/combined-status-line.js` exits 0.
    - Live render: `bash scripts/combined-status-line.js 2>&1 | grep -F "[🧠"` returns at least 1 line containing the new badge.
    - Live render does NOT crash on missing coordinator: `HEALTH_COORDINATOR_URL=http://127.0.0.1:1 bash scripts/combined-status-line.js >/dev/null 2>&1; echo $?` returns 0 (script handles the unreachable case gracefully).
    - **W-1 — Live tmux render check (per project memory feedback_test_statusline_in_tmux.md). MANDATORY before `git add`.** Offline output looking right is NECESSARY BUT NOT SUFFICIENT — tmux's actual rendering is the only ground truth. Run:
      ```bash
      tmux new-session -d -s phase34-render \; \
        set -g status-right "#(bash scripts/combined-status-line.js)" \; \
        refresh-client -S \; \
        capture-pane -p -t phase34-render | grep -F "[🧠" \; \
        kill-session -t phase34-render
      ```
      Acceptance: exit code 0 with the `[🧠` token visible in the captured pane output. If grep returns no match (statusline rendered blank or truncated), the change is broken — DO NOT `git add`. If exit is non-zero, capture the failure mode and revert before staging.
  </acceptance_criteria>
  <done>
    getProxySystemStatus + [🧠] badge live; UKB workflow indicator preserved; dead readers cleaned per corrected line list; line 593 comment left alone; live tmux render check captured the [🧠] token.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add LLM Proxy Health card to system-health-dashboard + frontend rebuild + supervisorctl restart (D-11 + CLAUDE.md FUSE caveat)</name>
  <files>integrations/system-health-dashboard/src/components/system-health-dashboard.tsx</files>
  <read_first>
    - integrations/system-health-dashboard/src/components/system-health-dashboard.tsx — read whole file (or at minimum lines 1-50 for imports + 322-348 for getPortDetailItems analog + the existing HealthStatusCard render sites)
    - integrations/system-health-dashboard/src/components/health-status-card.tsx (the generic card component)
    - 34-PATTERNS.md section 5 (getProxyHealthItems builder + render block + FUSE caveat invocation)
    - 34-CONTEXT.md D-11 (dashboard card content: semantic_ok green/red, networkMode badge, auto_heal_status badge with cooldown countdown)
  </read_first>
  <action>
Add the LLM Proxy Health card to the dashboard.

**(a) Add `getProxyHealthItems()` builder** in `integrations/system-health-dashboard/src/components/system-health-dashboard.tsx`. Place it immediately after `getPortDetailItems()` (around line 348). Body per 34-PATTERNS.md section 5:

```typescript
const getProxyHealthItems = () => {
  // state.proxy slice from coordinator (added in Plan 34-02 + 34-03).
  // Surfaced via existing /api/health-verifier/status reverse-proxy.
  const proxy = (healthStatus as any)?.proxy;
  if (!proxy) {
    return [{ name: 'Proxy semantic', status: 'offline' as const, description: 'No data yet — coordinator unreachable or proxy slice missing' }];
  }
  const semanticStatus: 'operational' | 'warning' | 'error' | 'offline' =
    proxy.semantic_ok === true ? 'operational' :
    proxy.semantic_ok === false ? 'error' : 'offline';
  const cooldownDescription = proxy.auto_heal_status === 'cooldown'
    ? `cooldown — ${(proxy.kickstart_timestamps?.length ?? '?')}/3 kickstarts in last 5 min`
    : proxy.auto_heal_status;
  return [
    {
      name: 'Semantic readiness',
      status: semanticStatus,
      description: proxy.last_round_trip_ms != null ? `${proxy.last_round_trip_ms}ms RTT` : 'no data',
      tooltip: proxy.reason ?? 'OK'
    },
    {
      name: 'Network mode',
      status: proxy.networkMode === 'unknown' ? 'warning' : 'operational',
      description: proxy.networkMode
    },
    {
      name: 'Auto-heal',
      status: proxy.auto_heal_status === 'cooldown' ? 'warning' : (proxy.auto_heal_status === 'disabled' ? 'offline' : 'operational'),
      description: cooldownDescription
    }
  ];
};
```

**(b) Add the card render** alongside the other `<HealthStatusCard>` invocations. Pick a card grid position adjacent to the existing service-health card cluster (planner discretion per CONTEXT — choose visual coherence). Use:

```tsx
<HealthStatusCard
  title="LLM Proxy Health"
  icon={<Brain className="h-5 w-5 text-purple-500" />}
  items={getProxyHealthItems()}
/>
```

Verify `Brain` is imported from `lucide-react` — if not already at line ~22, add it. If `Brain` is already used elsewhere in the file, do NOT re-import.

**(c) Confirm the redux slice carries `proxy`.** The existing reducer typically spreads coordinator `/health/state` into `healthStatus`. Verify via `grep -n 'healthStatus\\|proxy' integrations/system-health-dashboard/src/store/` (location may vary; locate via `find integrations/system-health-dashboard/src/store -type f`). If the reducer EXCLUDES top-level keys, add `proxy` to the allow-list. If it spreads `...state`, no change needed.

**(d) Frontend rebuild + supervisorctl restart per CLAUDE.md FUSE caveat:**

```bash
cd /Users/Q284340/Agentic/coding/integrations/system-health-dashboard && npm run build
docker exec coding-services supervisorctl restart web-services:health-dashboard-frontend
```

NO docker-compose build needed (the directory is bind-mounted). The frontend rebuild + supervisorctl restart re-reads `dist/` after FUSE cache invalidation.

**(e) Live verify:** Open http://localhost:3032 in a browser (use /playwright-cli or /gsd-browser per CLAUDE.md feedback rules — never claim "it works" from DB queries alone). Confirm:
1. The new "LLM Proxy Health" card renders.
2. Three items visible: Semantic readiness / Network mode / Auto-heal.
3. Values reflect coordinator's `state.proxy` (if proxy is up + probe has fired, Semantic readiness shows green; if not, shows offline/error).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('integrations/system-health-dashboard/src/components/system-health-dashboard.tsx','utf8'); const required = ['getProxyHealthItems','LLM Proxy Health','Semantic readiness','Network mode','Auto-heal']; let fail = false; for (const t of required) { if (!src.includes(t)) { process.stderr.write('MISSING: ' + t + '\\n'); fail = true; } } if (fail) process.exit(1); process.stdout.write('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getProxyHealthItems' integrations/system-health-dashboard/src/components/system-health-dashboard.tsx` returns at least 2 (definition + call).
    - `grep -c 'LLM Proxy Health' integrations/system-health-dashboard/src/components/system-health-dashboard.tsx` returns 1.
    - `grep -cE 'Semantic readiness|Network mode|Auto-heal' integrations/system-health-dashboard/src/components/system-health-dashboard.tsx` returns 3.
    - `grep -F 'Brain' integrations/system-health-dashboard/src/components/system-health-dashboard.tsx` returns at least 2 lines (import + usage in the new card).
    - Frontend builds cleanly: `cd integrations/system-health-dashboard && npm run build` exits 0.
    - Supervisorctl restart succeeds: `docker exec coding-services supervisorctl status web-services:health-dashboard-frontend` shows RUNNING.
    - Browser-driven check (use /playwright-cli per CLAUDE.md): visit http://localhost:3032, verify the "LLM Proxy Health" card text is visible. Save a screenshot to `.tmp-verify-34-05-dashboard.png` (delete after the verification phase confirms).
  </acceptance_criteria>
  <done>
    Dashboard card live and visually verified. Frontend bundle rebuilt. supervisorctl restart applied. Brain icon imported. No FUSE-cache-stale issue.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> deleted modules | rm operations are local; reverted by git revert. |
| statusline -> coordinator (GET /health/state) | Localhost-only; existing Pattern B contract. |
| dashboard -> coordinator (via /api/health-verifier/status) | Localhost-only; existing Phase 33 D-03 reverse-proxy. |
| frontend bundle -> docker bind-mount | Read-only mount; FUSE caveat means rebuild + supervisorctl restart required (CLAUDE.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05-01 | Tampering | KEEP-list deletion | mitigate | Task 1 verifier explicitly checks all KEEP-list files still exist after rm operations. |
| T-34-05-02 | Denial of Service | accidental KEEP-list delete | mitigate | Each rm command names the file path explicitly; no glob deletes. KEEP-list assertion runs after the rm batch. |
| T-34-05-03 | Confused-deputy | partial-skip test misses | mitigate | grep audit finds ALL describe/it referencing deleted modules; both files explicitly read in read_first. W-2 revision: post-skip jest run catches import-time crashes that --listTests alone misses. |
| T-34-05-04 | Repudiation | dashboard misrender | mitigate | Browser-driven verify (Task 3 (e)) per CLAUDE.md project rule "ALWAYS use /gsd-browser or /playwright-cli to visually verify UI/dashboard changes." |
| T-34-05-05 | Spoofing | [🧠] collision with UKB | mitigate | Suffix shape distinguishes (single-emoji vs N-counter); comment in code marks the resolution; W-1 live tmux render check confirms both badges coexist visually in the running pane. |
| T-34-05-06 | DoS via stale frontend bundle | FUSE cache stale | mitigate | CLAUDE.md FUSE caveat command included verbatim in action; supervisorctl restart after npm run build invalidates cache. |
| T-34-05-07 | Repudiation | statusline regression | mitigate | W-1 live tmux render check (Task 2 acceptance) is mandatory pre-`git add` per project memory feedback_test_statusline_in_tmux.md. Offline checks alone have repeatedly missed tmux-render-only breakage. |
</threat_model>

<verification>
- All 8 deletion targets gone; all 8 KEEP-list files still present
- SPEC AC #8 grep returns 0 lines
- combined-status-line.js: getProxySystemStatus + 6-state [🧠] switch present; node --check passes
- Live tmux render shows [🧠] badge (W-1 mandate satisfied)
- Skipped-jest suites RUN cleanly post-skip-mark, not just discovered (W-2 mandate satisfied)
- Dashboard rebuilt, supervisorctl restarted, "LLM Proxy Health" card visible at http://localhost:3032
- Cooldown observable truths exercised against the real 34-03 FSM (W-3 dependency landed)
</verification>

<success_criteria>
- 6 source files + 2 test files deleted
- 2 partial-skip tests cleaned and PASSING under jest run (not just discovered)
- KEEP-list untouched (audit-confirmed live consumers)
- combined-status-line.js: new [🧠] badge + dead-reader cleanup; live tmux render captured the badge
- system-health-dashboard.tsx: new LLM Proxy Health card
- Frontend rebuilt + supervisorctl restarted (FUSE caveat)
- SPEC AC #8 grep clean
</success_criteria>

<output>
After completion, create .planning/phases/34-proxy-supervision-and-etm-cleanup/34-05-SUMMARY.md
</output>
</content>
</invoke>