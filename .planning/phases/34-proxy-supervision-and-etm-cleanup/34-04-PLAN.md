---
phase: 34-proxy-supervision-and-etm-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enhanced-transcript-monitor.js
autonomous: false
requirements:
  - "R5-partial: ETM strip — delete the ~80 LoC of dead online-learning paths from scripts/enhanced-transcript-monitor.js (Plan A of D-08); imports + init + methods + per-exchange call + per-prompt-set call + stopHeartbeat + health-output fields"
  - "R6: ETM behavior preserved — both coding ETM (PID 30761) and rapid-automations ETM (PID 54103) keep posting lsl_heartbeat to coordinator and feeding observations to obs_api after the strip"

must_haves:
  truths:
    - "scripts/enhanced-transcript-monitor.js no longer imports RealTimeTrajectoryAnalyzer or StreamingKnowledgeExtractor"
    - "ETM constructor no longer initializes this.knowledgeExtractor or this.trajectoryAnalyzer"
    - "ETM methods initializeKnowledgeExtractor, extractKnowledgeAsync, getKnowledgeExtractionStatus all DELETED"
    - "ETM per-exchange loop no longer calls trajectoryAnalyzer.analyzeTrajectoryState"
    - "ETM per-prompt-set finalization no longer calls extractKnowledgeAsync"
    - "ETM shutdown path no longer calls trajectoryAnalyzer.stopHeartbeat"
    - "ETM health output no longer emits trajectory or knowledgeExtraction keys"
    - "After hard restart of coding ETM: state.lsl[<coding-sid>].lastBeat advances within 30s of a new prompt"
    - "After hard restart of coding ETM: state.knowledge_pipeline.lastObservationAt advances within 30s of a new prompt"
    - "After hard restart of rapid-automations ETM: state.lsl[<rapid-sid>].lastBeat advances within 30s of a new prompt in that project"
    - "Statusline knowledge-pipeline badge stays healthy after ETM restart (Phase 33 lsl_heartbeat contract preserved)"
    - "ETM per-prompt-set duration drops by >=2s vs pre-cleanup baseline (proves dead LLM calls stopped)"
  artifacts:
    - path: "scripts/enhanced-transcript-monitor.js"
      provides: "Stripped ETM with no online-learning code"
      contains: "lsl_heartbeat"
  key_links:
    - from: "scripts/enhanced-transcript-monitor.js"
      to: "POST http://localhost:3034/signals"
      via: "lsl_heartbeat (Phase 33 D-09 contract — preserved)"
      pattern: "lsl_heartbeat"
    - from: "scripts/enhanced-transcript-monitor.js"
      to: "POST http://localhost:3033/api/observations (obs_api)"
      via: "observation feed (preserved)"
      pattern: "/api/observations"
---

<objective>
Surgically strip ~80 LoC of dead online-learning code from scripts/enhanced-transcript-monitor.js per D-08 Plan A. ONLY this file changes. The 6 source files in src/live-logging/ and src/knowledge-management/ are deleted in Plan 34-05 — keeping them on disk during this strip means revertability stays single-file.

Purpose: implements R5 (partial) + R6. Removes the per-exchange RealTimeTrajectoryAnalyzer.analyzeTrajectoryState() LLM call and the per-prompt-set extractKnowledgeAsync() LLM call whose output nothing reads since commit 0049fc179.

Output: one-file commit + a manual checkpoint to verify both ETMs still work cross-project (per D-09, D-10 — coding-first cutover).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-SPEC.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-CONTEXT.md
@.planning/phases/34-proxy-supervision-and-etm-cleanup/34-PATTERNS.md
@scripts/enhanced-transcript-monitor.js

<interfaces>
This is pure deletion. The KEEP contract is documented in PATTERNS.md section 3: lsl_heartbeat POST to coordinator (Phase 33 D-09) and observation feed to obs_api MUST stay intact. PATTERNS.md section 3 also has the exact line ranges and excerpts of every block to delete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip 7 dead-code blocks from enhanced-transcript-monitor.js</name>
  <files>scripts/enhanced-transcript-monitor.js</files>
  <read_first>
- scripts/enhanced-transcript-monitor.js — read all 7 target line ranges before editing. Use grep to confirm CURRENT line numbers (file may have drifted since SPEC was written; line numbers in SPEC are dated 2026-05-09):
  - grep -n RealTimeTrajectoryAnalyzer scripts/enhanced-transcript-monitor.js (imports + constructor refs)
  - grep -n StreamingKnowledgeExtractor scripts/enhanced-transcript-monitor.js (imports + init)
  - grep -nE "initializeKnowledgeExtractor|extractKnowledgeAsync|getKnowledgeExtractionStatus" scripts/enhanced-transcript-monitor.js (method definitions + call sites)
  - grep -n analyzeTrajectoryState scripts/enhanced-transcript-monitor.js (per-exchange call site)
  - grep -n stopHeartbeat scripts/enhanced-transcript-monitor.js (shutdown path; only the trajectoryAnalyzer one)
  - grep -nE "trajectory:|knowledgeExtraction:" scripts/enhanced-transcript-monitor.js (health output fields)
- .planning/phases/34-proxy-supervision-and-etm-cleanup/34-PATTERNS.md (section 3 — exact code excerpts for each block, by line range)
- .planning/phases/34-proxy-supervision-and-etm-cleanup/34-CONTEXT.md (D-08 Plan A — locked line ranges; D-09 + D-10 — restart strategy)
  </read_first>
  <action>
Remove the 7 blocks below from scripts/enhanced-transcript-monitor.js. Use grep to LOCATE each. Line numbers may have drifted from the SPEC values; the SPEC ranges are sourced from a snapshot dated 2026-05-09 and remain authoritative for content but may be off by a few lines for position.

For each block, delete the WHOLE block AS A UNIT — never partial deletion (that creates dangling braces / orphan handlers).

The 7 strip blocks are documented IN FULL in 34-PATTERNS.md section 3 with exact code excerpts. The action is to:
1. Read each excerpt from PATTERNS.md
2. Locate the matching block in scripts/enhanced-transcript-monitor.js (line numbers may have drifted)
3. Delete the entire matched block

Block summary (full content in PATTERNS.md):
- Strip 1: imports for RealTimeTrajectoryAnalyzer + StreamingKnowledgeExtractor (lines 65–83)
- Strip 2: constructor init for knowledgeExtractor + trajectoryAnalyzer (lines 186–217); KEEP `this.semanticAnalyzer = null;`
- Strip 3: methods initializeKnowledgeExtractor, extractKnowledgeAsync, getKnowledgeExtractionStatus (lines 736–846)
- Strip 4: per-exchange `if (this.trajectoryAnalyzer) { ... analyzeTrajectoryState ... }` (lines 3681–3698)
- Strip 5: per-prompt-set `if (this.knowledgeExtractionEnabled && this.knowledgeExtractor && exchanges.length > 0) { extractKnowledgeAsync... }` (lines 3806–3812)
- Strip 6: shutdown `if (this.trajectoryAnalyzer) { trajectoryAnalyzer.stopHeartbeat(); ... }` (lines 4256–4260)
- Strip 7: health-output object literal keys `trajectory:` and `knowledgeExtraction:` (lines 4372–4373); KEEP `streamingActive` and `errors`

KEEP CONTRACTS (NON-NEGOTIABLE):
- lsl_heartbeat POST to coordinator — Phase 33 D-09 contract; R6 acceptance hinges on this.
- /api/observations POST — obs_api feed; preserved.
- transcript-monitor-state.json writer at line ~284 — state-resume artifact; KEPT (out-of-scope per SPEC).
- this.semanticAnalyzer = null vestige — leave.

After deletion, the file MUST still parse: `node --check scripts/enhanced-transcript-monitor.js` exits 0. Run a SMOKE TEST that imports the module — must succeed (catches dangling imports).

Use multiple Edit calls (one per strip block) for clarity, or one careful Write if the file is too large. Confirm each strip with `grep -c '<deleted-token>' scripts/enhanced-transcript-monitor.js` returns 0 BEFORE moving to the next.
  </action>
  <verify>
    <automated>node --check scripts/enhanced-transcript-monitor.js</automated>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('scripts/enhanced-transcript-monitor.js','utf8'); const banned = ['RealTimeTrajectoryAnalyzer','StreamingKnowledgeExtractor','initializeKnowledgeExtractor','extractKnowledgeAsync','getKnowledgeExtractionStatus','analyzeTrajectoryState','this.trajectoryAnalyzer','this.knowledgeExtractor','this.knowledgeExtractionEnabled','this.knowledgeExtractionStatus']; let fail = false; for (const t of banned) { if (src.includes(t)) { process.stderr.write('STILL: ' + t + '\\n'); fail = true; } } if (!src.includes('lsl_heartbeat')) { process.stderr.write('CONTRACT BROKEN: lsl_heartbeat\\n'); fail = true; } if (fail) process.exit(1); process.stdout.write('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c RealTimeTrajectoryAnalyzer scripts/enhanced-transcript-monitor.js returns 0.
    - grep -c StreamingKnowledgeExtractor scripts/enhanced-transcript-monitor.js returns 0.
    - grep -cE "initializeKnowledgeExtractor|extractKnowledgeAsync|getKnowledgeExtractionStatus" scripts/enhanced-transcript-monitor.js returns 0.
    - grep -c analyzeTrajectoryState scripts/enhanced-transcript-monitor.js returns 0.
    - grep -cE "this.trajectoryAnalyzer|this.knowledgeExtractor" scripts/enhanced-transcript-monitor.js returns 0.
    - grep -cE "^[[:space:]]*trajectory:" scripts/enhanced-transcript-monitor.js returns 0.
    - grep -cE "^[[:space:]]*knowledgeExtraction:" scripts/enhanced-transcript-monitor.js returns 0.
    - grep -c lsl_heartbeat scripts/enhanced-transcript-monitor.js is unchanged from pre-edit (>=1) — Phase 33 D-09 contract preserved.
    - grep -c /api/observations scripts/enhanced-transcript-monitor.js is unchanged from pre-edit — obs_api feed preserved.
    - grep -c transcript-monitor-state.json scripts/enhanced-transcript-monitor.js is unchanged from pre-edit (>=1) — state-resume artifact preserved.
    - grep -c "this.semanticAnalyzer = null" scripts/enhanced-transcript-monitor.js returns 1 (vestige preserved).
    - node --check scripts/enhanced-transcript-monitor.js exits 0.
    - Import smoke test succeeds.
    - LoC removed: git diff --stat scripts/enhanced-transcript-monitor.js shows ~80 lines deleted (allow +/- 20).
  </acceptance_criteria>
  <done>
    All 7 dead-code blocks deleted; all KEEP contracts preserved; file parses + imports cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: CHECKPOINT — coding-first ETM hard restart + cross-project smoke verify (D-09 + D-10)</name>
  <action>
What this checkpoint verifies:
Plan 34-04 stripped ~80 LoC of dead online-learning code from scripts/enhanced-transcript-monitor.js. The 6 source files (StreamingKnowledgeExtractor.js, RealTimeTrajectoryAnalyzer.js, etc.) are still on disk — they are deleted in Plan 34-05. This checkpoint verifies the strip didn't break either ETM (coding or rapid-automations) BEFORE we move on to Plan 34-05.
    Per D-10: cutover order is coding ETM first; if coding ETM fails to restart or stops heartbeating, ABORT before touching rapid.

Verification steps:
Run these commands IN ORDER on the host. Each step has a defined pass/fail; if any step fails, STOP and revert the commit (`git revert HEAD`) before approving.

    **Step 1 — locate ETM PIDs:** `pgrep -af enhanced-transcript-monitor | grep -v grep`. Expect two lines, one for project coding and one for rapid-automations. Note both PIDs as OLD_CODING_PID and OLD_RAPID_PID.

    **Step 2 — record baseline timing for the per-prompt-set duration delta:** `grep -E "prompt-set finalized|exchanges processed" ~/.coding/logs/enhanced-transcript-monitor*.log 2>/dev/null | tail -5`. Note typical durations from the last 5 prompt-set lines; we will compare post-cutover.

    **Step 3 — kill the coding ETM only (NOT rapid yet):** `kill -TERM "$OLD_CODING_PID" && sleep 2 && pgrep -af enhanced-transcript-monitor | grep -v grep | grep -i coding | grep -v rapid`. Expected: bin/coding tmux launcher respawns coding ETM within ~5s. NEW_CODING_PID differs from OLD_CODING_PID.

    **Step 4 — verify coding ETM heartbeat advances within 30s:**

    Run before: `BEFORE=$(curl -fs http://localhost:3034/health/state | jq -r '.lsl[] | select(.source=="coding") | .lastBeat' | head -1)`. Wait 35s. Run after: `AFTER=$(curl -fs http://localhost:3034/health/state | jq -r '.lsl[] | select(.source=="coding") | .lastBeat' | head -1)`. Assert `$AFTER` differs from `$BEFORE`. If FAIL: revert the commit (`git revert HEAD`) and report the failure. DO NOT proceed.

    **Step 5 — verify coding observation flow advances on a NEW PROMPT.** Send any new prompt in the coding project (e.g., type something to the active claude session, or invoke claude-mcp with a test message). Then capture before/after of `curl -fs http://localhost:3034/health/state | jq -r .knowledge_pipeline.lastObservationAt` with a 35s sleep between, and assert advancement. If FAIL: revert and abort.

    **Step 6 — verify the knowledge-pipeline statusline badge stays healthy.** `bash scripts/combined-status-line.js | grep -F "[📚"`. Expected: output contains the healthy variant (NOT the red/X variant). If red, the Phase 33 lsl_heartbeat contract is broken — revert and abort.

    **Step 7 — only if all of steps 3-6 PASS, restart the rapid-automations ETM (D-10 cross-project guardrail):** `kill -TERM "$OLD_RAPID_PID" && sleep 2 && pgrep -af enhanced-transcript-monitor | grep -v grep | grep rapid`. Expected: rapid-automations launcher respawns within ~5s. Note NEW_RAPID_PID.

    **Step 8 — verify rapid-automations heartbeat advances within 30s.** (User must send a test prompt in rapid-automations.) Capture before/after of `curl -fs http://localhost:3034/health/state | jq -r '.lsl[] | select(.source=="rapid-automations") | .lastBeat' | head -1` with a 35s sleep between, and assert advancement. If FAIL: revert and abort.

    **Step 9 — verify per-prompt-set duration drop (SPEC AC #12 partial):** Re-run the Step 2 grep and compare. Median duration should drop by >=2s (proves the dead LLM calls stopped firing). If unchanged or increased, log it and proceed but flag for the verification phase — NOT a hard abort.
  </action>
  <resume-signal>Type "approved" if all 9 steps passed (or proceed-with-warning if Step 9 only didn't show >=2s drop). Otherwise describe which step failed and the captured diagnostic so we can revert and fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ETM -> coordinator (POST /signals lsl_heartbeat) | Localhost-only; UID-bound. Phase 33 D-09 contract preserved across this strip. |
| ETM -> obs_api (POST /api/observations) | Localhost-only; preserved unchanged. |
| ETM -> .health/<project>-transcript-monitor-state.json | Local file, written by ETM at line ~284. State-resume artifact, deliberately KEPT (out of scope). |
| Cross-project ETM (rapid-automations PID 54103) | The locked must-not-break failure mode. D-10 cutover order (coding-first, abort-if-broken) protects this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Tampering | strip blocks | mitigate | Each strip block has a corresponding grep-clean check. Smoke test confirms file imports cleanly. |
| T-34-04-02 | Denial of Service | broken ETM | mitigate | D-10 coding-first cutover + Step 4-6 PASS gate before rapid-automations is touched. If coding ETM does not heartbeat, revert; rapid stays untouched. |
| T-34-04-03 | Cross-project blast | rapid-automations ETM | mitigate | Two-project verification (Step 4 coding, Step 8 rapid) explicit in checkpoint. SPEC R6 + AC #9 + AC #10 reflect this. |
| T-34-04-04 | Repudiation | observation flow break | mitigate | Step 5 verifies state.knowledge_pipeline.lastObservationAt advances on a NEW prompt — proves obs_api feed still flows. |
| T-34-04-05 | Confused-deputy | KEEP contract violation | mitigate | grep -c lsl_heartbeat / observations / state.json / semanticAnalyzer ASSERTS those tokens are unchanged from pre-edit. |
</threat_model>

<verification>
- node --check + import smoke test pass
- All grep-clean assertions pass (banned tokens removed; required tokens preserved)
- Checkpoint Step 4 + Step 5 + Step 6 PASS for coding ETM (heartbeat, observation flow, knowledge-pipeline badge)
- Checkpoint Step 8 PASS for rapid-automations ETM
- Step 9: per-prompt-set duration drop >=2s (proves dead LLM calls stopped) — soft verify
</verification>

<success_criteria>
- 7 dead-code blocks deleted from scripts/enhanced-transcript-monitor.js
- KEEP contracts (lsl_heartbeat, /api/observations, state.json writer, semanticAnalyzer vestige) preserved
- Coding ETM restarts and heartbeats within 30s
- Rapid-automations ETM restarts and heartbeats within 30s (cross-project guardrail per D-10)
- Statusline knowledge-pipeline badge stays healthy after both ETM restarts
- Per-prompt-set duration drops by >=2s
</success_criteria>

<output>
After completion, create .planning/phases/34-proxy-supervision-and-etm-cleanup/34-04-SUMMARY.md
</output>
</content>
</invoke>