---
phase: 33-health-monitoring-consolidation
plan: 08
type: execute
wave: 6
depends_on: ["33-07"]
files_modified: []
autonomous: false
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9]
must_haves:
  truths:
    - "All 13 SPEC acceptance criteria pass when running the full test suite"
    - "scripts/__tests__/health-coordinator/run-all.sh exits 0"
    - "Two-session agreement test (AC #5) passes"
    - "Detection latency P95 ≤ 10s, P99 ≤ 15s on 50 trials (AC #6)"
    - "Injection test confirms forced throw → unknown, never healthy (AC #13)"
    - "Keepalive test confirms kill -9 → respawn within 30s (AC #11)"
    - "rules-schema test passes (AC #10)"
    - "All grep gates from SPEC AC return empty (#1, #6, #7)"
  artifacts: []
  key_links: []
---

<objective>
Run the full Phase 33 acceptance suite end-to-end against the cutover system from plan 33-07. Confirm all 13 SPEC acceptance criteria pass. This plan executes only — it makes no code changes. Failures here trigger gap-closure planning (`/gsd-plan-phase --gaps`); successes mark Phase 33 ready for verification.

Purpose: Independent confirmation that Phase 33 actually achieved its goal. The test scripts already exist (created in plan 33-01); the coordinator + reporters + readers have been wired up (plans 33-02..33-06); cutover has happened (plan 33-07). This plan exercises every contract.

Output:
- Documented pass/fail for each of the 13 SPEC acceptance criteria
- A `33-VERIFICATION-PRECHECK.md` artifact summarising results (helps `/gsd-verify-work`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-health-monitoring-consolidation/33-SPEC.md
@.planning/phases/33-health-monitoring-consolidation/33-VALIDATION.md
@.planning/phases/33-health-monitoring-consolidation/33-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run scripts/__tests__/health-coordinator/run-all.sh and capture results for all 13 SPEC ACs</name>
  <files>.planning/phases/33-health-monitoring-consolidation/33-VERIFICATION-PRECHECK.md</files>
  <read_first>
    - .planning/phases/33-health-monitoring-consolidation/33-SPEC.md "Acceptance Criteria" section (the 13 numbered checks)
    - scripts/__tests__/health-coordinator/run-all.sh (the wrapper that calls all .test.sh files)
    - .planning/phases/33-health-monitoring-consolidation/33-07-SUMMARY.md (confirms coordinator is running)
  </read_first>
  <action>
    Execute every SPEC AC check in order, capture pass/fail, write the precheck file. Concrete sequence:

    First, verify the coordinator is running:

        pgrep -f health-coordinator.js | head -1 || { echo "FATAL: coordinator not running — plan 33-07 failed?"; exit 1; }
        curl -fs http://localhost:3034/health || { echo "FATAL: coordinator HTTP unreachable"; exit 1; }

    Then run each acceptance check. For each, record `PASS` or `FAIL` + the captured output.

    AC #1 — `pgrep -fl '(system-monitor-watchdog|global-process-supervisor|global-service-coordinator|global-lsl-coordinator)'` returns empty:

        out=$(pgrep -fl '(system-monitor-watchdog|global-process-supervisor|global-service-coordinator|global-lsl-coordinator)' || true)
        [ -z "$out" ] && echo "AC#1 PASS" || echo "AC#1 FAIL: $out"

    AC #2 — exactly one `com.coding.*` launchctl entry:

        n=$(launchctl list | grep -c '^.*com\\.coding\\.')
        [ "$n" = "1" ] && echo "AC#2 PASS (n=1)" || echo "AC#2 FAIL (n=$n)"
        launchctl list | grep '^.*com\\.coding\\.' || true

    AC #3 — `curl http://localhost:3034/health/state` valid JSON with all 7 top-level keys:

        if curl -fs http://localhost:3034/health/state | jq -e '.container, .services, .lsl, .lsl_by_project, .processes, .generated_at, .coordinator_uptime_s' >/dev/null; then
          echo "AC#3 PASS"
        else
          echo "AC#3 FAIL"
        fi

    AC #4 — container reader returns the same bytes:

        host_state=$(curl -sf http://localhost:3034/health/state)
        cont_state=$(docker exec coding-services curl -sf http://host.docker.internal:3034/health/state)
        # generated_at differs by definition; compare structure instead
        host_keys=$(echo "$host_state" | jq -S 'keys')
        cont_keys=$(echo "$cont_state" | jq -S 'keys')
        [ "$host_keys" = "$cont_keys" ] && echo "AC#4 PASS" || echo "AC#4 FAIL"

    AC #5 — two-session agreement test:

        bash scripts/__tests__/health-coordinator/two-session-agreement.test.sh && echo "AC#5 PASS" || echo "AC#5 FAIL"

    AC #6 — detection latency 50 trials:

        bash scripts/__tests__/health-coordinator/detection-latency.test.sh && echo "AC#6 PASS" || echo "AC#6 FAIL"

    AC #6 ALSO — grep gate `force-recreate`:

        if ! grep -rE "force-recreate|--force-recreate" scripts/health-remediation-actions.js scripts/health-verifier.js; then
          echo "AC#6-grep PASS"
        else
          echo "AC#6-grep FAIL"
        fi

    AC #7 — grep gate readFileSync(.health/...):

        if ! grep -rE "readFileSync.*\\.health/(verification|.*-transcript-monitor)" \
              scripts/health-prompt-hook.js \
              scripts/statusline-health-monitor.js \
              integrations/system-health-dashboard/server.js \
              integrations/mcp-constraint-monitor/src/dashboard-server.js; then
          echo "AC#7 PASS"
        else
          echo "AC#7 FAIL"
        fi

    AC #8 — prompt-hook JSON shape preserved:

        if echo '{"cwd":"/Users/Q284340/Agentic/coding"}' | node scripts/health-prompt-hook.js | python3 -c 'import json,sys; d=json.load(sys.stdin); assert "hookSpecificOutput" in d and "additionalContext" in d["hookSpecificOutput"]'; then
          echo "AC#8 PASS"
        else
          echo "AC#8 FAIL"
        fi

    AC #9 — dashboard endpoints preserved:

        if curl -fs http://localhost:3032/api/health-verifier/status | python3 -m json.tool >/dev/null && \
           curl -fs http://localhost:3032/api/health-verifier/status | jq -e '.data.overallStatus, .data.violationCount, .data.criticalCount, .data.lastUpdate, .data.autoHealingActive' >/dev/null; then
          echo "AC#9 PASS"
        else
          echo "AC#9 FAIL"
        fi

    AC #10 — rules schema validates:

        if node --test scripts/__tests__/health-coordinator/rules-schema.test.mjs 2>&1 | grep -E '# pass|✔' | grep -qv 'fail'; then
          echo "AC#10 PASS"
        else
          echo "AC#10 FAIL"
        fi

    AC #11 — coordinator survives kill -9 within 30s:

        bash scripts/__tests__/health-coordinator/keepalive.test.sh && echo "AC#11 PASS" || echo "AC#11 FAIL"

    AC #12 — docker-health passthrough (the "container reads same as host" test, also covered by AC #4 partially; here we verify the docker-status passthrough specifically):

        bash scripts/__tests__/health-coordinator/docker-health-passthrough.test.sh && echo "AC#12 PASS" || echo "AC#12 FAIL"

    AC #13 — injection test (forced throw → unknown):

        bash scripts/__tests__/health-coordinator/injection.test.sh && echo "AC#13 PASS" || echo "AC#13 FAIL"

    Eviction test (D-10; not strictly in SPEC AC but worth running):

        bash scripts/__tests__/health-coordinator/eviction.test.sh && echo "EVICTION PASS" || echo "EVICTION FAIL"

    Aggregate: run `bash scripts/__tests__/health-coordinator/run-all.sh` once at the end and capture overall exit code.

    Write the precheck file at `.planning/phases/33-health-monitoring-consolidation/33-VERIFICATION-PRECHECK.md` with this structure:

        # Phase 33 Verification Precheck

        Run date: <ISO 8601 timestamp>
        Coordinator PID: <pid>
        Coordinator uptime: <seconds>

        ## SPEC Acceptance Criteria

        | AC # | Description | Status | Evidence |
        |------|-------------|--------|----------|
        | 1    | No legacy daemons running | <PASS/FAIL> | <captured output> |
        | 2    | Exactly 1 com.coding.* launchctl entry | <PASS/FAIL> | <captured output> |
        | 3    | /health/state has all 7 top-level keys | <PASS/FAIL> | <captured output> |
        | 4    | Container reader matches host reader (structure) | <PASS/FAIL> | <captured output> |
        | 5    | Two-session agreement | <PASS/FAIL> | <test output> |
        | 6    | P95 ≤ 10s, P99 ≤ 15s, no force-recreate | <PASS/FAIL> | <P95/P99 numbers + grep result> |
        | 7    | No readFileSync(.health/) in 4 consumers | <PASS/FAIL> | <grep output> |
        | 8    | Prompt-hook JSON shape preserved | <PASS/FAIL> | <python assert result> |
        | 9    | Dashboard endpoints preserved | <PASS/FAIL> | <curl + jq result> |
        | 10   | Rules schema validates | <PASS/FAIL> | <node --test output> |
        | 11   | Keepalive: kill -9 → respawn ≤30s | <PASS/FAIL> | <keepalive test output> |
        | 12   | Docker health passthrough | <PASS/FAIL> | <test output> |
        | 13   | Injection: forced throw → unknown | <PASS/FAIL> | <test output> |

        ## Aggregate

        run-all.sh exit code: <0/non-zero>
        Total SPEC ACs: 13
        Passed: <n>
        Failed: <n>

        ## Notes
        <Any anomalies, flakes, or environmental quirks worth recording>

    If ALL 13 SPEC ACs pass, this task is done. If any fail, the captured evidence in the precheck file feeds the gap-closure plan (`/gsd-plan-phase 33 --gaps`).
  </action>
  <verify>
    <automated>bash scripts/__tests__/health-coordinator/run-all.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/33-health-monitoring-consolidation/33-VERIFICATION-PRECHECK.md` exists with the table populated
    - The aggregate `run-all.sh exit code: 0` is recorded in the precheck file (if non-zero, the precheck file still exists but documents the failures)
    - For each of AC #1, #2, #3, #4, #5, #6 (numeric SLA), #6 (grep), #7, #8, #9, #10, #11, #12, #13: the precheck table has a status of `PASS` (the success target) or `FAIL` (which triggers gap-closure)
    - At minimum, the smoke + grep ACs MUST pass: AC #1, #2, #3, #6-grep, #7, #8, #9, #10. The latency / injection / keepalive ACs may need rerun under quieter system load if they flake; the precheck file should record any flake observations
    - Coordinator PID is captured and matches `pgrep -f health-coordinator.js`
  </acceptance_criteria>
  <done>
    All 13 SPEC ACs are exercised; results captured in `33-VERIFICATION-PRECHECK.md`. If ALL pass, Phase 33 is functionally complete and ready for `/gsd-verify-work`. If any fail, the precheck file documents what failed and feeds gap-closure planning.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — final acceptance review</name>
  <files>(none — human-only verification)</files>
  <action>This is a checkpoint:human-verify task. Execute the steps in <how-to-verify> below. Do not proceed until the human approves with the resume signal.</action>
  <what-built>
    Phase 33 is functionally complete. The cutover from plan 33-07 has been validated end-to-end by the test suite. The precheck file at `.planning/phases/33-health-monitoring-consolidation/33-VERIFICATION-PRECHECK.md` records all 13 SPEC AC outcomes.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/33-health-monitoring-consolidation/33-VERIFICATION-PRECHECK.md`. Every row in the SPEC AC table should be `PASS`.

    2. If any row is `FAIL`, decide:
       - Investigate locally and rerun a single test (transient flake)?
       - Trigger gap-closure planning (`/gsd-plan-phase 33 --gaps`)?
       - Roll back the cutover (last resort)?

    3. Open the dashboards one more time:
       - `http://localhost:3032` — system health dashboard
       - `http://localhost:3030` — constraint monitor dashboard
       Both should render with health data flowing from the new coordinator.

    4. (Optional) Run two real `coding/bin/coding` sessions in tmux on the same project. Confirm dashboard's LSL panel shows BOTH sessions. Close one. Confirm the project still shows `healthy` in the dashboard's project rollup view.

    5. (Optional) Tail `.logs/health-coordinator.log` for ~30s — verify steady 5s tick activity without errors.

    If steps 1-3 all pass: approve. Phase 33 is complete; STATE.md will be updated by `/gsd-mark-complete` after this human approval.

    If any step reveals a real failure: do NOT approve. Instead capture diagnostics and trigger gap closure or rollback.
  </how-to-verify>
  <resume-signal>Type "approved" if all SPEC ACs pass and the dashboards work; otherwise describe the failure and which row(s) of the precheck table failed.</resume-signal>
  <verify>
    <automated>echo "human-verify checkpoint — see how-to-verify steps; approval required"</automated>
  </verify>
  <done>Human approves via the resume signal after completing the how-to-verify steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (n/a) | This plan only runs tests and writes a markdown summary. No new boundaries are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08-01 | T (Tampering) | A test that silently passes when it should fail would let a real regression ship | mitigate | Each AC check uses explicit assertions (jq -e, python assert, exit-code checks). The grep gates are negative assertions (`! grep ...`) that fail loud if the forbidden pattern reappears. SPEC R6 grep gate has been enforced in plans 33-02..33-05. |
| T-33-08-02 | I (Information Disclosure) | The precheck file may include verbose curl output with paths/PIDs | accept | File is in `.planning/`, committed to git, same scope as other planning docs. No secrets. |
</threat_model>

<verification>
- All 13 SPEC ACs exercised.
- `33-VERIFICATION-PRECHECK.md` written with the result table.
- `run-all.sh` exit code recorded.
- Human checkpoint approved.
</verification>

<success_criteria>
- [ ] Coordinator running and HTTP-reachable (preflight)
- [ ] All 13 SPEC ACs exercised
- [ ] `33-VERIFICATION-PRECHECK.md` exists with full table
- [ ] `bash scripts/__tests__/health-coordinator/run-all.sh` exits 0 (or any failure is captured + understood)
- [ ] Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/33-health-monitoring-consolidation/33-08-SUMMARY.md` documenting:
- Aggregate AC pass/fail count
- Path to the precheck file
- Note any flakes or environmental quirks
- Confirmation of human approval
- Recommended next step: `/gsd-verify-work 33` (the orchestrator's verification step)
</output>
