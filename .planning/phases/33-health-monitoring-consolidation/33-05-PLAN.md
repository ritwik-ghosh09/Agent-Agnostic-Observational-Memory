---
phase: 33-health-monitoring-consolidation
plan: 05
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - scripts/health-prompt-hook.js
  - integrations/system-health-dashboard/server.js
  - integrations/mcp-constraint-monitor/src/dashboard-server.js
autonomous: true
requirements: [R2, R6, R8]
must_haves:
  truths:
    - "scripts/health-prompt-hook.js does a single GET /health/state instead of multiple readFileSync of .health/*.json"
    - "scripts/health-prompt-hook.js preserves its existing graceful exit-0 branch when invoked outside the coding repo (per user-resolved Q3)"
    - "scripts/health-prompt-hook.js returns the SPEC R8 JSON shape: hookSpecificOutput.additionalContext"
    - "integrations/system-health-dashboard/server.js's 4 /api/health-verifier/* routes reverse-proxy to host coordinator"
    - "integrations/system-health-dashboard/server.js returns 503 with overallStatus: 'unknown' (NOT 'healthy') when coordinator unreachable"
    - "integrations/mcp-constraint-monitor/src/dashboard-server.js's 2 health-verifier readers reverse-proxy to host coordinator (per user-resolved Q2)"
    - "Dashboard frontend dist/ is NOT rebuilt (compat preserved per SPEC R8)"
  artifacts:
    - path: "scripts/health-prompt-hook.js"
      provides: "Single fetch to /health/state; preserves R8 JSON shape; preserves out-of-coding-env exit-0 branch"
      contains: "HEALTH_COORDINATOR_URL"
    - path: "integrations/system-health-dashboard/server.js"
      provides: "4 routes reverse-proxy to coordinator; existing dashboard frontend works unchanged"
      contains: "_forwardCoordinator"
    - path: "integrations/mcp-constraint-monitor/src/dashboard-server.js"
      provides: "2 health-verifier reader routes reverse-proxy to coordinator (closes RESEARCH §8 leak)"
      contains: "HEALTH_COORDINATOR_URL"
  key_links:
    - from: "scripts/health-prompt-hook.js"
      to: "coordinator's /health/state"
      via: "fetch GET"
      pattern: "fetch.*HEALTH_COORDINATOR_URL.*health/state"
    - from: "integrations/system-health-dashboard/server.js"
      to: "coordinator's /health/state and /health/refresh"
      via: "fetch (reverse-proxy)"
      pattern: "HEALTH_COORDINATOR_URL"
    - from: "integrations/mcp-constraint-monitor/src/dashboard-server.js"
      to: "coordinator's /health/state"
      via: "fetch GET"
      pattern: "HEALTH_COORDINATOR_URL"
---

<objective>
Migrate FOUR consumer routes (per user-resolved Q2: include `mcp-constraint-monitor` as the 4th consumer even though SPEC's grep targets don't list it — RESEARCH §8 leak) from `readFileSync(.health/*.json)` to `fetch(${HEALTH_COORDINATOR_URL}/health/state)`:

1. `scripts/health-prompt-hook.js` — single `fetch` replaces multiple readFileSync calls. PRESERVE the existing graceful exit-0 branch when invoked outside the coding repo (per user-resolved Q3).
2. `integrations/system-health-dashboard/server.js` — 4 routes (`/api/health-verifier/status`, `/api/health-verifier/report`, `/api/health-verifier/verify`, `/api/health`) reverse-proxy to coordinator. Existing dashboard frontend (`dist/`) is NOT rebuilt; backward-compat JSON shapes preserved per SPEC R8.
3. `integrations/mcp-constraint-monitor/src/dashboard-server.js` — 2 health-verifier reader handlers (lines ~961 and ~1005) reverse-proxy to coordinator. Closes the silent-breakage leak documented in RESEARCH §8.

CRITICAL: All three migrations follow SPEC R6 — when coordinator is unreachable, return `overallStatus: 'unknown'` (NOT `'healthy'`).

Purpose: After this plan, NO consumer reads `.health/verification-status.json`, `.health/verification-report.json`, or `.health/<projectName>-transcript-monitor-health.json`. SPEC AC #7 grep gate (`! grep -rE "readFileSync.*\.health/(verification|.*-transcript-monitor)" ...`) passes.

Output:
- 3 modified files
- ~100-200 lines modified (route handlers swap their data source from local file to upstream HTTP)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-health-monitoring-consolidation/33-SPEC.md
@.planning/phases/33-health-monitoring-consolidation/33-CONTEXT.md
@.planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md
@.planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md
@scripts/health-prompt-hook.js
@integrations/system-health-dashboard/server.js
@integrations/mcp-constraint-monitor/src/dashboard-server.js
@.planning/phases/33-health-monitoring-consolidation/33-03-SUMMARY.md

<interfaces>
Coordinator endpoint shape (built in plan 33-03):

GET ${HEALTH_COORDINATOR_URL}/health/state -> {
  container: { healthcheck: 'starting'|'healthy'|'unhealthy'|'unknown'|'none' },
  services:  Array<{ name, status, last_seen, pid }>,
  lsl:       Record<string, { status, lastBeat, projectPath, projectName, ... }>,
  lsl_by_project: Record<string, 'healthy'|'degraded'|'unknown'>,
  processes: Array<{ name, pid, status }>,
  databases: { status },
  generated_at: string,
  coordinator_uptime_s: number
}

POST ${HEALTH_COORDINATOR_URL}/health/refresh -> same as GET /health/state, but force-tick first

Dashboard backend's existing JSON envelopes (must be preserved per SPEC R8):

GET /api/health-verifier/status -> {
  status: 'success',
  data: {
    overallStatus: 'healthy'|'unhealthy'|'unknown',
    violationCount: number,
    criticalCount: number,
    lastUpdate: string (ISO 8601),
    autoHealingActive: boolean
  }
}

GET /api/health-verifier/report -> {
  status: 'success',
  data: { checks: [...], violations: [...] }
}

POST /api/health-verifier/verify -> { status: 'success', data: ... } (now triggered by POST /health/refresh)

GET /api/health -> small health envelope used by dashboard's own self-check

Existing in-repo proxy pattern to mirror (integrations/system-health-dashboard/server.js:4162-4172):

async _forwardObsApi(req, res, pathAndQuery) {
  const base = process.env.OBS_API_URL || 'http://host.docker.internal:12436';
  const url = `${base}${pathAndQuery}`;
  try {
    const upstream = await fetch(url, { method: 'GET' });
    const body = await upstream.text();
    res.status(upstream.status).type(upstream.headers.get('content-type') || 'application/json').send(body);
  } catch (err) {
    process.stderr.write(`[ObservationsAPI] forward to ${url} failed: ${err.message}\n`);
    res.status(502).json({ error: 'Observations API unreachable' });
  }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite scripts/health-prompt-hook.js to single fetch /health/state (preserve out-of-coding-env exit-0 branch per Q3)</name>
  <files>scripts/health-prompt-hook.js</files>
  <read_first>
    - scripts/health-prompt-hook.js (read fully — file is ~370 lines)
    - .planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md "scripts/health-prompt-hook.js" section — explicit Read first / Apply / Landmines lists
    - .planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md "9. Pitfalls" row about prompt-hook outside coding env
    - .planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md "Open Questions" #3 (preserve graceful exit when not in coding env)
    - planning_context user-resolved Q3: graceful exit-0 with empty additionalContext when HEALTH_COORDINATOR_URL is unset AND cwd is not the coding repo
  </read_first>
  <action>
    Surgical rewrite of `scripts/health-prompt-hook.js`. Apply these changes:

    PRESERVE:
    - The Claude Code hook contract (file MUST `process.exit(0)` even on errors so the user's prompt isn't blocked).
    - The output JSON shape: `{ hookSpecificOutput: { hookEventName: 'UserPromptSubmit', additionalContext: <string> } }` (SPEC R8).
    - The existing `existsSync(VERIFIER_SCRIPT)` early-exit branch — this is the "running outside the coding repo" graceful no-op (RESEARCH §9, planning_context Q3). Keep it.

    EXTEND the early-exit branch per Q3: When `HEALTH_COORDINATOR_URL` is unset AND `process.cwd()` is not the coding repo (heuristic: `existsSync(VERIFIER_SCRIPT)` returns false), return the same empty `{ hookSpecificOutput: { hookEventName, additionalContext: '' } }` and exit 0. Do NOT inject `unknown` warnings into prompts when running outside the coding repo. SPEC R6's no-fallback-to-healthy applies to coordinator-side checks; this consumer-side env-detection branch is a legitimate carve-out (per Q3).

    DELETE:
    - All `readFileSync(.health/<projectName>-transcript-monitor-health.json)` reads.
    - All `readFileSync(.health/.lsl-recovery-lock)` and `.health/lsl-watchdog-heartbeat.json` reads.
    - All `readFileSync(.global-lsl-registry.json)` reads.
    - All `readFileSync(.observations/db-recovering.json)` reads.
    - The spawn of `global-lsl-coordinator.js` at lines ~245-251 (the daemon is being deleted in plan 33-06).
    - `_watchdogIsFresh` at lines ~280-297 (no longer relevant).

    REPLACE the body of `checkHealthStatus()` (lines ~73-100) with:

        async function checkHealthStatus() {
          const url = process.env.HEALTH_COORDINATOR_URL;

          // Q3 carve-out: outside coding repo. Maintain the existing graceful no-op
          // branch — SPEC R6's no-fallback-to-healthy applies to coordinator-side
          // checks, not to this consumer-side env-detection branch.
          if (!existsSync(VERIFIER_SCRIPT)) {
            return { servicesAvailable: false, exists: false, isStale: false, shouldBlock: false, status: null };
          }

          // Inside coding repo. Coordinator URL must work; if it doesn't, surface 'unknown'.
          const coordinator = url || 'http://localhost:3034';
          try {
            const r = await fetch(`${coordinator}/health/state`);
            if (!r.ok) {
              return {
                servicesAvailable: true,
                exists: true,
                isStale: false,
                shouldBlock: false,
                status: { overallStatus: 'unknown', upstream: `http_${r.status}` }
              };
            }
            const state = await r.json();
            return {
              servicesAvailable: true,
              exists: true,
              isStale: false,
              shouldBlock: false,
              status: deriveSummary(state)
            };
          } catch (err) {
            // SPEC R6: NOT 'healthy' on exception. Surface 'unknown'.
            return {
              servicesAvailable: true,
              exists: true,
              isStale: false,
              shouldBlock: false,
              status: { overallStatus: 'unknown', upstream: 'unreachable', error: err.message }
            };
          }
        }

        function deriveSummary(state) {
          // Reduce coordinator state to the prompt-hook's summary shape.
          // The prompt-hook output JSON shape is preserved per SPEC R8; only the
          // additionalContext STRING content may change.
          const issues = [];
          if (state.container?.healthcheck === 'unhealthy') issues.push('container unhealthy');
          if (state.databases?.status && state.databases.status !== 'healthy') issues.push(`db ${state.databases.status}`);
          for (const svc of state.services || []) {
            if (svc.status !== 'running') issues.push(`service ${svc.name} ${svc.status}`);
          }
          for (const [project, status] of Object.entries(state.lsl_by_project || {})) {
            if (status !== 'healthy') issues.push(`LSL ${project} ${status}`);
          }
          return {
            overallStatus: issues.length === 0 ? 'healthy' : 'unhealthy',
            issues,
            generated_at: state.generated_at
          };
        }

    PRESERVE the existing `outputHealthContext` (lines ~303-346) — keep its output JSON shape; only its data source changes (now reads from the `status` returned by `checkHealthStatus()` above).

    Keep the file's existing `process.exit(0)` on Fatal errors (lines ~64-67, ~362-365) — Claude Code hooks must never block.

    Re-validate with `node --check scripts/health-prompt-hook.js`.
  </action>
  <verify>
    <automated>node --check scripts/health-prompt-hook.js && echo '{"cwd":"/Users/Q284340/Agentic/coding"}' | HEALTH_COORDINATOR_URL=http://127.0.0.1:1 node scripts/health-prompt-hook.js | python3 -c 'import json,sys; d=json.load(sys.stdin); assert "hookSpecificOutput" in d and "additionalContext" in d["hookSpecificOutput"], "shape broken"; print("ok")'</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/health-prompt-hook.js` exits 0
    - `grep -c "HEALTH_COORDINATOR_URL" scripts/health-prompt-hook.js` is at least 1
    - `grep -c "fetch.*health/state\|fetch.*coordinator" scripts/health-prompt-hook.js` is at least 1
    - `grep -c "deriveSummary" scripts/health-prompt-hook.js` is at least 1
    - `! grep -E "readFileSync.*\.health/(verification|.*-transcript-monitor)" scripts/health-prompt-hook.js` exits 0 (SPEC AC #7 grep gate)
    - `! grep -E "readFileSync.*\.health/\.lsl-recovery-lock|readFileSync.*\.health/lsl-watchdog-heartbeat" scripts/health-prompt-hook.js` exits 0 (legacy files removed)
    - `! grep -q "readFileSync.*\.observations/db-recovering" scripts/health-prompt-hook.js` exits 0
    - `! grep -q "readFileSync.*\.global-lsl-registry" scripts/health-prompt-hook.js` exits 0
    - `! grep -q "global-lsl-coordinator" scripts/health-prompt-hook.js` exits 0 (deleted daemon reference removed)
    - `! grep -nE "catch[^/]*return\s*['\"]healthy" scripts/health-prompt-hook.js` exits 0 (SPEC R6 grep gate)
    - The Q3 graceful exit branch is preserved: `grep -c "existsSync.*VERIFIER_SCRIPT\|!existsSync(VERIFIER_SCRIPT)" scripts/health-prompt-hook.js` is at least 1
    - Output JSON shape preserved: SPEC AC #8 — `echo '{"cwd":"/Users/Q284340/Agentic/coding"}' | HEALTH_COORDINATOR_URL=http://127.0.0.1:1 node scripts/health-prompt-hook.js | python3 -c 'import json,sys; d=json.load(sys.stdin); assert "hookSpecificOutput" in d and "additionalContext" in d["hookSpecificOutput"]'` exits 0 (works even with coordinator unreachable, because the hook must never block — exits 0 with `additionalContext` being either an unknown-marker string or empty)
  </acceptance_criteria>
  <done>
    `scripts/health-prompt-hook.js` does a single `fetch` to coordinator's `/health/state`, returns SPEC R8-compliant JSON shape, and preserves the Q3 graceful no-op branch when invoked outside the coding repo. No legacy `.health/*.json` reads. SPEC R6 grep gate clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate integrations/system-health-dashboard/server.js (4 routes reverse-proxy to coordinator)</name>
  <files>integrations/system-health-dashboard/server.js</files>
  <read_first>
    - integrations/system-health-dashboard/server.js lines 200-230 (route registration)
    - integrations/system-health-dashboard/server.js lines 300-360 (handleGetHealthStatus — the readFileSync site at ~325)
    - integrations/system-health-dashboard/server.js lines 470-510 (handleGetHealthReport — the readFileSync site at ~493)
    - integrations/system-health-dashboard/server.js lines 510-550 (handleTriggerVerification — the execSync verifier-script invocation at ~524)
    - integrations/system-health-dashboard/server.js lines 4162-4172 (the verbatim _forwardObsApi pattern to mirror — analog)
    - integrations/system-health-dashboard/server.js lines 360-410 (checkDaemonHeartbeat / restartDaemon — DELETE per PATTERNS.md)
    - .planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md "integrations/system-health-dashboard/server.js" section
    - .planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md "6. host.docker.internal reachability" — the 503 with overallStatus: 'unknown' shape
  </read_first>
  <action>
    Surgical edit of `integrations/system-health-dashboard/server.js`. The file is bind-mounted in the Docker container — no Docker rebuild needed for this task; just `npm run build` if there's a TS step (this file is .js per the existing source — no TS compile).

    ADD a `_forwardCoordinator` helper method on the class (alongside the existing `_forwardObsApi` near line 4160):

        /**
         * Reverse-proxy a request to the host health-coordinator.
         * On upstream failure, returns 503 with the SPEC R8 JSON shape using
         * overallStatus: 'unknown' (NEVER 'healthy' — SPEC R6).
         */
        async _forwardCoordinator(req, res, pathAndQuery) {
          const base = process.env.HEALTH_COORDINATOR_URL || 'http://host.docker.internal:3034';
          const url = `${base}${pathAndQuery}`;
          try {
            const upstream = await fetch(url, { method: req.method, headers: { 'Content-Type': 'application/json' } });
            const body = await upstream.text();
            res.status(upstream.status).type(upstream.headers.get('content-type') || 'application/json').send(body);
          } catch (err) {
            process.stderr.write(`[HealthCoordinatorProxy] forward to ${url} failed: ${err.message}\n`);
            // Return SPEC R8-compliant shape so dashboard frontend keeps rendering.
            res.status(503).json({
              status: 'success',
              data: {
                overallStatus: 'unknown',
                violationCount: 0,
                criticalCount: 0,
                lastUpdate: new Date().toISOString(),
                autoHealingActive: false,
                upstream: 'unreachable'
              }
            });
          }
        }

    REPLACE the body of `handleGetHealthStatus` (lines ~307-357) with:

        async handleGetHealthStatus(req, res) {
          const base = process.env.HEALTH_COORDINATOR_URL || 'http://host.docker.internal:3034';
          try {
            const upstream = await fetch(`${base}/health/state`);
            if (!upstream.ok) {
              return res.status(503).json({
                status: 'success',
                data: {
                  overallStatus: 'unknown',
                  violationCount: 0,
                  criticalCount: 0,
                  lastUpdate: new Date().toISOString(),
                  autoHealingActive: false,
                  upstream: `HTTP ${upstream.status}`
                }
              });
            }
            const state = await upstream.json();
            // Reshape coordinator state into existing dashboard envelope (SPEC R8).
            const violations = [];
            if (state.container?.healthcheck === 'unhealthy') violations.push({ kind: 'container', severity: 'critical' });
            for (const svc of state.services || []) {
              if (svc.status !== 'running') violations.push({ kind: `service.${svc.name}`, severity: 'high' });
            }
            const overallStatus = violations.length === 0 ? 'healthy' :
              violations.some(v => v.severity === 'critical') ? 'unhealthy' : 'degraded';
            res.json({
              status: 'success',
              data: {
                overallStatus,
                violationCount: violations.length,
                criticalCount: violations.filter(v => v.severity === 'critical').length,
                lastUpdate: state.generated_at,
                autoHealingActive: false  // D-08: narrow heals; not "actively healing"
              }
            });
          } catch (err) {
            process.stderr.write(`[Dashboard] health-verifier/status proxy failed: ${err.message}\n`);
            res.status(503).json({
              status: 'success',
              data: { overallStatus: 'unknown', violationCount: 0, criticalCount: 0, lastUpdate: new Date().toISOString(), autoHealingActive: false, upstream: 'unreachable' }
            });
          }
        }

    REPLACE the body of `handleGetHealthReport` (lines ~478-507) with a similar reshape using `state.services`, `state.processes`, `state.lsl_by_project` for the `data.checks` array. Keep the existing JSON envelope `{ status: 'success', data: { checks: [...], violations: [...] } }`. On upstream failure, return 503 with empty `checks: []`, `violations: []`, but `overallStatus: 'unknown'` set on a sibling key for compatibility.

    REPLACE the body of `handleTriggerVerification` (lines ~512-544) with:

        async handleTriggerVerification(req, res) {
          const base = process.env.HEALTH_COORDINATOR_URL || 'http://host.docker.internal:3034';
          try {
            const upstream = await fetch(`${base}/health/refresh`, { method: 'POST' });
            const body = await upstream.text();
            res.status(upstream.status).type('application/json').send(body);
          } catch (err) {
            process.stderr.write(`[Dashboard] health-verifier/verify proxy failed: ${err.message}\n`);
            res.status(503).json({ status: 'error', error: 'coordinator unreachable' });
          }
        }

    DELETE these methods entirely (PATTERNS.md "Landmines"):
    - `checkDaemonHeartbeat` (lines ~363-398) — coordinator's launchd KeepAlive is the heartbeat truth
    - `restartDaemon` (lines ~403+) — dashboard never restarts a daemon

    PRESERVE every other route (`/api/observations`, `/api/digests`, `/api/insights`, etc.) — these are owned by obs-api proxy, NOT touched in this phase.

    W4 PRE-GREP STEP for `handleRestartService`: before editing, run this grep against the surrounding lines (~549-650, but extend if `handleRestartService` is longer) to enumerate every broad-heal token that survives:

        grep -nE "force-recreate|docker-compose up -d|--force-recreate" scripts/health-remediation-actions.js

    AND in this same file:

        grep -nE "force-recreate|docker-compose up -d|--force-recreate" integrations/system-health-dashboard/server.js

    Each match in `handleRestartService` (or its helpers in this file) MUST be either:
    (a) NEUTERED — replace the broad-heal branch body with `res.status(400).json({ error: 'narrow heals only — broad-heal removed in Phase 33 per D-08', service: <name> });` and ensure no `--force-recreate` / `docker-compose up -d` token remains anywhere in the function body.
    (b) DELETED — if a per-service branch is exclusively a broad-heal call (no narrow alternative), remove the entire branch. If `handleRestartService` is left with no surviving narrow branches, the dispatcher itself becomes a stub returning 400 for every input.
    (c) PRESERVED — branches that already use `supervisorctl restart <service>` (in-container narrow heal) or `docker restart coding-services` (one-shot, no `--force-recreate`) are legitimate D-08 narrow heals; keep them.

    The acceptance criteria below convert the post-edit state into a hard grep gate: `! grep -nE "force-recreate|docker-compose up -d|--force-recreate" integrations/system-health-dashboard/server.js` MUST exit 0 after the task completes.

    Re-validate with `node --check integrations/system-health-dashboard/server.js`.
  </action>
  <verify>
    <automated>node --check integrations/system-health-dashboard/server.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check integrations/system-health-dashboard/server.js` exits 0
    - `grep -c "HEALTH_COORDINATOR_URL" integrations/system-health-dashboard/server.js` is at least 2 (multiple route handlers reference it)
    - `grep -c "_forwardCoordinator\|/health/state\|/health/refresh" integrations/system-health-dashboard/server.js` is at least 3
    - `! grep -E "readFileSync.*\.health/(verification|.*-transcript-monitor)" integrations/system-health-dashboard/server.js` exits 0 (SPEC AC #7 grep gate)
    - `! grep -nE "execSync.*verifierScript|spawn.*verifierScript" integrations/system-health-dashboard/server.js` exits 0 (no in-container verifier spawn — D-04 forbids)
    - `! grep -q "checkDaemonHeartbeat" integrations/system-health-dashboard/server.js` exits 0 (PATTERNS landmine — method deleted)
    - `! grep -q "restartDaemon\b" integrations/system-health-dashboard/server.js` exits 0 (PATTERNS landmine — method deleted; the substring 'restartDaemon' must not appear anywhere)
    - `! grep -nE "catch[^/]*return\s*['\"]healthy|catch[^/]*overallStatus\s*:\s*['\"]healthy" integrations/system-health-dashboard/server.js` exits 0 (SPEC R6 — no fallback-to-healthy)
    - `! grep -q "force-recreate" integrations/system-health-dashboard/server.js` exits 0 (SPEC R5)
    - The 4 routes are still registered: `grep -cE "/api/health-verifier/(status|report|verify|restart-service)|/api/health\b" integrations/system-health-dashboard/server.js` is at least 4
    - W4 broad-heal gate: `! grep -nE "force-recreate|docker-compose up -d|--force-recreate" integrations/system-health-dashboard/server.js` exits 0 (no broad-heal token surfaces in `handleRestartService` or any other handler in this file)
    - W8 dashboard frontend supervisorctl gate: `! grep -rE "supervisorctl|supervisor_status|expected_processes" integrations/system-health-dashboard/dist/` exits 0. (Pre-checked at planning time: dist/ contains only `index.html`, `health-icon.svg`, and bundled JS/CSS in `assets/`; none reference these tokens. The frontend doesn't need supervisorctl detail. If a future rebuild reintroduces these tokens, the supervisorctl filter logic must be re-implemented inside `integrations/system-health-dashboard/server.js` rather than restored to the dropped `verifySupervisord` path — D-08 forbids the latter.)
    - The dashboard backend still returns SPEC R8-compliant top-level keys when coordinator unreachable: simulate by setting `HEALTH_COORDINATOR_URL=http://127.0.0.1:1` and starting the backend, then `curl -s http://localhost:3032/api/health-verifier/status | jq -e '.data.overallStatus,.data.violationCount,.data.criticalCount,.data.lastUpdate,.data.autoHealingActive'` exits 0 with `overallStatus == "unknown"` (this requires the dashboard backend running; mark MISSING if unable to spin it up; integration test in plan 33-07 covers this)
  </acceptance_criteria>
  <done>
    `integrations/system-health-dashboard/server.js`'s 4 health-verifier routes reverse-proxy to the coordinator. The `_forwardCoordinator` helper is the single mediator. On coordinator failure, all routes return SPEC R8-compliant 503 responses with `overallStatus: 'unknown'` (never 'healthy'). The legacy `checkDaemonHeartbeat` / `restartDaemon` methods are gone. The dashboard frontend `dist/` is NOT rebuilt — backward-compat preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate integrations/mcp-constraint-monitor/src/dashboard-server.js (2 health reader routes; SUBMODULE REBUILD REQUIRED)</name>
  <files>integrations/mcp-constraint-monitor/src/dashboard-server.js</files>
  <read_first>
    - integrations/mcp-constraint-monitor/src/dashboard-server.js (read lines 1-30 for imports, then lines 950-1040 for the two health-verifier reader handlers)
    - .planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md "integrations/mcp-constraint-monitor/src/dashboard-server.js" section — Landmines
    - .planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md "8. Constraint Monitor leak" finding (this is the file:line citation source)
    - planning_context user-resolved Q2: this file IS in scope for Phase 33 even though SPEC's grep targets don't list it
    - integrations/mcp-constraint-monitor/package.json (check whether `npm run build` is required; this is a TypeScript-built submodule per CLAUDE.md)
    - CLAUDE.md "Submodules & Build Pipeline" section — submodule requires `npm run build` THEN Docker rebuild
  </read_first>
  <action>
    Surgical edit of `integrations/mcp-constraint-monitor/src/dashboard-server.js`. Apply these changes:

    REPLACE the body of `handleGetHealthStatus` (around line 955-997 — the `readFileSync(statusPath, ...)` block at ~973):

        async handleGetHealthStatus(req, res) {
          const url = process.env.HEALTH_COORDINATOR_URL || 'http://host.docker.internal:3034';
          try {
            const upstream = await fetch(`${url}/health/state`);
            if (!upstream.ok) {
              return res.status(503).json({
                status: 'success',
                data: { status: 'unknown', overallStatus: 'unknown', upstream: `HTTP ${upstream.status}` }
              });
            }
            const state = await upstream.json();
            // Reshape into the constraint-dashboard's existing envelope.
            res.json({
              status: 'success',
              data: {
                status: state.container?.healthcheck === 'healthy' ? 'healthy' :
                        state.container?.healthcheck === 'unhealthy' ? 'unhealthy' : 'unknown',
                overallStatus: state.container?.healthcheck || 'unknown',
                generated_at: state.generated_at,
                services: state.services || [],
                lsl_by_project: state.lsl_by_project || {}
              }
            });
          } catch (err) {
            // SPEC R6: never 'healthy' on exception
            res.status(503).json({
              status: 'success',
              data: { status: 'unknown', overallStatus: 'unknown', upstream: 'unreachable', error: err.message }
            });
          }
        }

    REPLACE the body of `handleGetHealthReport` (around line 1000-1030 — the `readFileSync(reportPath, ...)` block at ~1018) with the same pattern: fetch `/health/state`, reshape into the existing constraint-dashboard envelope (preserving the legacy keys the constraint frontend reads).

    PRESERVE the file's existing imports and unrelated handlers (LSL registry reads at ~157, ~668, ~836 — those use `readFileSync` for other purposes; do NOT touch). Only the two health-verifier reader handlers change.

    SUBMODULE REBUILD STEPS (CLAUDE.md "Submodules & Build Pipeline" — REQUIRED, recurring failure mode):

    1. Determine if `npm run build` is needed by checking the submodule's package.json:

       grep -E '"build"|"start"|"main"' integrations/mcp-constraint-monitor/package.json

       If `package.json` has a `"build"` script and `"main"` points at a `dist/` path, then YES, build is required.

    2. Build the submodule (if required):

       cd /Users/Q284340/Agentic/coding/integrations/mcp-constraint-monitor && npm run build

    3. Rebuild + restart the Docker container so the new code is loaded:

       cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services

    Re-validate the source file with `node --check integrations/mcp-constraint-monitor/src/dashboard-server.js`.
  </action>
  <verify>
    <automated>node --check integrations/mcp-constraint-monitor/src/dashboard-server.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check integrations/mcp-constraint-monitor/src/dashboard-server.js` exits 0
    - `grep -c "HEALTH_COORDINATOR_URL" integrations/mcp-constraint-monitor/src/dashboard-server.js` is at least 1
    - `grep -c "fetch.*health/state" integrations/mcp-constraint-monitor/src/dashboard-server.js` is at least 1
    - `! grep -nE "readFileSync.*\.health/" integrations/mcp-constraint-monitor/src/dashboard-server.js` exits 0 (no legacy file reads in any handler — closes RESEARCH §8 leak)
    - `! grep -nE "catch[^/]*return\s*['\"]healthy|catch[^/]*overallStatus\s*:\s*['\"]healthy" integrations/mcp-constraint-monitor/src/dashboard-server.js` exits 0 (SPEC R6)
    - The `import { readFileSync, ... } from 'fs'` line at the top is preserved (other unrelated handlers in the file still use it for LSL registry reads — verify: `grep -c "import.*readFileSync.*from 'fs'\|require.*readFileSync\|readFileSync.*lsl-registry" integrations/mcp-constraint-monitor/src/dashboard-server.js` is at least 1)
    - Submodule rebuild done (if applicable): if `integrations/mcp-constraint-monitor/package.json` has a `"build"` script, then `integrations/mcp-constraint-monitor/dist/` mtime is newer than `integrations/mcp-constraint-monitor/src/dashboard-server.js` mtime — verify: `[ "$(stat -f %m integrations/mcp-constraint-monitor/dist/dashboard-server.js 2>/dev/null || stat -c %Y integrations/mcp-constraint-monitor/dist/dashboard-server.js 2>/dev/null || echo 0)" -ge "$(stat -f %m integrations/mcp-constraint-monitor/src/dashboard-server.js 2>/dev/null || stat -c %Y integrations/mcp-constraint-monitor/src/dashboard-server.js 2>/dev/null || echo 0)" ]` (this acceptance applies only IF the submodule has a build step; if `package.json` has no `"build"` script, this check is N/A — record N/A in summary)
    - Docker rebuild done: `docker ps --filter name=coding-services --format '{{.Status}}'` shows the container is running and `docker inspect coding-services --format '{{.Created}}'` is recent (within last hour). If image was rebuilt, the running container's image SHA must match the freshly built image. Verify: `docker inspect coding-services --format '{{.Image}}'` returns the current image, and `docker history` on that image shows the latest layer is recent.
  </acceptance_criteria>
  <done>
    `integrations/mcp-constraint-monitor/src/dashboard-server.js` reverse-proxies its 2 health-verifier reader handlers to the coordinator. RESEARCH §8 leak closed. Submodule built (if applicable per package.json) and Docker container rebuilt + restarted per CLAUDE.md submodule rules.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container → Host | The two dashboard backends inside `coding-services` reach the host coordinator via `host.docker.internal:3034`. Trust scope: anyone with shell on the container can read the SoT or trigger `/health/refresh`. Localhost-only deployment per SPEC. |
| Prompt-hook → Coordinator | The hook runs in the user's Claude Code session, fetches state, formats prompt context. A malicious coordinator response could in principle inject content into prompts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-05-01 | T (Tampering) | A modified `HEALTH_COORDINATOR_URL` env var (e.g., docker-compose tampering) could redirect the dashboard to a hostile listener | accept | URL is read-only, set in `docker-compose.yml`, not user-input. Same scope as `OBS_API_URL` which already uses this pattern. Out of scope per SPEC. |
| T-33-05-02 | I (Information Disclosure) | The prompt-hook could emit attacker-controlled coordinator content into Claude prompts | mitigate | `deriveSummary` only extracts known fields (`container.healthcheck`, `databases.status`, `services[*].status`, `lsl_by_project[*]`) and converts each to a fixed-format string. No unsanitized passthrough. The Q3 carve-out for out-of-coding-env returns `additionalContext: ''` (empty), eliminating any cwd-driven injection vector. |
| T-33-05-03 | D (DoS) | Coordinator unreachable → dashboard returns 503 forever; constraint dashboard panel goes grey | accept | This is the desired SPEC R6 surface (`overallStatus: 'unknown'`). Operator response is to investigate why the coordinator is down. |
| T-33-05-04 | S (Spoofing) | Anyone on the host could spoof the coordinator response | accept | Localhost-only deployment per SPEC; same scope as the rest of phase 33 threat model. |
| T-33-05-05 | E (Elevation) | The reverse proxy passes request headers / methods upstream; a crafted client request could manipulate the coordinator | mitigate | `_forwardCoordinator` explicitly sets `method: req.method, headers: { 'Content-Type': 'application/json' }` — does NOT forward arbitrary headers. The coordinator's input validation (in `ingestSignal`) is the second line of defense. |
</threat_model>

<verification>
- `scripts/health-prompt-hook.js` does a single GET `/health/state`; preserves the SPEC R8 JSON shape and the Q3 graceful exit-0 branch.
- `integrations/system-health-dashboard/server.js` 4 routes reverse-proxy via `_forwardCoordinator`. `checkDaemonHeartbeat` and `restartDaemon` deleted. No `execSync(verifier-script)` or `spawn(verifier-script)`. No `--force-recreate`. No `readFileSync(.health/...)`.
- `integrations/mcp-constraint-monitor/src/dashboard-server.js` 2 health-verifier reader routes reverse-proxy. Other unrelated handlers (LSL registry reads, violations file reads) untouched. RESEARCH §8 leak closed per Q2.
- All three files: SPEC R6 grep gate clean (`! grep -nE "catch[^/]*return\s*['\"]healthy" ...`).
- SPEC AC #7 grep gate clean: `! grep -rE "readFileSync.*\.health/(verification|.*-transcript-monitor)" scripts/health-prompt-hook.js scripts/statusline-health-monitor.js integrations/system-health-dashboard/server.js integrations/mcp-constraint-monitor/src/dashboard-server.js` exits 0.
- mcp-constraint-monitor submodule rebuilt + Docker container rebuilt per CLAUDE.md submodule rules.
</verification>

<success_criteria>
- [ ] All three files pass `node --check`
- [ ] All three files honor SPEC R6 (no fallback-to-healthy on errors; coordinator unreachable returns `overallStatus: 'unknown'`)
- [ ] Prompt-hook output JSON shape preserved (SPEC R8 / AC #8): `hookSpecificOutput.additionalContext`
- [ ] All four `/api/health-verifier/*` routes preserved on dashboard backend (SPEC R8 / AC #9)
- [ ] mcp-constraint-monitor migration in scope per Q2 (RESEARCH §8 leak closed)
- [ ] Q3 graceful exit branch preserved in prompt-hook
- [ ] Submodule build + Docker rebuild executed per CLAUDE.md
</success_criteria>

<output>
After completion, create `.planning/phases/33-health-monitoring-consolidation/33-05-SUMMARY.md` documenting:
- Lines modified per file
- Submodule build invocation result (success / no-build-step / failure with details)
- Docker rebuild result
- Confirmation that SPEC R6 + SPEC AC #7 grep gates pass on all four migrated files
- Confirmation that prompt-hook output JSON shape unchanged when coordinator unreachable
- Note: legacy daemons + plist + rules-config still untouched; cutover is plan 33-06 + 33-07
</output>
