---
phase: 33-health-monitoring-consolidation
plan: 15
type: execute
wave: 4
gap_closure: true
closes_gaps: [G7]
supersedes: 33-12  # 33-12's plist-propagation hypothesis was falsified empirically; this plan replaces it with a test-injection HTTP endpoint
depends_on: ["33-09", "33-11"]
files_modified:
  - scripts/health-coordinator.js
  - scripts/__tests__/health-coordinator/injection.test.sh
autonomous: true
requirements: [R6, R8]
must_haves:
  truths:
    - "scripts/health-coordinator.js has a POST /test/inject route guarded by 127.0.0.1-only request origin (loopback gate) — AC#13 unblocked without launchd interaction"
    - "POST /test/inject body { kind: 'db_health'|'container'|'docker', mode: 'throw'|'fail' } sets a per-process injection flag the existing check dispatcher already honors via process.env.HEALTH_COORDINATOR_INJECT_THROW (or equivalent in-memory state)"
    - "POST /test/inject body { reset: true } clears all injection flags"
    - "Non-loopback requests get 403; SPEC R6 satisfied (never returns 'healthy' on injection-induced exception)"
    - "scripts/__tests__/health-coordinator/injection.test.sh uses curl POST /test/inject (NOT launchctl setenv) — runs against the live coordinator on :3034 with no plist mutations"
    - "AC#13 (injection: forced throw → unknown) PASSES end-to-end"
  artifacts:
    - path: "scripts/health-coordinator.js"
      provides: "POST /test/inject endpoint with loopback gate; in-memory injection flags read by check dispatcher; POST /test/reset clears flags"
      contains: "/test/inject"
    - path: "scripts/__tests__/health-coordinator/injection.test.sh"
      provides: "Test uses HTTP injection vector; runs in <30s; resets on exit"
      contains: "/test/inject"
  key_links:
    - from: "scripts/__tests__/health-coordinator/injection.test.sh"
      to: "coordinator's POST /test/inject"
      via: "curl loopback POST"
      pattern: "curl.*localhost:3034/test/inject"
    - from: "POST /test/inject handler"
      to: "in-memory flag → existing INJECT_THROW handling in pollDatabases / pollDockerHealth / etc."
      via: "shared module state in health-coordinator.js"
      pattern: "process.env.HEALTH_COORDINATOR_INJECT_THROW|injectionFlags"
---

<objective>
Replace the falsified plist-propagation approach (plan 33-12, supersedes) with a test-only POST `/test/inject` HTTP endpoint on the health coordinator. Gated to loopback (127.0.0.1) requests only — no `NODE_ENV=test` dependency, since the production coordinator runs without that env. Consumed by `scripts/__tests__/health-coordinator/injection.test.sh` for SPEC AC #13 verification.

This closes G7 (the only outstanding gap from 33-VERIFICATION.md after waves 1-3). After this plan + a final 33-08 acceptance re-run, all 13 SPEC ACs should PASS (with AC#2's documented LLM-CLI-proxy deviation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-health-monitoring-consolidation/33-SPEC.md (AC#13)
@.planning/phases/33-health-monitoring-consolidation/33-12-SUMMARY.md (the 302-line falsification report — explains WHY this plan exists)
@.planning/phases/33-health-monitoring-consolidation/33-08-SUMMARY.md (G7 fix-options inventory + AC#13 failure mode)
@scripts/health-coordinator.js (target — already reads `process.env.HEALTH_COORDINATOR_INJECT_THROW` per the falsified 33-12 evidence)
@scripts/__tests__/health-coordinator/injection.test.sh (target — currently uses launchctl setenv approach that doesn't propagate)

<interfaces>
**New endpoints on coordinator:**

```
POST http://localhost:3034/test/inject
  body: { "kind": "db_health" | "container" | "docker_health", "mode": "throw" | "fail" }
       OR { "reset": true }
  response: 200 { "ok": true, "active": [<list of active flags>] }
  loopback gate: 403 if remote address is not 127.0.0.1, ::1, or ::ffff:127.0.0.1
```

**Existing in-process consumer (already wired per 33-12 SUMMARY's evidence):**
- `scripts/health-coordinator.js` lines 60-65 read `process.env.HEALTH_COORDINATOR_INJECT_THROW` to inject errors into specific check classes. The new POST endpoint sets a parallel in-memory flag that the check dispatcher should consult IN ADDITION TO the env var (env var path stays for backward compat, just doesn't propagate via launchctl).

**Loopback gate logic:**
```javascript
const ip = req.socket.remoteAddress;
if (!['127.0.0.1', '::1', '::ffff:127.0.0.1'].includes(ip)) {
  return res.status(403).json({ error: 'loopback only' });
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add POST /test/inject and POST /test/reset endpoints with loopback gate; wire in-memory flags into existing check dispatch</name>
  <files>scripts/health-coordinator.js</files>
  <read_first>
    - scripts/health-coordinator.js full file (read to find: existing route registrations, the existing INJECT_THROW consumer at lines ~60-65, the per-check error handling that reads it)
    - .planning/phases/33-health-monitoring-consolidation/33-12-SUMMARY.md — the existing INJECT_THROW handler shape (it's already there, just unreachable via launchctl setenv)
  </read_first>
  <action>
    Edit `scripts/health-coordinator.js`. Add the following:

    1. Near the top of the file (alongside other module-level state), declare an in-memory flag map:

           // Phase 33-15: in-memory injection flags for AC#13 (replaces 33-12's plist
           // propagation approach which was falsified empirically — see 33-12-SUMMARY).
           // POST /test/inject sets these; POST /test/reset clears them. Loopback only.
           const injectionFlags = new Map();  // key: kind ('db_health' | 'container' | 'docker_health'), value: mode ('throw' | 'fail')

    2. Add a small helper function near other utility functions:

           function shouldInject(kind) {
             // Honor BOTH the in-memory flag (set by POST /test/inject) AND the
             // legacy env var (kept for backward compat / dev-time use). Either
             // path triggers the injection.
             const memMode = injectionFlags.get(kind);
             if (memMode) return memMode;
             const envMatch = process.env.HEALTH_COORDINATOR_INJECT_THROW;
             if (envMatch === kind) return 'throw';
             return null;
           }

    3. Update the existing INJECT_THROW consumers in the check dispatch (the agent should grep for `HEALTH_COORDINATOR_INJECT_THROW` in the file and replace each direct env read with a `shouldInject(<kind>)` call). Behavior preserved: when the function returns truthy, the check throws (mode='throw') or returns a 'fail' status (mode='fail'); when null, normal probe runs.

    4. Add the routes (in the same place where other routes like `/health`, `/health/state`, `/health/refresh`, `/signals` are registered):

           // POST /test/inject — loopback-only; sets in-memory injection flags for AC#13
           app.post('/test/inject', (req, res) => {
             const ip = req.socket.remoteAddress;
             if (!['127.0.0.1', '::1', '::ffff:127.0.0.1'].includes(ip)) {
               return res.status(403).json({ error: 'loopback only' });
             }
             const body = req.body || {};
             if (body.reset === true) {
               injectionFlags.clear();
               return res.json({ ok: true, active: [] });
             }
             const { kind, mode } = body;
             const VALID_KINDS = new Set(['db_health', 'container', 'docker_health']);
             const VALID_MODES = new Set(['throw', 'fail']);
             if (!VALID_KINDS.has(kind) || !VALID_MODES.has(mode)) {
               return res.status(400).json({ error: 'invalid kind or mode', valid_kinds: [...VALID_KINDS], valid_modes: [...VALID_MODES] });
             }
             injectionFlags.set(kind, mode);
             return res.json({ ok: true, active: [...injectionFlags.entries()].map(([k, m]) => ({ kind: k, mode: m })) });
           });

    5. Run `node --check scripts/health-coordinator.js`.

    6. Reload the live coordinator: `launchctl kickstart -k "gui/$UID/com.coding.health-coordinator"` then sleep 4 and verify `curl -fs http://localhost:3034/health` returns 200.

    7. Verify the endpoint works end-to-end:

           # Should set the flag
           curl -fs -X POST http://localhost:3034/test/inject \
             -H 'Content-Type: application/json' \
             -d '{"kind":"db_health","mode":"throw"}' | python3 -c "import json,sys; d=json.load(sys.stdin); assert d.get('ok'); print('SET ok')"

           # Wait one tick (~5s) for the next runAllChecks pass
           sleep 6

           # databases.status should now be 'unknown' (NOT 'healthy', SPEC R6)
           curl -fs http://localhost:3034/health/state | python3 -c "import json,sys; d=json.load(sys.stdin); s=d.get('databases',{}).get('status'); assert s=='unknown', f'expected unknown got {s}'; print('INJECTION ok: status=unknown')"

           # Reset
           curl -fs -X POST http://localhost:3034/test/reset \
             -H 'Content-Type: application/json' -d '{"reset":true}' >/dev/null || \
           curl -fs -X POST http://localhost:3034/test/inject \
             -H 'Content-Type: application/json' -d '{"reset":true}' >/dev/null

           # Wait one tick
           sleep 6

           # databases.status should recover
           curl -fs http://localhost:3034/health/state | python3 -c "import json,sys; d=json.load(sys.stdin); print('post-reset:', d.get('databases',{}).get('status'))"
  </action>
  <verify>
    <automated>node --check scripts/health-coordinator.js && curl -fs -X POST http://localhost:3034/test/inject -H 'Content-Type: application/json' -d '{"reset":true}' | grep -q '"ok":true'</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/health-coordinator.js` exits 0
    - `grep -c "/test/inject" scripts/health-coordinator.js` is at least 1
    - `grep -c "shouldInject" scripts/health-coordinator.js` is at least 2 (one declaration + one consumer at minimum)
    - `grep -c "loopback only" scripts/health-coordinator.js` is at least 1 (the 403 message)
    - `curl -fs -X POST http://localhost:3034/test/inject -d '{"kind":"db_health","mode":"throw"}' -H 'Content-Type: application/json'` returns 200 with `ok: true`
    - After setting the flag and waiting one tick: `curl -fs http://localhost:3034/health/state | jq -r .databases.status` returns `unknown` (NOT `healthy`)
    - After reset: `curl -fs http://localhost:3034/health/state | jq -r .databases.status` recovers (typically `healthy`)
    - From the container: `docker exec coding-services curl -fs -X POST http://host.docker.internal:3034/test/inject` returns 403 (loopback gate works — host.docker.internal is NOT 127.0.0.1)
  </acceptance_criteria>
  <done>
    Coordinator has a working POST /test/inject endpoint. Live verification confirms injection flips databases.status to 'unknown'; reset recovers it. Loopback gate verified by attempting the call from inside the Docker container (gets 403).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update injection.test.sh to use the HTTP injection vector</name>
  <files>scripts/__tests__/health-coordinator/injection.test.sh</files>
  <read_first>
    - scripts/__tests__/health-coordinator/injection.test.sh (current — uses launchctl setenv per the falsified plan; needs full rewrite)
    - .planning/phases/33-health-monitoring-consolidation/33-SPEC.md AC#13 specification
  </read_first>
  <action>
    Replace the test's launchctl-setenv approach with curl POST /test/inject:

        #!/usr/bin/env bash
        # SPEC AC#13: forced throw in db_health check → coordinator's databases.status = 'unknown'
        # Replaces the falsified 33-12 launchctl-setenv approach (see 33-12-SUMMARY.md).
        set -euo pipefail
        COORD="${HEALTH_COORDINATOR_URL:-http://localhost:3034}"

        echo "=== AC#13 — injection: forced throw → unknown ==="

        # Reset any leftover flags (idempotent — fresh start)
        curl -fs -X POST "$COORD/test/inject" -H 'Content-Type: application/json' -d '{"reset":true}' >/dev/null || {
          echo "FAIL: coordinator unreachable at $COORD"; exit 1;
        }

        # Inject db_health throw
        curl -fs -X POST "$COORD/test/inject" -H 'Content-Type: application/json' -d '{"kind":"db_health","mode":"throw"}' \
          | python3 -c "import sys,json; d=json.load(sys.stdin); assert d.get('ok'), 'set failed'" || {
          echo "FAIL: could not set injection flag"; exit 1;
        }
        echo "  injection flag set: db_health=throw"

        # Wait one tick (~5s default) plus margin
        sleep 7

        # Assert: databases.status is now 'unknown' (NOT 'healthy' — SPEC R6)
        STATUS=$(curl -fs "$COORD/health/state" | python3 -c "import json,sys; print(json.load(sys.stdin).get('databases',{}).get('status'))")
        if [ "$STATUS" = "unknown" ]; then
          echo "  PASS: databases.status=unknown (R6 satisfied — never healthy on inject-throw)"
        else
          echo "  FAIL: databases.status=$STATUS (expected 'unknown')"
          curl -fs -X POST "$COORD/test/inject" -H 'Content-Type: application/json' -d '{"reset":true}' >/dev/null || true
          exit 1
        fi

        # Reset
        curl -fs -X POST "$COORD/test/inject" -H 'Content-Type: application/json' -d '{"reset":true}' >/dev/null
        echo "  reset complete"

        # Optional recovery check — give one tick for normal probe
        sleep 7
        RECOVERED=$(curl -fs "$COORD/health/state" | python3 -c "import json,sys; print(json.load(sys.stdin).get('databases',{}).get('status'))")
        echo "  post-reset databases.status: $RECOVERED"

        echo "=== AC#13 — PASS ==="

    Make sure the script is executable (`chmod +x`), passes `bash -n`, and runs end-to-end against the live coordinator in under 30s.
  </action>
  <verify>
    <automated>bash -n scripts/__tests__/health-coordinator/injection.test.sh && timeout 30 bash scripts/__tests__/health-coordinator/injection.test.sh | grep -q "AC#13 — PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/__tests__/health-coordinator/injection.test.sh` exits 0
    - The script contains `curl.*test/inject` (uses HTTP, not launchctl)
    - The script does NOT contain `launchctl setenv` (the falsified approach is gone)
    - The script exits 0 in under 30s (vs the prior version which hung)
    - Output contains "AC#13 — PASS"
  </acceptance_criteria>
  <done>
    `scripts/__tests__/health-coordinator/injection.test.sh` uses the new HTTP injection vector, runs in <30s end-to-end, and PASSES AC#13.
  </done>
</task>

</tasks>

<verification>
- POST /test/inject endpoint registered, loopback-gated, accepts kind+mode + reset.
- shouldInject() helper unifies the in-memory flag and the legacy env var.
- Live verification: setting db_health=throw → after one tick, databases.status='unknown'; reset recovers.
- From container: same POST gets 403 (loopback gate works).
- injection.test.sh exits 0 in <30s.
- AC#13 PASSES end-to-end.
</verification>

<success_criteria>
- [ ] Task 1: POST /test/inject + reset endpoints; shouldInject helper wired into existing checks
- [ ] Task 2: injection.test.sh rewritten to use HTTP vector
- [ ] AC#13 PASSES (live verified)
- [ ] Loopback gate verified (container request → 403)
- [ ] No new env vars added to plist
- [ ] SUMMARY.md created and committed
</success_criteria>

<output>
After completion, create `.planning/phases/33-health-monitoring-consolidation/33-15-SUMMARY.md` documenting:
- Endpoint registration site + line numbers
- shouldInject helper consumers (which checks now go through it)
- Live verification: injection round-trip (set → tick → assert unknown → reset → recover)
- Container loopback-gate verification (403)
- AC#13 final result (PASS expected)
- Note on 33-12's leftover plist mutations (3 empty-default keys still on disk; harmless; can be reverted manually with `defaults delete` or by reverting the plist to its 33-02-original or running `launchctl bootout + bootstrap` cycles, but not required since the new approach doesn't depend on launchctl setenv)
</output>
