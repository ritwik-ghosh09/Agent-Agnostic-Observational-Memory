---
phase: 33-health-monitoring-consolidation
plan: 07
type: execute
wave: 5
depends_on: ["33-04", "33-05", "33-06"]
files_modified:
  - docker/docker-compose.yml
  - scripts/system-monitor-watchdog.js
  - scripts/global-process-supervisor.js
  - scripts/global-service-coordinator.js
  - scripts/global-lsl-coordinator.js
autonomous: false
requirements: [R1, R2, R5, R7, R9]
must_haves:
  truths:
    - "docker/docker-compose.yml has HEALTH_COORDINATOR_URL=http://host.docker.internal:3034 in coding-services.environment block"
    - "Four legacy daemon scripts deleted (system-monitor-watchdog, global-process-supervisor, global-service-coordinator, global-lsl-coordinator)"
    - "Legacy launchd plist com.coding.system-watchdog removed via launchctl bootout + rm"
    - "New launchd plist com.coding.health-coordinator loaded via launchctl bootstrap"
    - "Exactly one launchctl entry matches '^.*com.coding.' after cutover (SPEC AC #2)"
    - "coding-services container rebuilt + restarted with the new env var"
    - "Coordinator process is running (pgrep -f health-coordinator returns a PID)"
    - "Stale .health/<projectName>-transcript-monitor-health.json files removed"
  artifacts:
    - path: "docker/docker-compose.yml"
      provides: "HEALTH_COORDINATOR_URL env var added to coding-services.environment"
      contains: "HEALTH_COORDINATOR_URL"
    - path: "(deleted) scripts/system-monitor-watchdog.js"
    - path: "(deleted) scripts/global-process-supervisor.js"
    - path: "(deleted) scripts/global-service-coordinator.js"
    - path: "(deleted) scripts/global-lsl-coordinator.js"
  key_links:
    - from: "coding-services container"
      to: "host coordinator at host.docker.internal:3034"
      via: "HEALTH_COORDINATOR_URL env var"
      pattern: "HEALTH_COORDINATOR_URL.*host\\.docker\\.internal:3034"
    - from: "launchd"
      to: "scripts/health-coordinator.js"
      via: "com.coding.health-coordinator.plist (loaded)"
      pattern: "com\\.coding\\.health-coordinator"
---

<objective>
The cutover commit. Atomically:
1. Add `HEALTH_COORDINATOR_URL=http://host.docker.internal:3034` to `docker/docker-compose.yml` `coding-services.environment` (D-02).
2. Rebuild + restart `coding-services` so the in-container readers (dashboard backend, mcp-constraint-monitor backend) pick up the new env var.
3. Delete the four legacy daemon scripts: `scripts/system-monitor-watchdog.js`, `scripts/global-process-supervisor.js`, `scripts/global-service-coordinator.js`, `scripts/global-lsl-coordinator.js` (per SPEC R9).
4. Bootout + delete the legacy launchd plist `~/Library/LaunchAgents/com.coding.system-watchdog.plist`.
5. Bootstrap the new launchd plist `~/Library/LaunchAgents/com.coding.health-coordinator.plist` (created in plan 33-02; loaded for the first time here).
6. Clean up any remaining `.health/*-transcript-monitor-health.json` files (PATTERNS.md: ETM no longer writes them).

This plan is **autonomous: false** because it requires a `checkpoint:human-verify` step after cutover to ensure the user observes the new state before declaring victory. Cutover is by SPEC Constraints "Migration policy: cutover, not shadow"; rollback path = `launchctl bootout gui/$UID ~/Library/LaunchAgents/com.coding.health-coordinator.plist` + `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.coding.system-watchdog.plist` (which means we should NOT delete the legacy plist file from disk until rollback is no longer needed — but per SPEC R9 + RESEARCH §1, we DO delete it; rollback then requires reverting the git commit and rerunning launchctl).

Output:
- Compose file updated
- 4 legacy script files deleted
- 1 legacy plist booted out + deleted from disk
- 1 new plist bootstrapped (now actively running)
- Stale .health/*.json files removed
- Coordinator running and answering on :3034
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-health-monitoring-consolidation/33-SPEC.md
@.planning/phases/33-health-monitoring-consolidation/33-CONTEXT.md
@.planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md
@.planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md
@docker/docker-compose.yml
@.planning/phases/33-health-monitoring-consolidation/33-02-SUMMARY.md
@.planning/phases/33-health-monitoring-consolidation/33-04-SUMMARY.md
@.planning/phases/33-health-monitoring-consolidation/33-05-SUMMARY.md
@.planning/phases/33-health-monitoring-consolidation/33-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update docker/docker-compose.yml — add HEALTH_COORDINATOR_URL env var</name>
  <files>docker/docker-compose.yml</files>
  <read_first>
    - docker/docker-compose.yml lines 25-80 (the coding-services service block, specifically the `environment:` list around line 60 where `OBS_API_URL=http://host.docker.internal:12436` is already set)
    - docker/docker-compose.yml lines 130-150 (verify `extra_hosts: ["host.docker.internal:host-gateway"]` exists at lines ~131-132 — REQUIRED for Linux Docker reachability)
    - .planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md "docker/docker-compose.yml (ADD env var)" section
  </read_first>
  <action>
    Edit `docker/docker-compose.yml`. In the `coding-services.environment` block, ADD this line adjacent to the existing `OBS_API_URL` line:

        # Health Coordinator — host-side SoT for system health (Phase 33)
        # In-container readers (dashboard, constraint-monitor) reach the host
        # coordinator via host.docker.internal (extra_hosts wires this on Linux).
        - HEALTH_COORDINATOR_URL=http://host.docker.internal:3034

    DO NOT touch:
    - `extra_hosts` block at lines ~131-132 — already correct.
    - `healthcheck:` block at lines ~141-146 — Docker's own healthcheck stays the SoT for container health (SPEC R7).
    - Any port mapping — coordinator runs on the HOST, not inside the container; the container reaches it via host.docker.internal.

    Validate with `cd docker && docker-compose config >/dev/null` to confirm YAML is valid.
  </action>
  <verify>
    <automated>cd docker && docker-compose config >/dev/null 2>&1 && grep -q 'HEALTH_COORDINATOR_URL=http://host.docker.internal:3034' docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `cd docker && docker-compose config >/dev/null 2>&1` exits 0 (YAML still valid)
    - `grep -c 'HEALTH_COORDINATOR_URL=http://host.docker.internal:3034' docker/docker-compose.yml` is at least 1
    - The env var is INSIDE the `coding-services` block: confirm by `awk '/^[a-z_-]+:$/{section=$0} /HEALTH_COORDINATOR_URL/{print section}' docker/docker-compose.yml | grep -q 'coding-services:'`
    - `extra_hosts` block still present: `grep -c 'host.docker.internal:host-gateway' docker/docker-compose.yml` is at least 1
    - No port mapping for 3034 was added (coordinator is host-only): `! grep -E '"?3034:3034"?' docker/docker-compose.yml` exits 0
  </acceptance_criteria>
  <done>
    `docker/docker-compose.yml` declares `HEALTH_COORDINATOR_URL=http://host.docker.internal:3034` in the `coding-services.environment` block. YAML still valid. `extra_hosts` and `healthcheck` blocks untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rebuild + restart coding-services container so in-container readers pick up the new env var</name>
  <files>(none — Docker operation only)</files>
  <read_first>
    - CLAUDE.md "Rebuilding After Code Changes" section
    - docker/docker-compose.yml (verify the env var landed in Task 1)
  </read_first>
  <action>
    Run the standard CLAUDE.md rebuild protocol:

        cd /Users/Q284340/Agentic/coding/docker && docker-compose build coding-services && docker-compose up -d coding-services

    The build can take 3-5 min per CLAUDE.md feedback rules. After the build completes, the container should automatically restart.

    Verify the container picked up the env var:

        docker exec coding-services printenv HEALTH_COORDINATOR_URL

    Expected output: `http://host.docker.internal:3034`
  </action>
  <verify>
    <automated>docker exec coding-services printenv HEALTH_COORDINATOR_URL | grep -q 'http://host.docker.internal:3034'</automated>
  </verify>
  <acceptance_criteria>
    - `docker ps --filter name=coding-services --format '{{.Status}}'` shows the container is running with status containing `Up`
    - `docker exec coding-services printenv HEALTH_COORDINATOR_URL` outputs `http://host.docker.internal:3034`
    - Container's healthcheck eventually returns healthy: `docker inspect coding-services --format '{{.State.Health.Status}}'` returns `starting` or `healthy` (not `unhealthy`)
    - Dashboard backend on `:3032` is reachable (still up after restart): `curl -fs -o /dev/null http://localhost:3032/api/health` exits 0 (or, if coordinator is down, returns 503 with `overallStatus: 'unknown'` per plan 33-05 — both acceptable since coordinator hasn't been bootstrapped yet)
  </acceptance_criteria>
  <done>
    `coding-services` container is rebuilt, restarted, and now has `HEALTH_COORDINATOR_URL=http://host.docker.internal:3034` in its env. Dashboard backend reachable.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete four legacy daemon scripts (SPEC R9)</name>
  <files>
    scripts/system-monitor-watchdog.js,
    scripts/global-process-supervisor.js,
    scripts/global-service-coordinator.js,
    scripts/global-lsl-coordinator.js
  </files>
  <read_first>
    - .planning/phases/33-health-monitoring-consolidation/33-PATTERNS.md "MODIFY (config / compose / delete)" table
    - .planning/phases/33-health-monitoring-consolidation/33-SPEC.md R9 (single-process supervision tree)
  </read_first>
  <action>
    Use `git rm` to delete the four legacy daemon scripts (so the deletion is staged for the cutover commit):

        git rm scripts/system-monitor-watchdog.js
        git rm scripts/global-process-supervisor.js
        git rm scripts/global-service-coordinator.js
        git rm scripts/global-lsl-coordinator.js

    DO NOT delete:
    - `scripts/health-verifier.js` (kept as reporter — plan 33-04 reduced it)
    - `scripts/statusline-health-monitor.js` (kept as reader+reporter — plan 33-04 reduced it)
    - `scripts/health-coordinator.js` (the new owner)
    - `scripts/health-prompt-hook.js` (kept as consumer — plan 33-05 migrated it)
    - `scripts/enhanced-transcript-monitor.js` (kept as reporter — plan 33-04 added signals)
    - `scripts/process-state-manager.js` (kept; coordinator uses PSM in-process)
    - `scripts/health-remediation-actions.js` (kept; plan 33-06 cleaned it up)

    After deletion, audit for any straggler imports of the deleted files anywhere in the repo (these would break at runtime):

        grep -rE "system-monitor-watchdog|global-process-supervisor|global-service-coordinator|global-lsl-coordinator" \
          scripts/ integrations/ lib/ tests/ 2>&1 \
          | grep -vE '^Binary file|/.git/|/node_modules/|/\\.planning/|\\.md:|\\.json:|\\.log:'

    Expected: zero matches in source code (some matches in `.planning/` docs and SUMMARY.md files are fine; only code references break things).

    If any source file still imports a deleted script, that import must be cleaned up. Most likely candidate: a stale `import` in another script that this plan missed. Plan 33-04's reduction of `statusline-health-monitor.js` already removed `getGlobalCodingMonitorHealth` (which called global-service-coordinator). Plan 33-05's migration of `health-prompt-hook.js` removed the `global-lsl-coordinator` spawn. So a clean grep is expected — but DO check.
  </action>
  <verify>
    <automated>! test -f scripts/system-monitor-watchdog.js && ! test -f scripts/global-process-supervisor.js && ! test -f scripts/global-service-coordinator.js && ! test -f scripts/global-lsl-coordinator.js</automated>
  </verify>
  <acceptance_criteria>
    - All four files no longer exist on disk: `! test -e scripts/system-monitor-watchdog.js && ! test -e scripts/global-process-supervisor.js && ! test -e scripts/global-service-coordinator.js && ! test -e scripts/global-lsl-coordinator.js`
    - Files are staged for deletion in git: `git status --short scripts/system-monitor-watchdog.js scripts/global-process-supervisor.js scripts/global-service-coordinator.js scripts/global-lsl-coordinator.js 2>&1 | grep -E '^D' | wc -l` is at least 4 (or git already shows them as deleted in `git status`)
    - No source file imports the deleted scripts (audit grep above returns zero matches in `scripts/`, `integrations/`, `lib/`, `tests/`): `! grep -rEl "system-monitor-watchdog|global-process-supervisor|global-service-coordinator|global-lsl-coordinator" scripts/ integrations/ lib/ tests/ --include='*.js' --include='*.mjs' --include='*.ts' --include='*.tsx' 2>/dev/null` exits 0
    - The four kept files (health-verifier, statusline-health-monitor, health-coordinator, health-prompt-hook, enhanced-transcript-monitor, process-state-manager, health-remediation-actions) still pass `node --check`: run for each.
  </acceptance_criteria>
  <done>
    Four legacy daemon scripts deleted via `git rm`. No source file in the repo references them. The seven kept scripts (5 reduced/migrated + 2 untouched) all pass `node --check`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: launchctl cutover — bootout legacy, bootstrap new, clean stale .health files</name>
  <files>
    ~/Library/LaunchAgents/com.coding.system-watchdog.plist (deleted),
    ~/Library/LaunchAgents/com.coding.health-coordinator.plist (loaded; created in plan 33-02)
  </files>
  <read_first>
    - .planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md "1. launchd specifics" — verbatim launchctl bootout / bootstrap commands
    - .planning/phases/33-health-monitoring-consolidation/33-RESEARCH.md "9. Pitfalls" — the bootout-vs-unload note (macOS 14+ uses bootout)
    - .planning/phases/33-health-monitoring-consolidation/33-SPEC.md AC #1, #2, #11 (launchctl entries + respawn)
  </read_first>
  <action>
    Execute the launchctl cutover. Sequence is intentional:

    Step 1 — Confirm the legacy plist is currently loaded:

        launchctl list | grep com.coding.system-watchdog || echo "(legacy plist not loaded — that's fine, skip Step 2)"

    Step 2 — Bootout the legacy plist (W9 fail-loud variant; macOS 14+ syntax per RESEARCH §9):

        if launchctl list | grep -q '^.*com\.coding\.system-watchdog'; then
          launchctl bootout gui/$UID ~/Library/LaunchAgents/com.coding.system-watchdog.plist \
            || { echo "FATAL: bootout of com.coding.system-watchdog failed"; exit 1; }
        else
          echo "Note: legacy plist com.coding.system-watchdog not loaded — skipping bootout"
        fi

    Why fail-loud (W9): the prior `|| true` masked a real bootout failure (e.g., the plist is loaded but `launchctl bootout` returns non-zero for a reason other than "not loaded"). With the conditional + hard-fail, the only swallowed exit code is the explicitly-checked "not loaded" path. After this step, the legacy plist MUST be unloaded — the next acceptance gate enforces that explicitly.

    Step 3 — Delete the legacy plist file from disk:

        rm -f ~/Library/LaunchAgents/com.coding.system-watchdog.plist

    Step 4 — Verify the new plist file exists on disk (created by plan 33-02):

        test -f ~/Library/LaunchAgents/com.coding.health-coordinator.plist || { echo "FATAL: plan 33-02 plist missing"; exit 1; }

    Step 5 — Free port 3034 if anything is squatting on it (RESEARCH §9 pitfall: stale process holding port across cutover):

        port_holder=$(lsof -ti :3034 2>/dev/null || true)
        if [ -n "$port_holder" ]; then
          echo "Port 3034 held by PID $port_holder; killing it"
          kill -9 $port_holder || true
          sleep 1
        fi

    Step 6 — Bootstrap the new plist:

        launchctl bootstrap gui/$UID ~/Library/LaunchAgents/com.coding.health-coordinator.plist

    Step 7 — Wait up to 30s for the coordinator to come up:

        for i in $(seq 1 30); do
          if curl -fs http://localhost:3034/health >/dev/null 2>&1; then
            echo "coordinator up after ${i}s"
            break
          fi
          sleep 1
        done

    Step 8 — Clean up any stale `.health/<projectName>-transcript-monitor-health.json` files (PATTERNS.md: ETM no longer writes them; remove the four files from Phase A's earlier cleanup if any reappeared since):

        rm -f /Users/Q284340/Agentic/coding/.health/*-transcript-monitor-health.json
        rm -f /Users/Q284340/Agentic/coding/.health/.lsl-recovery-lock
        rm -f /Users/Q284340/Agentic/coding/.health/lsl-watchdog-heartbeat.json

    DO NOT delete:
    - `.health/verification-status.json` or `.health/verification-report.json` if they happen to still exist — they are not actively written anymore (plan 33-04 stripped the writer in `health-verifier.js`); leaving them around is harmless and would only matter if a consumer still reads them, which plans 33-04/05 already proved is not the case (grep gates clean).
    - `.global-lsl-registry.json` if it exists — out of scope.

    Validate the cutover by running the smoke test from plan 33-01:

        bash scripts/__tests__/health-coordinator/quick.sh
  </action>
  <verify>
    <automated>! launchctl list | grep -q com.coding.system-watchdog && launchctl list | grep -q com.coding.health-coordinator && curl -fs http://localhost:3034/health >/dev/null && bash scripts/__tests__/health-coordinator/quick.sh</automated>
  </verify>
  <acceptance_criteria>
    - W9 post-bootout assertion: after Step 2, `! launchctl list | grep -q '^.*com\.coding\.system-watchdog'` exits 0 (legacy plist must be unloaded; if `launchctl list` still shows it, the conditional bootout in Step 2 failed and the script exited 1 — this assertion is what the fail-loud variant guarantees)
    - Legacy plist file deleted: `! test -e ~/Library/LaunchAgents/com.coding.system-watchdog.plist`
    - Legacy plist no longer in launchctl: `! launchctl list | grep -q com.coding.system-watchdog`
    - New plist loaded: `launchctl list | grep -q com.coding.health-coordinator`
    - Exactly ONE com.coding.* entry (SPEC AC #2): `[ "$(launchctl list | grep -c '^.*com\\.coding\\.')" = 1 ]`
    - Coordinator process running: `pgrep -f health-coordinator.js | head -1 | grep -E '^[0-9]+$'` returns a PID
    - Coordinator HTTP reachable: `curl -fs http://localhost:3034/health` exits 0
    - All 7 SPEC AC #3 top-level keys present in `/health/state`: `curl -fs http://localhost:3034/health/state | jq -e '.container, .services, .lsl, .lsl_by_project, .processes, .generated_at, .coordinator_uptime_s' >/dev/null` exits 0
    - Container can reach coordinator (SPEC AC #4): `docker exec coding-services curl -fs http://host.docker.internal:3034/health/state | jq -e '.container.healthcheck' >/dev/null` exits 0
    - Stale per-project transcript-monitor-health files removed: `! ls /Users/Q284340/Agentic/coding/.health/*-transcript-monitor-health.json 2>/dev/null` exits 0
    - quick.sh smoke test passes: `bash scripts/__tests__/health-coordinator/quick.sh` exits 0
  </acceptance_criteria>
  <done>
    Cutover complete. Exactly one `com.coding.*` launchctl entry: `com.coding.health-coordinator`. Coordinator answers HTTP on :3034 from both host and container. Stale `.health/*-transcript-monitor-health.json` files cleaned up. Smoke test passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verify — cutover sanity check</name>
  <files>(none — human-only verification)</files>
  <action>This is a checkpoint:human-verify task. Execute the steps in <how-to-verify> below. Do not proceed until the human approves with the resume signal.</action>
  <what-built>
    Phase 33 cutover is complete: 4 legacy daemons deleted, 1 legacy plist booted out + removed, 1 new coordinator plist running, container env var landed, in-container readers (dashboard at :3032, constraint dashboard at :3030) now reverse-proxy to coordinator.
  </what-built>
  <how-to-verify>
    1. Open `http://localhost:3032` in a browser. The system-health dashboard should render. The "Health Status" card should show data sourced from the new coordinator (overallStatus and lastUpdate should be plausible — e.g., overallStatus is `healthy` if the docker container is healthy, `unknown` only if the coordinator was down).
    2. Click the "Run Verification" button. The dashboard should refresh; the lastUpdate timestamp should advance. (This proves D-04: button → POST /health/refresh → coordinator force-tick → updated state.)
    3. Open `http://localhost:3030`. The constraint dashboard should still render (RESEARCH §8 leak closed). Its health-verifier panel should populate from coordinator data, not silently break.
    4. Run `tail -f /Users/Q284340/Agentic/coding/.logs/health-coordinator.log` in a terminal — you should see periodic activity (5s tick logs).
    5. Run `pgrep -fl 'system-monitor-watchdog|global-process-supervisor|global-service-coordinator|global-lsl-coordinator'` — expected: empty (SPEC AC #1).
    6. Run `launchctl list | grep com.coding` — expected: exactly one entry, `com.coding.health-coordinator` (SPEC AC #2).
    7. (Optional but recommended) Open two `coding/bin/coding` panes in tmux against this repo. Verify the dashboard's LSL panel shows two distinct sessions; the statusline shows both alive; close one pane and verify the dashboard still says project healthy with one session stopped (the headline two-session test from SPEC AC #5).

    If any of items 1-5 fail, do NOT approve — instead:
    - Capture `cat /Users/Q284340/Agentic/coding/.logs/health-coordinator.log | tail -50`
    - Capture `launchctl list com.coding.health-coordinator`
    - Capture `curl -s http://localhost:3034/health/state | jq` (or note the curl error)
    - Decide whether to roll back: `launchctl bootout gui/$UID ~/Library/LaunchAgents/com.coding.health-coordinator.plist` then revert the git commit.

    Item 7 (two-session manual verification) is the human-only complement to the scripted `two-session-agreement.test.sh` from plan 33-08 — both should pass.
  </how-to-verify>
  <resume-signal>Type "approved" when items 1-6 (and ideally 7) all pass; otherwise describe the failure and the captured diagnostics.</resume-signal>
  <verify>
    <automated>echo "human-verify checkpoint — see how-to-verify steps; approval required"</automated>
  </verify>
  <done>Human approves via the resume signal after completing the how-to-verify steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk → launchd | The new plist file at `~/Library/LaunchAgents/com.coding.health-coordinator.plist` is now an active launchd job. Any future write to that path is privileged code on this user account. |
| launchd → Coordinator process | launchd respawns the coordinator on any exit; ThrottleInterval=30 caps the loop. |
| Container → Host | `coding-services` reaches `host.docker.internal:3034`; `extra_hosts: ["host.docker.internal:host-gateway"]` makes this work on Linux. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07-01 | T (Tampering) | A modified `~/Library/LaunchAgents/com.coding.health-coordinator.plist` could redirect launchd to a malicious binary | accept | Same threat scope as any user LaunchAgent. macOS user-account ACLs apply. |
| T-33-07-02 | D (DoS) | Cutover failure leaves no health monitoring at all | mitigate | Task 5 is a human-verify checkpoint; if cutover fails, the human rolls back via `launchctl bootout` + git revert. The legacy plist file is on disk in git history (deleted-not-gone). |
| T-33-07-03 | E (Elevation) | Coordinator process runs as the user; no privilege escalation introduced | accept | Same scope as the legacy daemons. |
| T-33-07-04 | I (Information Disclosure) | New env var in `docker-compose.yml` is committed to repo | accept | Only the URL (`http://host.docker.internal:3034`) is committed; no secrets. Same shape as `OBS_API_URL`. |
| T-33-07-05 | T (Tampering) | A malicious bind mount could replace `scripts/health-coordinator.js` at runtime; launchd would load the malicious code on next respawn | accept | Filesystem ACL is the dev box's user account scope. Out of scope per SPEC. |
</threat_model>

<verification>
- `docker-compose.yml` has `HEALTH_COORDINATOR_URL=http://host.docker.internal:3034` in coding-services environment.
- `docker exec coding-services printenv HEALTH_COORDINATOR_URL` outputs the expected URL.
- 4 legacy daemon scripts removed from disk + git index.
- Legacy plist booted out + file deleted.
- New plist loaded; coordinator process running; HTTP reachable.
- SPEC AC #2: exactly one com.coding.* launchctl entry.
- SPEC AC #3: /health/state has all 7 top-level keys.
- SPEC AC #4: container can fetch /health/state via host.docker.internal.
- Smoke test (`scripts/__tests__/health-coordinator/quick.sh`) passes.
- Human verifies: dashboard at :3032 still works; constraint dashboard at :3030 still works; "Run Verification" button forwards to /health/refresh.
</verification>

<success_criteria>
- [ ] docker-compose.yml has HEALTH_COORDINATOR_URL env var
- [ ] coding-services container rebuilt + restarted with new env
- [ ] 4 legacy daemon scripts deleted (git rm)
- [ ] Legacy launchd plist booted out + file deleted
- [ ] New launchd plist loaded (launchctl bootstrap)
- [ ] Exactly 1 com.coding.* launchctl entry
- [ ] Coordinator running, HTTP reachable from host AND container
- [ ] /health/state has all 7 SPEC AC #3 top-level keys
- [ ] Smoke test passes
- [ ] Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/33-health-monitoring-consolidation/33-07-SUMMARY.md` documenting:
- Sequence of cutover steps executed
- Coordinator PID + uptime as of cutover completion
- launchctl list output (exactly 1 com.coding.* entry)
- Curl probe results for /health/state from host AND container
- Confirmation that human checkpoint approved
- Any deviation from the plan (e.g., port 3034 was held by stale process; killed it)
</output>
